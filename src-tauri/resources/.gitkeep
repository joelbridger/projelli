# Projelli bundled resources
#
# Files under this directory are copied into the app's resource folder at
# install time via `tauri.conf.json:bundle.resources`.
#
# Phase 3 (M1) adds the e5-small ONNX model + tokenizer under `embeddings/`.
