/**
 * Keeps tsconfig.contract.json valid before a cross-boundary contract lands on
 * a branch. The real modules and tests are added to that project by glob.
 */
export const CONTRACT_GATE_FIXTURE = true;
