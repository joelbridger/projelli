# Firm source-record archive doorway receipt (WB-138, strict fix round)

**Base SHA:** `8118b12cca5f05892e1418c254818268795694e8`

**Final verified source SHA:** `6aa2e10ad`

This receipt is an evidence-only follow-up to the verified source commit. Its
own final Git ID is self-referential, so the exact clean evidence-tip SHA is
pasted in the worker handoff.

## CHANGES-3 cures

1. The Settings descriptor now uses the new `data-export-backup` flag. The flag
   was added as exactly one append-only `defineFlag(...)` line, is owned by the
   `data-export-backup` lane, and defaults off through the registry helper.
   Focused registry coverage proves the panel is absent while dark and mounts
   only after the flag is explicitly enabled.
2. The user-facing copy now states the native writer's real inclusion rule:
   every stored CRM record carrying `sourceType`, `sourceId`, and
   `sourcePayload` is eligible, regardless of provider. It no longer describes
   the file as Wealthbox-only and does not claim that documents or email can
   never appear as CRM records.
3. A receipt alone no longer creates a success state. The renderer reads the
   written JSON file, checks its exact byte length and SHA-256 checksum, parses
   its manifest and record list, recomputes total/per-type counts, and requires
   every archived source type to have one matching fidelity row whose fetched,
   imported, skipped, and rejected counts reconcile. Missing coverage becomes
   **needs review**, never success.

No native writer, native command, migration serializer, import path, CRM-home
surface, or other fenced seam changed.

## Real-contract test boundary

`__fixtures__/writer-produced-archive.json` was generated by compiling the
exact existing native `export.rs` writer in a temporary harness and invoking
`write_decrypted_archive`. Its inputs deliberately contained one Wealthbox
record and one record from another provider; the output proves that provider is
not part of the archive selection rule. The repository's normal final newline
is included in the fixture bytes, and the test derives the receipt byte count
and checksum from those committed bytes.

The jsdom test still mocks two desktop edges: the Tauri command response and
the filesystem bridge. It therefore proves the real writer-shaped
manifest-parsing, checksum, reconciliation, and UI-claims boundary. It does not
prove live Tauri IPC or a fresh on-disk native write. Those remain outside this
UI-only fix because the native writer was explicitly fenced.

The claims regression test freezes the three pre-export content statements and
the entire successful result text. Adding an unproven content type or success
claim makes the test fail.

## Verification at `6aa2e10ad`

```text
$ npx vitest run src/features/data-portability/DataExportBackupSettings.test.tsx src/features/settings/registry/settingsModuleRegistry.test.ts tests/unit/architecture-boundaries.test.ts tests/unit/i18n/en-json-snapshot.test.ts

Test Files  4 passed (4)
Tests       28 passed (28)
```

```text
$ npm run typecheck
> tsc --noEmit
PASS (exit 0, no diagnostics)

$ npm run typecheck:tests
> tsc -p tsconfig.test.json --noEmit
PASS (exit 0, no diagnostics)
```

```text
$ npm run boundaries:check
✅ No feature-boundary regression (599 current baseline finding(s)).
```

```text
$ npx eslint <all touched TypeScript and TSX files>
PASS (no output)

$ npx prettier --check <touched feature, registry test, and flag registry>
All matched files use Prettier code style!

$ git diff --check
PASS (no output)
```

## Review status

The requested strict-review findings are cured. Different-model coordinator
review remains coordinator-owned; no builder-supplied reviewer verdict is
claimed.
