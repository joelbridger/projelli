/**
 * Public Documents feature API.
 *
 * Cross-feature consumers must import through this entry point so Documents
 * can keep its internal file layout private.
 */
export { DocumentsCreateMenu } from './DocumentsCreateMenu';
export type { DocumentsCreateMenuProps } from './DocumentsCreateMenu';
