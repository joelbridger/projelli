/**
 * MarketplaceSettings — placeholder page.
 *
 * Stream C will populate this with the browse/install UI for community
 * templates and plugins. For now it's a shell so the nav item resolves.
 */

export function MarketplaceSettings() {
  return (
    <div className="space-y-6">
      <div>
        <h2 className="text-2xl font-semibold tracking-tight">Marketplace</h2>
        <p className="text-sm text-muted-foreground mt-1">
          Templates and plugins from the community. Stream C will populate this page.
        </p>
      </div>
    </div>
  );
}
