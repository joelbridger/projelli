# Advisor Prep Hero 3.0 — Living Status (single source of truth)

> ## 🚀 LAUNCHED 2026-06-09
> **3.0 is publicly live.** (1) **App:** `v3.0.0` GitHub release published — signed installers for Windows, macOS (arm + Intel), and Linux, plus `latest.json`; the auto-update endpoint serves 3.0.0. (2) **Website:** keepance.com deployed on the 3.0 positioning + pricing (Solo $468 / Professional $948 / Firm $1,548 per seat per year) with the download page repointed to v3.0.0. (3) **Firm backend:** deployed + verified live at `https://api.keepance.com` (systemd `keepance-backend.service`, Bun on loopback :5194, behind the Cloudflare tunnel + Caddy; existing sites unaffected; rollback in `backend/deploy/RUNBOOK.md`). The desktop client (firmConfig + CSP) points at api.keepance.com. The license-validator maps the new Solo/Firm names + handles subscription webhooks.
> **Sales are wired (2026-06-10):** the 3 subscription products are live + verified, the validator runs on a live LS API key (real purchases validate), and Solo + Professional Subscribe links are on the pricing cards. Open sales bits (NOT bugs): founding-coupon products, Firm multi-seat, one real test purchase — see `docs/operations/2026-06-10-lemonsqueezy-3.0-LIVE.md`.
>
> **➡️ NEXT SESSION FOCUS: software bug review of the 3.0 app.** Start at `docs/operations/2026-06-10-NEXT-SESSION-bug-review.md`.

**Last updated:** 2026-06-09 (autonomous build + launch session)
**Branch:** `keepance-3.0` (all work here; pushed to origin). **3.0 is LIVE** (see the launch banner above).
**Read also:** the roadmap (`2026-06-09-keepance-3.0-roadmap.md`, the plan), the vision (`../research/2026-06-08-ux-attorney-v2.5.1/vision-most-viable-keepance.md`, the north star), and `2026-06-09-keepance-3.0-P0-results.md` (gate proofs). This STATUS file supersedes the roadmap's phase tracking as the current-state record.

## Where we are in one line
The core wedge is built and runs: Office-native editing + AI redline (tracked changes) + confidential, matter-scoped, cited recall + an honest trust layer + rebuilt onboarding. The firm-platform/cloud half and ship-hardening remain.

## Board decisions (locked 2026-06-09)
Commit to the firm platform (operate cloud infra); ship one big 3.0; build Word fidelity fully in-house.

## P0 — de-risk gates: ALL PASSED + verified
- Gate 1 in-house Word track-changes round-trip — `d9468be`, `spikes/docx-roundtrip/`.
- Gate 2 matter-scoped exact isolation + citation verify — `8e4c2c7`, `spikes/matter-retrieval/`.
- Gate 3 conflict-free CRDT collaboration + firm architecture decision — `d452543`, `spikes/firm-sync/` (`DECISION.md`).
Spikes are feasibility proofs that seeded production; see `spikes/README.md`.

## P1 — built, tested, pushed (commits on keepance-3.0)
| Piece | Status | Commit | Key files |
|---|---|---|---|
| OOXML engine (in-house, preserve-by-default, zip-bomb hardened) | DONE | `7ea2ae8`, `8b87c22` | `src-tauri/crates/keepance-docx/` |
| Accept/reject revisions (engine) | DONE | `1ddf8d9` | `crates/keepance-docx/src/resolve.rs` |
| Word-familiar editor (light theme, review pane) | DONE | `3183f6b` | `src/components/media/DocxEditor.tsx`, `src/types/docx.ts`, `src/utils/docx-*.ts` |
| AI redline (tracked changes from instruction; drift-safe batch) | DONE | `4af0de1` | `src/modules/docx/redline.ts`, `crates/keepance-docx/src/author.rs` |
| Matter-scoped cited retrieval engine | DONE | `8bbd77a` | `src-tauri/src/commands/rag/` |
| Matter model + scoped cited chat (UI) | DONE | `aff7fdd` | `src/stores/matterStore.ts`, `src/components/matter/`, `src/modules/memory/` |
| Egress indicator + data map + confidentiality modes | DONE | `aedbc9b` | `src/modules/privacy/`, `src/components/privacy/` |
| Ollama wired into chat + redline (Local-only truly local) | DONE | `f0b13cb` | `src/modules/models/providerFactory.ts`, `AIChatViewer.tsx` |
| Rebuilt onboarding (plain-English AI setup, 3 paths, data map) | DONE | `1b2ac4b` | `src/components/onboarding/AiSetupStep.tsx`, `FirstRunWizard.tsx` |

**Verification:** engine ~50 Rust tests, frontend ~2,292 vitest tests, `tsc --noEmit` clean, `npm run build` + `cargo build --bin keepance` pass. Re-run: `cargo test -p keepance-docx`, `cargo test --lib commands::rag`, `npx vitest run`, `npx tsc --noEmit`.

## Must-fix-before-ship gaps (surfaced, not buried)
1. **Mount the rebuilt `FirstRunWizard` in `App.tsx`** — it exists but only `WelcomeOnboardingDialog` + `FeatureTour` are wired, so the new onboarding is not the live first-run yet.
2. **Encrypt the vector store at rest** — residual plaintext embeddings + metadata in `.keepance/vectors/` (cross-matter isolation in-app is intact; this is a data-at-rest gap).
3. **Email→matter assignment + a `keepance:open-email` listener** (mail citations currently resolve to "unassigned" and have no open target).
4. Repo lint baseline is large/pre-existing; `tsc` is the gate; a test enforces the no-em-dash rule.

## Autonomous build queue (no Jameson needed) — priority order
1. Mount onboarding (closes gap #1).
2. Privilege/work-product tagging + exclude-from-retrieval (litigation safety).
3. Vector store at-rest encryption (closes gap #2).
4. PDF/xlsx/pptx export (the "real deliverables" job; A6).
5. Audit hardening: new events (retrieval/citation/privilege/scope/egress) + SQLCipher (the "defense file").
6. Email→matter assignment + open-email (closes gap #3; completes the email wedge).
7. Documentation/pricing/version reconciliation (research flagged inconsistency as a trust killer; WS-G).
8. MCP/plugin "Privileged Matter Mode" guardrails (WS-G).
9. Binary version history (A5).
10. Litigation associate workflows on Office + cited RAG (WS-D; un-draft templates only after design-partner review).

## Needs Jameson (cannot be automated)
- **Recruit design-partner firms** — outreach drafted at `design-partner-program/` (validation authority + attorney proof).
- **Firm cloud backend + assured zero-retention proxy** — infra to stand up (architecture decided in `spikes/firm-sync/DECISION.md`).
- **SOC 2 + DPA**, and the **deploy go-ahead** (commercial).

---

## Autonomous build progress (2026-06-09, continued)

Completed since the tables above (all tested + pushed to keepance-3.0):
- Onboarding mounted as live first-run (`392f4ec`) — closes gap #1.
- Privilege/work-product tagging + default retrieval exclusion (`7fa56cb`): composed `matter_id AND privilege='none'` prefilter; adversarial test proves a privileged top-hit is withheld; explicit non-persisted include-privileged toggle.
- A6 export: discoverable Word/PDF export + privilege-safe "clean copy" metadata scrub + xlsx/pptx round-trip (`86b21b1`).
- Email wedge completed: decrypted email viewer + `keepance:open-email` wired (cited emails open) + email->matter mapping, scoped + privilege-aware (`cfb894d`) — closes gap #3.

**Updated gaps:** #1 DONE, #3 DONE. Open: #2 vector store at-rest encryption; per-message mail privilege UI (engine path ready); es/de i18n drift in the `matter` namespace (needs a `translate-i18n` run with an API key).

**Remaining autonomous queue (priority order):** WS-D litigation associate workflows (Office output + cited RAG; flagship deposition contradiction-finder; keep `@draft` until design-partner review) → audit hardening (events + SQLCipher "defense file") → vector store at-rest encryption → MCP/plugin privileged-mode → binary version history (A5). Documentation/pricing/version reconciliation is DEFERRED: pricing is board-level and the live marketing site gets a full 3.0 repositioning, so it waits on Jameson's 3.0 positioning + pricing decision.

**Tests now:** frontend ~2360 vitest, Rust ~299 lib + engine integration, all green; `tsc` clean; builds pass.

**Verdict:** the confidential cited-recall + Office redline + real-deliverables + trust + onboarding wedge is built and runs end to end. Next: the litigation associate, then trust hardening, then the firm-platform/cloud half (needs Jameson's infra).

---

## FINAL STATE: autonomous build complete (2026-06-09)

The entire single-user product is built, tested, and green on `keepance-3.0` (all pushed). Final verification: frontend **2422 tests pass** (220 files, 0 failures), `tsc` clean, `cargo build --bin keepance` succeeds.

**Everything built (commits):** Office engine + Word editor + AI redline + accept/reject (`7ea2ae8` `8b87c22` `1ddf8d9` `3183f6b` `4af0de1`); matter-scoped cited retrieval + Matter model + cited chat (`8bbd77a` `aff7fdd`); trust layer egress/data-map/modes + Ollama-local honesty (`aedbc9b` `f0b13cb`); onboarding rebuilt + mounted (`1b2ac4b` `392f4ec`); privilege tagging + default-exclude (`7fa56cb`); Word/PDF export + metadata scrub (`86b21b1`); email viewer + open-email + matter mapping (`cfb894d`); litigation associate contradiction-finder (`a49e980`); encrypted audit defense-file + provenance events (`b2d2d01`) + detection fix (`6d4c7d0`); vector chunk_text encrypted at rest (`1e537d1`); MCP Privileged Matter Mode (`a1fc685`); binary version history + .docx default (`6ce1197`).

**The honest boundary:** the solo/single-user product is DONE. The FIRM PLATFORM half needs infrastructure + board decisions only Jameson can make, so the autonomous run stops here.

**NEEDS JAMESON (cannot be automated):**
1. **Recruit design-partner firms** — outreach drafted at `design-partner-program/`. Gates real-attorney UX validation and the un-drafting of the legal templates.
2. **Stand up the firm cloud backend + the assured zero-retention inference proxy** (architecture in `spikes/firm-sync/DECISION.md`). Until then, collaboration, multi-seat sync, and the "Assured" confidentiality mode remain stubs.
3. **License-server rewrite for per-org seats + user identity/auth** (depends on the backend).
4. **SOC 2 + DPA** (firm-buyer assurance).
5. **The 3.0 positioning + pricing decision (board-level)** — unblocks documentation/pricing/website reconciliation and the marketing repositioning.
6. **Deploy go-ahead** (commercial) when ready to ship.

**Smaller follow-ups (buildable later, non-blocking):** structural/track-change-level docx version diff; opaque ids for `source_id`/`matter_id` at rest (the metadata-map residual); per-message mail privilege UI; es/de i18n parity for the `matter` namespace (needs a translate-i18n run with an API key); CRDT integration into the live doc model (only useful once the relay backend exists).

**Verify:** `npx vitest run` · `npx tsc --noEmit` · `cargo build --bin keepance --manifest-path src-tauri/Cargo.toml` · `cargo test --manifest-path src-tauri/Cargo.toml` · `cd backend && bun test`.

---

## Items 2-5 delivered (autonomous, 2026-06-09 cont.)

Per Jameson's direction: do items 2-5 autonomously, lead with pricing (deep research first, then recommend, then build out, then present), and drop the design-partner recruiting.

- **#4 Pricing — DONE.** Deep research (cited, adversarially verified; run `wf_fc9dcbe8-692`) -> recommendation (`docs/strategy/2026-06-09-keepance-3.0-pricing.md`) -> built out (`src/config/pricing.ts` canonical source, in-app display, staged `website/3.0/pricing.html`, LemonSqueezy plan `docs/operations/2026-06-09-lemonsqueezy-3.0-setup.md`). Recommendation: per-seat ANNUAL subscription, three tiers — Solo $39/mo annual, Professional $79/mo, Firm $129/seat/mo (min 3); BYOK framed as transparency/savings; a hard "your files are always yours" data-ownership guarantee; founding rate; existing buyers grandfathered. Tier codes stay `personal|professional|practice`; display = Solo/Professional/Firm. (`cfd81ea`, `22879ea`)
- **#3 Assurance — DONE.** DPA template + trust/security overview (data-flow + retention map) + SOC 2 readiness mapping (`docs/legal/`, `docs/trust/`); verified against the real crypto/audit code; honest caveats (BYOK exposes prompts to the provider; a formed legal entity is the prerequisite for both). (`e106cb7`)
- **#2 Firm backend — BUILT (code complete, tested; not deployed).** New Bun service `backend/`: identity/auth + per-org licensing (signed Ed25519 seats, atomic seat-limit), the E2EE matter sync relay (opaque ciphertext, ethical-wall ACL, WebSocket fan-out, key-epoch rotation hooks), and the assured zero-retention proxy (OpaqueBody no-serialize path, sentinel guard test proven falsifiable). 96 backend tests pass. (`d583d0e`, `2954844`, `e148e79`)
- **#5 Deploy readiness — DONE (plan).** `docs/operations/2026-06-09-keepance-3.0-deploy-readiness.md`: version strategy (2.5.2 -> 3.0.0), CI review (+ the Windows-signing-flake RC pre-test), backend hosting (home-server Bun behind the Cloudflare tunnel; coexist with the existing `licenses.keepance.com` by landing the firm backend on `api.keepance.com` for the Firm tier; keep the validator for Solo/Pro + grandfathered buyers), go-live sequence, rollback, verification. No version bump/tag/deploy.

**The one large buildable piece still required for the Firm tier to go live: firm-client desktop wiring** — seat tokens in the OS keychain, the sync-relay client (Yjs per matter), per-matter key release/rotation + admin escrow, the assured-proxy client, and the firm auth + admin UI. Buildable now against the local backend; fully testable + shippable only once the backend is deployed. Contracts in `backend/src/contract.ts` + the chunk reports.

**NEEDS JAMESON:** ratify the pricing (board-level); create the LemonSqueezy products (his account); firm backend infra/secrets/DNS; a formed legal entity (DPA + SOC 2 prerequisite); the website 3.0 positioning decision (board-level); the production go-live. (Design-partner recruiting deliberately dropped per his 2026-06-09 instruction.)
