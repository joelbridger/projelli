# Advisor Prep Hero UX — ROUND 5 plan (2026-06-15)

Three efforts Jameson queued, in order. Branch `feature/ux-round5-2026-06-15`, backup tag
`pre-ux-round5-2026-06-15`. NOT deployed.

## R5-1 — Settings: full evaluation + simplification
Not just a tidy-up: evaluate how Settings can be simplified, condensed, and elegantly concise while
keeping EVERY necessary setting and working seamlessly. Today ~20 flat categories (General, License,
Firm, Editor, AI, Memory, Voice, Files & Workspace, Keyboard Shortcuts, Cost & Usage, Templates,
Integrations, Marketplace, Plugins, Mobile, Advanced, Updates, Onboarding, Privacy, About).
- Evaluate (Explore agent maps every category's real contents + the render/deep-link wiring).
- Design a grouped IA: ~5-7 top-level sections, each current category merged/demoted into a group,
  every real control still reachable; preserve deep-links (`keepance:open-settings` by category) +
  any firm/law/entitlement gating + the Setup Checklist entry point.
- Build it (regroup the schema + the SettingsModal nav into sections; merge the near-empty ones;
  demote niche/legacy into an Advanced bucket). Seamless with the current software.

## R5-2 — Real AI-written "at a glance" on the matter hub
The hub's "At a glance" is curated for the sample matter but shows only honest counts/recent activity
for REAL matters. Add a real AI-generated at-a-glance for a real matter when an AI key is connected:
summarize the matter's open issues / deadlines / next actions from its indexed documents + email, with
a clear "generated by AI" + refresh, graceful no-key (keep the honest counts) and loading/empty states.
Cache it so it doesn't regenerate on every hub open. Cite or ground it in the matter's content.

## R5-3 — Per-vertical copy pass
Beyond the entity label (Round 4), adapt the deeper copy per profession where it is still legal-centric:
the privacy/trust framing (return security for tax, engagement confidentiality for consulting vs
attorney-client privilege for legal), headline/empty-state/onboarding copy that assumes "legal", and any
remaining "matter/privilege/case" phrasing in high-frequency spots. Keep it honest, profession-aware,
driven off the same profession source of truth as useEntityLabel.

## Waves (sequential, his order)
R5-1 Settings, then R5-2 AI at-a-glance, then R5-3 copy. Verify + commit each; then verify + merge to
keepance-3.0, docs + memory, NOT deployed, notify.

## Verification gate (every effort)
typecheck 0 · targeted vitest then full suite · eslint clean on touched · live dev-server check
(Settings opens to the new sections + every old deep-link lands; the hub shows the AI at-a-glance with
a key / counts without; copy adapts by profession; citations survive nav; --kp-navy ok; zero errors).

## Resume note
Commits per effort on `feature/ux-round5-2026-06-15`. Master ledger: `2026-06-14-ux-program-LEDGER.md`.
