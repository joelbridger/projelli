Feedback on the new UI screens July 8, 2026 3:06 pm:
- NOTE: I tried to look at all the new screens online for this feedback, but honestly I was looking at a version of the live app on the Legion test bench laptop that could have been outdated. So please take that into account. If these changes have already been made, please intelligently skip them, but incorporate everything else. 
- Whenever the clients screen is visible, the all clients tab on the left should be highlighted to show the user where they are. And client map tab on the left should not be highlighted because that's not where the user is. 
- I'm not sure why you didn't include the Advisor Prep Hero logo on the new design and changed it to a lantern text. The current logo is the Advisor Prep Hero logo, the one on the previous UI. 
- For adding a fact, the one quiet row has two plus signs now, an icon and one before add fact. We need to remove one of those pluses. 
- For the facts that are bullets, I like the source pills But I don't like them underneath the fact. I want them to the right side of the fact so we can use up more of the space and collapse the area so it can be more condensed.
- I like the section history panel but for a specific change I like most of the information like the time stamp and the ID and the action but we also need a simple before and after section that shows what the fact was before and then after the edit or basically the before state and then after it was changed. Right now that's in the metadata, but it's hard to see at a glance. 
- The Sources Panel, when it's expanded, has empty room at the top where the collapse caret is. We need to move the sources title with the shield icon up horizontally in line with that caret to use up that space. 
- For the facts in the client map, the main bullets, I don't like the three dots to edit and remove. Instead, I want a pencil icon to the right of the fact that pops down the menu to edit or remove. 
- I don't like the location of the plus icon above the tabs in a client map. For instance, above household, the plus has its own row up there. we need to move that plus maybe below follow-ups basically below the bottom tab before the missing tab so that it's clear that a user can add another one under follow-ups and then after we move the plus down there we can shift all the tabs up to use that space.
- In the Meetings tab, I don't like how big and bold the alert is that says that no spoken notice was found in the first five minutes. This needs to be scaled way back in terms of visibility as a small notice with a menu of options. Also, the horizontal, I mean the vertical tabs that show different meetings that you can select, each tab is so vertically high with a bunch of blank space on the right side. It doesn't look good. Right now it says meeting and then the date and the duration and then needs review vertically stacked. Those need to be horizontally stacked and condensed and just look better overall. 
- For both the meeting tab and the ask tab, we need to give users the ability to rename their meetings and their conversations right in the left hand side with a pencil icon. 
- It would be really nice to have the ability to create groups of clients in the client list on the left side. Where plus button And you can name the group in a modal that pops up and then add clients to it. And then those groups appear at the top of the client list but below all clients. 
- Okay, here's a big one. It's very clear that adding a client, that whole experience has not been reviewed through a senior UX designer lens. You'll see that it's a modal that pops up to create a client, but below that it has auto expanded every single client with all of their details and there's containers around everything and so much text. This needs a complete overhaul and redesign. Please tackle this to be as clean and simple as the rest of the app. 
- In the three-dot menu by the client name in the client map, it says export word, export PDF, but it doesn't say what kind of word or PDF. It needs to say export client map parentheses docx, and the next one needs to say export client map parentheses PDF.
- I actually think it would look better if the tabs in Client Map, for instance, documents, emails, meetings, and Client Map, were actually left aligned to be to the right of the three-dot menu on that row. I think it would be easier access for users.
- I don't know if it's just the version I'm using, but it looks like there's a bunch of clients loaded into the current version of the Legion test bench. And instead of using the whole area on the left to display them, it's a container that uses about half the pane on the left that scrolls. It's fine to scroll, but it needs to be taking up that whole container down to your account area. 
- I don't like the layout of the new workflows page. I think there's been some design improvements, but we need to still be matching the look of the header row of the Ask tab and the Client Map tab. Basically, workflows needs a red icon and then needs to be kind of a bigger title that has its own row, and then everything comes under that, just like the Ask tab. 
- And then we need to make sure that this header row on across client map ask and workflow tabs is the same height right now the ask tab is a bit shorter So we need to match all of them.
- Also the Workflows area, the vertical tabs showing the list of workflows, that font is way bigger than the font in the other tabs. We need to keep this consistent and decrease the size of that. Make sure that we're using standardized font sizes across the app. 
- The Documents tab is looking a lot better, but unfortunately it doesn't look like it's matching the vertical tab area of the other tabs. For instance, compared to the ClientMap vertical tabs, it is horizontally shorter and is using a darker line to separate it from the Document content area.
- Also, I think we need a search icon by Files tab on the same row so you can search the files below it and inside of it. And then also we need to move the new file button over into the left hand side, same as files, or in the same row and tab area as the files tab. And then we need to add a separator line between the files tab and the start of the files list in the vertical file list area so that the user knows that when they click, they can click files to show all files. And below that are the actual individual files. And if we could change the text files to all files, that would make it clearer. They would make that tab clearer to click on. As a part of this effort, let's remove that large search bar across the whole top of the files area, content area, in favor of a small search icon in the files tab area. 
- I also noticed that across every tab in the app including all of the client map tabs such as documents, emails, meetings, and client map as well as the ask tab and the workflow tab all of them have really nice vertical tab areas to organize things like categories or documents or conversations. But the problem is they all have a different system of showing a search function and a new function and a collapse function. These three things should look the same across every one of these screens for visual consistency. There should be the ability to search, create new, and collapse. And that should be consistent across all of these. Including, for example, email where it has a search bar, it has a plus button, and a three dot menu. But this doesn't look the same as the other tabs like documents. It doesn't have any search bar and needs one in the left hand side, etc. So we need each tab of the entire app to have a search icon, a plus icon, a three dot menu where everything else would go. And a collapse button.
- In the settings it's not clear what unit the autosave delay is. I'm guessing seconds, but we need to specify. It's also unclear what to enter into the letterhead template setting, so we need to clarify that. Also a little more information about show hidden files would help to understand what hidden files those are. Also I'm seeing a setting for how to scroll on tabs. The choices are scroll horizontally and wrap to multiple rows. That doesn't apply anymore because we're using vertical tabs, so I think we can remove this setting. Also I see a "reopen last workspace" setting, but I'm not sure we're using workspaces anymore, so I don't think we need this setting either. For the AI settings, the options to select where AI requests go, either on this computer only or cloud AI, those are too compact. They need to be more visually obvious and they need to be more obvious when one is selected. These deserve some real space in the settings so people know exactly what AI they're using. I'm also seeing some text that says local AI installed. That dynamic field should be inside of the card that says on this computer only. That card should house information about the local AI. Whether it's installed or not, a link to install it, etc. And then in the Cloud AI card, where it says Cloud AI your account, that should show options for putting in keys, how to make the keys, which keys are currently in and working, etc. Also currently the email updates area in settings Its blank state is really confusing because it doesn't show anything This needs to say that the user did not sign up for emails or something along those lines. Also I'm seeing two options in voice press to talk hotkey and voice to note hotkey that don't look like they have any options at all. I don't see what I can do with those. And when I click on them nothing happens. For the extensions in advanced, I don't think our marketplace is even close to being available for extensions. Correct me if I'm wrong, but if it's not, can we just flip this off so users don't even see anything about extensions or a marketplace?   And I just realized we also need to match the settings UI to have the same type of vertical tabs on the left as all of the other sections of the app. Right now, they're a bit different. When you select one, you can barely tell it's selected because the background is so light. Also in the Help section, we need to move Setup section to the very top above the shortcuts because this is important stuff. Connecting AI, connecting email, setting up a firm. 
- I just saw the new version of the guided setup when I clicked on restart guided setup in the settings. And it has the basic components, but it's missing a lot of the styling that we had before. Honestly, if we could revert to the previous version, I don't really like this new eye. I want the other screens that we had before. Let me know if you have any questions about this. 
- Also, I just found that if you click using Cloud AI Pill in the top right of the app, it opens a modal of settings, which is very confusing because I was on the settings page and now a new settings modal with the same options popped up over it. That's very jarring and we shouldn't have a separate settings modal. We should either have the built-in version into the app itself or only a modal that pops up. Right now I'm leaning towards not showing this modal settings and just using the version in the app. 
- The Cloud AI card in the AI section of the settings should show what model is being used and what keys they have with the ability to change or remove keys or add them. Also, there's a section called AI rules with a link that says Manage AI rules, but nothing happens when I click that and I'm not sure what's supposed to go there. 
- For the privacy center, I'm a little confused what the security overview for my firm is and how it's different than just the whole privacy center. I think I would rather instead of having a generated security overview for my firm, just have an export button that just exports the entire privacy center as a security overview in either Word or PDF., the user should be able to choose.
- The information icon to the left of the Using Cloud AI Pill has good intentions, but it's unclear what it is when you hover over it. And the data flow information is good, but I don't think it should be here. Honestly, it should be if you hover over the Using Cloud AI Pill, or it should just be content that appears when you click the Using Cloud AI Pill in the settings. So please figure a better experience out for that. according to my ideas here. 
- The three dots next to the client's name in the client map tab, that should be vertical three dots, not horizontal. And in fact, can we just change all instances of the three dots to vertical? So it takes up a bit less space and looks more like a menu.
