# CODEX FIX BRIEF — Wave 3, Lane 2 (proposal cards + accept) fix round

You are a Codex fix agent in worktree /home/jameson/lp-w3-2 (branch lp/intake-w3-2). Your build (c72a9618) is well-structured, but the adversarial review found 4 real issues — including two P1s that break the audit-before-effect guarantee and duplicate data on retry. Fix all four, TDD, commit on this branch. Do NOT push. One cargo compile at a time.

## Fixes (do ALL)

### [P1-A] The audit INTENT must actually block filing — `src/App.tsx` (~1607) + `src/platform/intake/emailReplyAudit.ts`
`App.tsx` registers `emitAuditEntry` as the intake-email-reply audit emitter, but `emitAuditEntry` discards the underlying `addAuditEntry` PROMISE (returns void). So `mustLogIntakeEmailReplyAudit` awaits `undefined` and returns immediately — file/fact writes begin BEFORE the intent row has durably persisted, and an audit-store outage still lets the item file. This defeats the core "intent before effect, refuse if intent fails" guarantee. Fix: the email-reply audit intent path must AWAIT durable persistence and REFUSE (throw, no file/fact write) if it fails or if no emitter is registered. Register a promise-returning emitter for the email-reply audit (return the `addAuditEntry` durable promise), and make `mustLogIntakeEmailReplyAudit` actually await it. Add a test: an intent-persist rejection (or unregistered emitter) → NO attachment persist and NO fact upsert happen (assert the persist/upsert mocks are never called).

### [P1-B] Partial-retry must be idempotent — `src/platform/intake/emailReplyAccept.ts` (~255) + the proposal store
When one selected row succeeds and another fails, the proposal is left pending WITHOUT recording the completed row; retrying reprocesses ALL original selections → the succeeded attachment is persisted AGAIN (unique filename → duplicate file), and a succeeded fact is re-upserted/superseded. Fix: durably record per-row completion (e.g. persisted filePath / factId per row id) on the proposal; on accept/retry, SKIP rows already completed so a retry only processes the not-yet-done rows — no duplicate file, no re-supersede. The outcome audit reflects only the newly-completed rows. Add a test: row A succeeds, row B fails → retry does NOT re-persist A (assert persist called once for A across both attempts) and only retries B.

### [P2-C] Do not offer body rows that cannot be filed — `emailReplyClassifier.ts` / `emailReplyAccept.ts` (~232) + the card
The classifier creates a SELECTABLE `body_fact` row without a `value` for normal body-only replies (anything other than the SSN regex). Selecting it hits a branch that always throws → the proposal is stuck pending, and the card has no dismiss action, so the advisor cannot clear it. Fix: a body row with no safely-writable value must be either NON-selectable (disabled, with a plain "needs manual review" note) or routed to the quarantine/manual path — never a selectable row that can only fail. ALSO add a **dismiss** action to the proposal card (audited) so any proposal can be cleared. Test: a valueless body row is not acceptable-then-failing; dismiss clears the proposal + audits it.

### [P2-D] Wire the model classifier into production ingestion — `src/platform/intake/useEmailReplyIngestion.ts` (~103)
The production call to `classifyEmailReplyCandidate` never supplies `modelConfidence`, so the AI body-text classification path is dead in the running app (confidence comes only from filename-token match / SSN regex). Fix: wire the configured email provider's model confidence into the ingestion call (reuse `resolveEmailProvider` + the prompt-safety helpers; the email body is UNTRUSTED — sanitize before the prompt; the model still chooses NO identifier/path — it only ranks confidence among the code-supplied open items). Guard for no-provider (fall back to deterministic confidence, don't crash). Test: with a stubbed provider, ingestion supplies modelConfidence and the classifier's model path is exercised; with no provider, it falls back cleanly.

## Done bar
- Intent audit truly blocks (no file/fact write unless the intent row durably persisted). Retry never duplicates a completed file/fact. No selectable-but-unfilable rows; every proposal is clearable (dismiss). Model classification reachable in production; body stays untrusted; model chooses no identifier/path.
- Never rename matter/matter_id. Restricted values masked, never in audit rows. Non-E2EE labeling intact. Light theme, tokens, client/household copy, no em dashes, no time estimates.
- GREEN before done: `npx vitest run src/features/intake src/platform/intake`; `cargo test` for the touched intake commands (SERIAL — the `backfill_marker...` flake passes in isolation); `npx tsc --noEmit`; `node scripts/eslint-gate.mjs`. Commit on this branch. Do NOT push.
