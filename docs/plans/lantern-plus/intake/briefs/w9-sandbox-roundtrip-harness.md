# Wave 9 — Live DocuSign Sandbox Round-Trip Harness

**Branch:** `lp/intake-w9-sandbox-roundtrip`, branched from `lp/intake-w9` @ `30e50037`. Worktree: `/home/jameson/lp-w9-sandbox-roundtrip` (already created, checked out — work directly in it).
**You are Codex, the builder.** Build the harness, run the checks, commit. Do NOT push. Do NOT merge. Do not send notifications; never invoke `notify-jameson`.

## Context

`prep/W9-PREP.md` §8 ("Mandatory sandbox envelope round-trip") and §9 ("Wave-end blocking commands") require a real, safe-by-construction end-to-end test against the DocuSign **demo** account — `npm run test:docusign-sandbox-roundtrip` — that is a release blocker for Wave 9. This command and its underlying test **do not exist yet**. Real DocuSign demo credentials are still being produced (blocked on a phone verification step) and are **not available right now** — they will eventually land at `~/lantern-plus/demo-creds/docusign/`, a directory that currently exists but is empty.

Your job is NOT to complete a live signing round-trip today (you have no credentials to do it with). Your job is to build the **entire harness** so that the moment credentials land, running `npm run test:docusign-sandbox-roundtrip` performs the real thing — with nothing mocked except what §8 itself allows (the DocuSign account and the signer are synthetic; the code paths, encryption, and request routing are all real, per §5 Lane 1: "Network may be pointed to the DocuSign demo account. Encryption and request routing may not be mocked.").

**A parallel Codex lane (`lp/intake-w9-r2-returnurl`) is fixing a return-URL pinning gap in this same worktree's parent branch at the same time.** It adds `allowedReturnUrl: string` to the `DocusignAuthorization` interface in `src/platform/docusignSigning/docusignAdapter.ts` and a matching `return_url` field to the backend capability response. Build your harness so it already supplies `allowedReturnUrl` on any `DocusignAuthorization`-shaped object you construct or receive (your test should get it from the real capability response once you call the real broker endpoint — do not hardcode a mock value, since you must not mock this response). If TypeScript complains that the field doesn't exist yet when you start (because the fix hasn't merged into your branch), that's expected — note it in your report; do not attempt to merge the other branch's work into yours, and do not resolve the conflict yourself. Just make sure your code is written correctly *assuming* that field exists (it will, at integration time).

## What already exists (read these before writing anything)

- `tests/integration/marketplace-fetch-from-live-repos.test.ts` — the established pattern for a live-network integration test in this repo: gated behind an env var (there, `LIVE_NETWORK_OK=1`), using `describe.skipIf(!LIVE_OK)` for the real suite plus a companion `describe.skipIf(LIVE_OK)` "is gated" placeholder so `npm run test`/`npm run gate` never hang or fail when the gate is off. **Follow this exact pattern.**
- `scripts/assured-live-exercise.sh` — the established pattern for a live-external-service exercise gated on an off-repo credentials env file (`~/.local/share/jameworld/keepance-assured-test.env` there), with clear "create this file, chmod 600, here's the exact shape" instructions printed and a non-zero exit when the file is missing. **Follow this exact pattern for your credentials file**, but see the exact path/shape below (different from that file's).
- `backend/test/http.test.ts` — proves `import { server } from "../src/server.ts"` boots a real `Bun.serve` listener just by importing the module (used from `bun:test`). Determine whether the same import works cleanly from a `vitest` test in this project (Bun-powered) by trying it — if it does, prefer it (simplest, no subprocess management). If it does not work cleanly under `vitest`, spawn the real backend as a child process instead (`bun run backend/src/server.ts` with your env vars set) and poll `/healthz` until it's up. Report which approach you used and why.
- `backend/src/lib/config.ts`'s `resolveDocusignSigningConfig()` (~line 118-183) — the exact `DOCUSIGN_SIGNING_*` env var names, and its own fail-closed production/demo separation logic. **Reuse this real logic by actually setting these env vars and letting the real backend enforce them — do not reimplement or duplicate any of this validation in your harness.** Your harness's OWN job is only to add one more belt-and-suspenders layer: refuse to even attempt the run if the loaded credentials file itself claims `DOCUSIGN_SIGNING_ENVIRONMENT=production` or `DOCUSIGN_SIGNING_PRODUCTION_RELEASE` is truthy — the harness must be structurally incapable of running against production even before the backend's own checks would catch it.
- `src/platform/intake/__tests__/docusignSignatureContract.test.ts` — shows exactly how to construct a real `FormRequest` with a `pdf_fill` + `signature` (`grade: 'docusign'`) item, how `createAdvisorIntake`, `IntakeRelayClient`, `DirectDocusignAdapter`, `retrieveAndFileDocusignCompletion`, and `saveLocalSignatureRecord` are actually imported and used together. Reuse its fixture-construction helpers (`pdfItem`, `signature`, `request`) as a starting point rather than inventing new ones — adapt as needed for a real (non-mocked-fetch) run.
- `src/platform/docusignSigning/signatureWorkflow.ts` — `StartSignatureInput`, `startDocusignSignature`, `retrieveAndFileDocusignCompletion`, `resolveDocusignTabMap` — the real functions your harness must call, not reimplement.
- `src/platform/docusignSigning/capabilityClient.ts` and `envelopeRegistration.ts` — the real broker HTTP clients (`createDocusignAuthorizationProvider`, `registerDocusignEnvelope`) — point `baseUrl` at your locally-booted backend instance.
- `tests/integration/workspace.test.ts` — shows the `WorkspaceService` + `FSBackend` shape; for this harness, prefer a **real** temp-directory-backed backend over an in-memory mock (search `src/platform/fs/` for any existing Node-temp-dir-backed `FSBackend` implementation used by other integration/desktop tests — e.g. check `tests/desktop/` — reuse it if one exists; only write a minimal real-disk one if nothing suitable exists). This harness should write real bytes to a real temp directory and read them back, to prove actual file I/O, not just an in-memory map.
- The global Chrome automation CLI `chrome-cdp` is on PATH (`chrome-cdp session create <name> --intent "..."`, `navigate`, `snapshot`, `click <ref>`, `type <ref> <value>`, `wait <selector>`, `read`, `session close <name>`). Drive it via `node:child_process.execFile`/`execFileSync` from your test/script — this is the only way to actually complete step 4 of §8 (open the sealed one-time recipient view in a clean browser session and sign as the synthetic recipient).

## Deliverables

### 1. Credentials loader + off-repo contract

Create `tests/live/docusignSandboxCreds.ts` exporting a function (e.g. `loadDocusignSandboxCredentials(): DocusignSandboxCredentials | null`) that:
- Reads a `.env`-shaped file at `~/lantern-plus/demo-creds/docusign/signing.env` (path overridable via `DOCUSIGN_SANDBOX_CREDS_PATH` for testability), with keys matching the **exact** names in `backend/src/lib/config.ts`: `DOCUSIGN_SIGNING_ENVIRONMENT`, `DOCUSIGN_SIGNING_INTEGRATION_KEY`, `DOCUSIGN_SIGNING_IMPERSONATED_USER_ID`, `DOCUSIGN_SIGNING_ACCOUNT_ID`, `DOCUSIGN_SIGNING_DEMO_API_BASE_URI` (optional, defaults as config.ts does), `DOCUSIGN_SIGNING_ALLOWED_RETURN_URL`, `DOCUSIGN_SIGNING_CONNECT_KEY`, `DOCUSIGN_SIGNING_PRIVATE_KEY_PATH` (a path to a sibling `private-key.pem` file — do not support inlining the PEM directly in the env file; keep the key in its own chmod-600 file).
- Returns `null` (not a throw) if the file does not exist — this is the normal "credentials not yet available" state, and your test's skip path treats `null` as "skip with a clear message," not a failure.
- **Throws** (fails the run loudly, does not silently proceed) if the file exists but `DOCUSIGN_SIGNING_ENVIRONMENT` is anything other than exactly `"demo"`, or if any production-flavored variable (`DOCUSIGN_SIGNING_PRODUCTION_RELEASE`, `DOCUSIGN_SIGNING_PRODUCTION_API_BASE_URI`) is present at all in this file — this file must only ever be able to express demo/sandbox configuration, never production, structurally.
- Add a focused unit test for this loader alone (`tests/live/docusignSandboxCreds.test.ts`, runs unconditionally, no live gate needed) covering: file absent → `null`; file present with valid demo config → parsed object; file present with `environment=production` → throws; file present with a production var set → throws.

### 2. The round-trip test

Create `tests/live/docusignSandboxRoundtrip.test.ts`. Gate: `const LIVE_OK = process.env['DOCUSIGN_LIVE_SANDBOX_OK'] === '1' && loadDocusignSandboxCredentials() !== null;` — both the explicit opt-in env var AND the presence of real credentials are required (mirrors the marketplace test's opt-in, plus the credentials gate — two independent switches, both must be on). Use `describe.skipIf(!LIVE_OK)(...)` for the real suite and a companion `describe.skipIf(LIVE_OK)('...(skipped)', ...)` placeholder proving the gate itself, exactly like the marketplace test.

Inside the gated suite, implement the real round-trip:

1. **Boot the real backend** with the loaded demo credentials injected as env vars (plus whatever other env vars `backend/src/lib/config.ts` needs for a minimal working instance — check `backend/test/http.test.ts`/`backend/.env.test` or equivalent for the minimal set already used by other backend tests, reuse it). Confirm `/healthz` responds before proceeding.
2. **Build a synthetic Wave-8-completed PDF** — a small real PDF (via `pdf-lib`, already a dependency) with known page dimensions, plus a real `PdfCompletionReceipt` (reuse whatever the codebase's real W8 completion/receipt-issuing helpers are — check `src/platform/intake/pdfFillReceipt.ts` and `pdfTemplates/receipt.ts`, already imported by `signatureWorkflow.ts` — do not fabricate a receipt shape by hand if a real issuing function exists).
3. **Create a real advisor intake/request** via `createAdvisorIntake` with one `pdf_fill` item and one `signature` item (`grade: 'docusign'`, a tab map with in-bounds coordinates for your synthetic PDF's actual page size), the same way the contract test does but without mocking `getCorsSafeFetch`/`fetch`.
4. **Obtain a real capability** from your locally-booted backend via `createDocusignAuthorizationProvider`, pointed at demo DocuSign through the real backend.
5. **Create a real envelope** via `DirectDocusignAdapter.createEnvelopeAndRecipientView` against the real DocuSign demo API. Assert: a real `envelopeId` comes back, and `recipientViewUrl` matches `https://.*\.docusign\.net/`. This is your live proof that the `/restapi` double-path fix and the point-based tab-geometry fix (both landed in earlier R1 fix rounds — verify by reading `src/platform/docusignSigning/docusignAdapter.ts` and `signatureWorkflow.ts`'s `resolveDocusignTabMap` current state, don't assume) actually work against DocuSign's real API, not just a mock.
6. **Register the envelope** with the broker via `registerDocusignEnvelope`.
7. **Drive the actual signing ceremony** with `chrome-cdp`: create a dedicated session (e.g. `docusign-sandbox-roundtrip`), navigate to the recipient view URL, complete DocuSign's embedded ceremony as the synthetic signer (adopt a signature, sign, finish — inspect the real page with `chrome-cdp snapshot`/`read` since DocuSign's exact UI can't be predicted from here), confirm the browser lands on your configured `DOCUSIGN_SIGNING_ALLOWED_RETURN_URL`, then close the session. Wrap this in a generous timeout (DocuSign's ceremony can be slow) and produce a clear failure message (not a silent hang) if a selector/step isn't found — this step is explicitly the one most likely to need hands-on adjustment once it's run against the real UI for the first time; say so plainly in your report rather than overclaiming it's bulletproof.
8. **Retrieve and file the completion**: poll (`pollEnvelopeStatus`/however `retrieveAndFileDocusignCompletion` is actually structured — read it, don't assume) until `completed`, then call `retrieveAndFileDocusignCompletion` for real, writing into a **real temp-directory-backed** `WorkspaceService`.
9. **Assert the filed result**: the signed PDF and certificate exist under `Requests/<slug>/signatures/` in the real temp dir, their on-disk bytes are NOT the plaintext PDF/certificate bytes (encrypted-at-rest per the merged frontend R1 fix — read `signatureWorkflow.ts`'s current encryption functions, don't assume the exact function names), decrypting with the key from the saved `LocalSignatureRecord` recovers the original signed bytes, and the original W8-completed PDF is untouched under `Requests/<slug>/forms/`.
10. **At least these two adverse cases** (document the rest of §8's adverse-case list as an explicit follow-up TODO comment at the top of the file — do not silently drop them, name them):
    - A second `createEnvelopeAndRecipientView`/recipient-view call against the same already-viewed envelope is refused (the adapter already tracks `recipientViewGeneratedFor` — confirm this against the real API, not just the existing unit test's mock).
    - A `retrieveAndFileDocusignCompletion` call with a deliberately wrong `requestId`/mismatched hash is refused and produces no filed output (confirm this against the real retrieval-time re-verification added in the merged frontend R1 fix — read the current code for the exact check, don't assume).

Wrap the whole gated suite in generous `it(..., { timeout: N })` values — real DocuSign network round-trips plus a driven browser ceremony are slow; do not let a tight default vitest timeout produce a false failure.

### 3. Wire the npm script

Add to `package.json`: `"test:docusign-sandbox-roundtrip": "vitest run tests/live/docusignSandboxRoundtrip.test.ts tests/live/docusignSandboxCreds.test.ts"`. This must be additive — do not touch any other script.

### 4. Write the off-repo credentials README now

Create `~/lantern-plus/demo-creds/docusign/README.md` (note: **outside this git repo**, at `/home/jameson/lantern-plus/demo-creds/docusign/README.md` — do not create it inside `~/lp-w9-sandbox-roundtrip`, and do not put it under git at all). Content: no secrets, just the exact expected shape — `signing.env` with the exact key names from your loader, `private-key.pem` as the sibling RSA private key file, both `chmod 600`, the directory itself `chmod 700`. State plainly that `DOCUSIGN_SIGNING_ENVIRONMENT` must be `demo` and that a `DOCUSIGN_SIGNING_PRODUCTION_RELEASE`/`DOCUSIGN_SIGNING_PRODUCTION_API_BASE_URI` value in this file will make the harness refuse to run. Reference the npm script name so whoever finishes producing credentials knows how to trigger the run.

## Proving the harness actually works without real credentials

You have no real credentials, so you cannot prove the live network calls succeed. You CAN and MUST prove everything up to the real-network boundary works, using a **throwaway, clearly-fake** local credentials file (fake integration key, a freshly-generated throwaway RSA keypair — `openssl genrsa`, never a real DocuSign key — fake account id) pointed at via `DOCUSIGN_SANDBOX_CREDS_PATH` during your own manual verification: confirm the loader parses it, the backend boots with it, the request/intake creation and encryption steps run for real, and the flow fails cleanly with a clear DocuSign-rejected-the-fake-credentials error only once it actually tries to reach DocuSign's real demo API (not earlier, from some local validation bug, and not by silently succeeding). Delete this throwaway fixture and its fake key when done — do not commit it. Report exactly how far you got in this dry run and where it stopped.

## Checks to run (report exact pass/fail; wrap every invocation in a timeout)

```
timeout 120 npx tsc --noEmit
timeout 240 node scripts/eslint-gate.mjs
timeout 120 npx vitest run tests/live/docusignSandboxCreds.test.ts
timeout 60 npm run test:docusign-sandbox-roundtrip
```

The last command must exit 0 right now (credentials absent → clean skip), proving it will not break `npm run gate` or any CI-style run before real credentials exist.

## Finish

Commit on this branch (`lp/intake-w9-sandbox-roundtrip`) with a conventional message containing `W9-SANDBOX-ROUNDTRIP-HARNESS`. Do NOT push. Do NOT merge. Report: which backend-boot approach you used and why, the exact credentials file contract (final field names), how far your throwaway-fake-credentials dry run got before it correctly stopped, and confirm the branch is clean. Also confirm `~/lantern-plus/demo-creds/docusign/README.md` was written (it lives outside git, so it won't show in your branch diff — say so explicitly).

The very last line of your output — after everything else, on its own line — must read exactly `DONE-EXIT:0` if every check passed and the branch is clean and committed, or `DONE-EXIT:1` if something is unresolved (explain above that line). Do not print this sentinel early, more than once, or inside quoted/example text.
