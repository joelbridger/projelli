# Track 2 (Standing Plans) — Lane Tracker

**Lead:** Track-2 coordinator session, branch `lp/track2-standing` (worktree `/home/jameson/lp-track2`).
**Directive:** build every "planned" item on the feature tracker that doesn't depend on the intake program's rails, using Codex as the builder, this coordinator as reviewer/merger.

## Setup notes (read before touching a lane)

- `lp/track2-standing` was created off `lp/ux-simplify-v1` (tip `3939b96c`) and did **not** yet contain the three plan docs referenced in the brief — they exist only on the `lantern-plus` mainline branch (tip `7959344c`), not yet merged into this line. Cherry-picked the four **docs-only** commits so the specs are readable here:
  - `810de176` — ACATS plan (`docs/plans/lantern-plus/acats-autopilot/PLAN.md`)
  - `913fd47e` + `1fbd6c0b` — planning write-sockets plan (`docs/plans/lantern-plus/planning-write-sockets/PLAN.md`)
  - `1879b0aa` — integration honesty cards (`docs/trust/integration-cards/*.md`) — **this means the docs deliverable for item 3 is already done**; the remaining work is the in-app surface (see Lane C).
- **Known cross-track landmine:** `/home/jameson/lp-schwab-prefill` (branch `feat/schwab-prefill`, commit `e802d32d`) already contains a real Schwab account-opening prefill flow (`src/features/accounts/NewAccountFlow.tsx`, `accountApplication.ts`) — this is the "existing Schwab account-opening plan" the ACATS plan wants to compose with. It is **not merged** into `lp/track2-standing` and is owned by a different in-flight lane, not this track. Lane A (ACATS) must NOT merge or depend on that branch — build standalone, read-only-reference its field-map shape if useful, and leave the real Wave-D adapter as documented future work once that branch lands on the mainline.
- Only Lane B (write-sockets) touches Rust (`src-tauri/src/commands/writeback/`). Lanes A and C are TS/React-only. One cargo compile at a time box-wide (the intake session also builds) — Lane B retries on lock (exit 144) rather than fighting it.
- File-overlap check across the three lanes: disjoint by design (new feature folders + new Rust module). None of the three touch `AccountWindow.tsx` in this first slice (Lane B defers any new connector-settings UI since there's no live OAuth to wire up yet).

## Lane decomposition

### Lane A — ACATS Transfer Autopilot
- Worktree: `/home/jameson/lp-t2-acats`, branch `lp/t2-acats`
- Plan: `docs/plans/lantern-plus/acats-autopilot/PLAN.md`
- Scope for this build: Waves A–C in full (schema + research pack, statement extraction engine reusing existing PDF/OCR pipeline, advisor review UI), plus a standalone Wave-D-lite (Schwab Prep Packet export: copyable fields, checklist, statement-attachment reminder, official-form field mapping) that does **not** depend on the unmerged `feat/schwab-prefill` branch. Wave E (NIGO/onboarding-board wiring) and Wave F (Schwab partner API) explicitly deferred — E depends on the intake program's onboarding board (a different track), F is partner-gated with no sandbox access.
- Status: **MERGED** into `lp/track2-standing` @ `62c3d357`
- Built by Codex (`145bcd6e`). Reviewed: `codex-review --base lp/track2-standing` found 3 findings. Fixed + regression-tested: (1) the "Account #:" line format wasn't matched (a trailing `\b` right after `#`/`.` doesn't assert across two non-word characters); (2) flattened PDF text like "Account Number: 1234 Account Title: ..." could capture "1234 Account" (fixed by excluding spaces from the captured value's character class — real account numbers never contain internal spaces); (3) firm-name normalization's `alias.includes(cleaned)` direction let a short generic OCR fragment ("The", "LLC", "Investments") false-match a longer alias before the real firm line was read (fixed by keeping only the `cleaned.includes(alias)` direction). A 4th finding (masked account numbers can be approved after acknowledging the warning) was checked against PLAN.md §3.4, which explicitly lists "Account number is masked" as an acknowledge-and-proceed warning alongside several other soft warnings, not a hard block — verified this matches the implemented behavior exactly, so left as-is (not a deviation from spec).
- Gate evidence: `npx vitest run src/features/acats/` — 20/20 green (17 original + 3 new regression tests). `npm run typecheck` clean. TS/React-only, no `src-tauri/` changes.

### Lane B — RightCapital / Holistiplan Write-Back Sockets
- Worktree: `/home/jameson/lp-t2-write-sockets`, branch `lp/t2-write-sockets`
- Plan: `docs/plans/lantern-plus/planning-write-sockets/PLAN.md`
- Scope for this build: Wave 0's non-code half (add RightCapital + Holistiplan rows to `docs/plans/lantern-plus/vendor-applications-checklist.md`) + Wave 1 in full (generic `ExternalWriteSocket` engine, ledger, queue, review-card prototype against **fixture/mock** targets — no live vendor calls, matching the "sandbox/mocks only" rule since we hold no RightCapital/Holistiplan credentials at all). RightCapital/Holistiplan-specific typed operations (`UpsertIncome`, `EnsureHousehold`, etc.) are stubbed behind the trait with mock-server-backed unit tests as a stretch goal, clearly marked partner-gated for live wiring. Waves 2–3 live sockets, Wave 4 research spike, and Wave 5 Wealthbox migration are out of scope for this pass (2–3 blocked on vendor access; 5 is a later cleanup).
- Status: **MERGED** into `lp/track2-standing` @ `e2212899`
- Built by Codex (`24ba2e87`). Reviewed: `codex-review --base lp/track2-standing` found 4 findings, all fixed + regression-tested: (1) [P1, security] the writeback ledger's SQLCipher master key (`WRITEBACK_ENC_SERVICE`) was missing from the renderer keychain denylist (`INTERNAL_EXACT_SERVICES`), so any renderer code could read or delete it via the generic `keychain_get`/`keychain_delete` bridge — added, with a new `connector_secret_services_are_denied` assertion; (2) [P1, completeness] `externalWriteSetWorkspace` + queue hydration were never called from workspace-open, so every command failed "workspace not set" — wired into `useMemoryWiring.ts` mirroring every other connector; (3) [P2] an unmatched Holistiplan target (no household/client linked) fell back to a non-empty placeholder subject key that passed backend validation, sending to nowhere real; (4) [P2] a multi-document Holistiplan upload only ever turned document[0] into a write while the review data covered the whole array, so approval reported full success after uploading one document. Both (3) and (4) now block the send with a clear error instead of silently mis-sending or under-sending — full per-document proposal support (one write + ledger row per document) is left as documented future work for whichever wave builds the real review-card UX, not a one-line fix.
- Gate evidence: TS — `npx vitest run tests/unit/externalWriteQueue.test.ts tests/unit/planning/ExternalWriteReviewCard.test.tsx` 11/11 green (9 original + 2 new). `npm run typecheck` clean. Rust — `cargo test writeback::` 8/8 green, `cargo test connector_secret_services_are_denied` 1/1 green. Confirmed no touches to `src-tauri/src/commands/crm/*`, `AccountWindow.tsx`, or any real vendor URL/OAuth flow.

### Lane C — Integration Honesty Cards (in-app surface)
- Worktree: `/home/jameson/lp-t2-honesty-cards`, branch `lp/t2-honesty-cards`
- Material: `docs/trust/integration-cards/` (TEMPLATE + 4 shipping cards already written and merged into this branch) + `docs/2026-07-10-advisor-pain-analysis-and-lantern-answers.md` §4 ("In-app, the connector shows the same card").
- Scope for this build: the docs are done. Build the in-app surface only — a shared `IntegrationHonestyCard` component/dialog with typed content (reads / writes / never-touch / gating / limits / last-verified) transcribed 1:1 from the four shipping cards (Wealthbox, Email, OneDrive/SharePoint, Calendly), triggered from each connector's row in Account → Connections (extends the existing `InfoHelp` affordance pattern). No new connectors, no vendor work.
- Status: **MERGED** into `lp/track2-standing` @ `d47ef430` (feat) + `b8e42989` (fix, folded into the merge)
- Built by Codex (`778f896b`). Reviewed: `codex-review --base lp/track2-standing` found one real finding — `src/platform/connectors/*` importing `src/features/settings/IntegrationHonestyCard` violated the machine-enforced `tests/unit/architecture-boundaries.test.ts` layer DAG (platform must not import features). Fixed by moving `IntegrationHonestyCard.tsx` + `integrationHonestyCards.ts` (+ tests) to `src/platform/connectors/`, alongside the existing `oauthPending.ts`/`syncAuditError.ts` shared connector modules. Re-verified: typecheck clean, all 10 new/moved tests pass including the architecture-boundaries test and the markdown↔TS structural-parity test.
- Gate evidence: full `npm run gate` minus the Rust step (logged reason: zero `src-tauri/` changes in this diff) — all steps green except two flagged items, both confirmed transient/environmental on isolated rerun, not caused by this diff: (1) an ESLint baseline regression on `src/features/documents/versioning/VersionService.ts` (a file untouched by this merge) — clean on `npm run lint:gate` rerun; (2) a 5s timeout on `tests/unit/provider-front-door.test.ts` (a synchronous full-repo fs scan) under heavy concurrent load from the other two active Codex lanes — passes in isolation (`npx vitest run tests/unit/provider-front-door.test.ts`, 5/5 green). Handle guard: 8 new handles frozen clean, no vanished handles. Token guard: clean, no new hardcoded colors.
- SHA / gate evidence: merge commit `d47ef430` on `lp/track2-standing`; gate log `/home/jameson/lp-track2-logs/gate-honesty-cards.log`.

## Merge order

No hard dependency between lanes — they land independently as each clears review + gate. Order of dispatch: A, B, C (parallel).

## Log
- 2026-07-10: tracker created, three worktrees created, docs cherry-picked, lanes about to dispatch.
- 2026-07-10: Lane C (honesty cards) merged into `lp/track2-standing` @ `d47ef430`, gate green (two flagged items independently confirmed transient, not real regressions).
- 2026-07-10: Lane A (ACATS) merged @ `62c3d357` and Lane B (write-sockets) merged @ `e2212899`. Both adversarially reviewed via `codex-review`; every real finding (3 in ACATS, 4 in write-sockets, including a P1 keychain-denylist security gap) fixed and regression-tested before merge. All three lanes now on `lp/track2-standing`.
- 2026-07-10: post-merge full `npm run gate` (with cargo, now required since Rust changed) surfaced three environment/integration issues, all fixed in `91300900`: (1) this coordinator worktree itself was missing the gitignored Tauri sidecar binary placeholders (`src-tauri/binaries/{piper,llama-server}-x86_64-unknown-linux-gnu`) — same class of gap as the per-lane worktrees, just not caught until the first Rust-inclusive gate run here; (2) `tests/unit/externalWriteQueue.test.ts`'s new unmatched-target regression test violated `tsconfig.test.json`'s `exactOptionalPropertyTypes` by passing `field: undefined` on a `Partial<>` override — rewritten as a plain literal; (3) `ExternalWriteReviewCard.tsx` had 6 hard-coded color literals instead of design tokens, caught by the token-guard gate — swapped for the existing `--kp-surface-card`/`--kp-warning`/`--kp-bg-soft` tokens.
- 2026-07-10: **full gate confirmed GREEN** (`GATE GREEN`, 767/768 test files, 7206/7210 tests, 0 Rust failures — one flaky unrelated mail-module test independently confirmed non-reproducible in isolation) and **pushed to origin** at `3c6ec4a0`. First `WORKER-DONE` sentinel sent.
- 2026-07-10: **coordinator's independent final review pass** (before mainline merge) found 3 real blocking findings, all verified against HEAD before any fix was dispatched:
  - **P1 (writeback):** `external_write_approve_proposal` wrote zero append-only audit log entries — every other real vendor-write path in this codebase (the CRM write engine) writes a must-succeed "intent" audit row before the send and an "outcome" row after, and this path skipped both. Fixed in `lp/t2-fix-writeback-audit` (`7a8ff859`) by mirroring `append_crm_audit_must_for_matter` exactly: a new `append_writeback_audit_must_for_matter` helper, wired so a failing intent-audit aborts the approval before any socket call, with a distinct id for the `VerifyPending`-ambiguous outcome case. `codex-review` follow-up found the new `writeback-audit-appended` Tauri event had no frontend listener (Activity Log stayed stale until workspace reopen) — fixed in `lp/t2-fix-writeback-audit-r2` (`e0b09753`) by extending the existing `useWorkspaceLifecycle.ts` CRM-audit-event listener to also listen for the writeback event. Second review: clean, no findings.
  - **P2a (ACATS):** `findAccountNumber()` silently picked the first candidate line with no check for a second, different account number elsewhere in the statement (common on multi-account/household statements) — reported false-confident (0.93) wrong data. Fixed in `lp/t2-fix-acats-audit` (`f9632ffe`): detects all distinct candidates (normalizing masked-vs-unmasked so the *same* number twice never false-triggers), lowers confidence to 0.45 and adds an acknowledge-to-proceed warning when more than one distinct value is found.
  - **P2b (ACATS):** `approveDraft()`/`exportSchwabPrepPacket()` wrote no audit rows at all. Fixed in the same `f9632ffe` commit using `AuditService.mustLogDurable`, masked account numbers only, blocking the action on audit failure. `codex-review` follow-up found two more real bugs in that fix: (a) the standalone `AuditService('acats')` instance's rows never reached the live Activity Log (each instance keeps private in-memory state; only `App.tsx`'s own instance feeds the visible `auditEntries`), and (b) the audit call happened on a cloned snapshot *before* the final approval commit, so a concurrent edit during the awaited call could leave a durable row describing an approval that never actually applied. Fixed in `lp/t2-fix-acats-audit-r2` (`ed50b912`): added a new awaitable `matterAuditEmitterAsync` bridge (mirroring the existing sync `matterAuditEmitter` pattern in `matterStore.ts`) that `App.tsx` registers pointing at its own `addAuditEntry`, and locked every draft-mutation action (`editField`, `confirmField`, `acknowledgeWarning`, `setTransferType`, `setAssetAction`, `setDraft`, `resetAcatsReview`) while an approval audit is in flight. A third adversarial pass then found the round-2 diff introduced 5 real `eslint-gate` regressions (a `setState`-in-effect, an undocumented silent-failure `void`, two lint-unclean test mocks, one unsafe-`any` matcher) that plain `npm run lint` doesn't catch but the merge-required `lint:gate` step does — fixed directly (`166d6c1c`), re-verified clean. Final review on this lane: clean.
  - All four fix branches (`lp/t2-fix-writeback-audit(-r2)`, `lp/t2-fix-acats-audit(-r2)`) merged into `lp/track2-standing` at `bc892eb1` (ACATS) and `2d77a8e5` (writeback). Full gate rerun in progress before the corrected `WORKER-DONE`.
