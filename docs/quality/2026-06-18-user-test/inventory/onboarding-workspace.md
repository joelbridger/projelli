# Domain: Onboarding + Workspace selection + first-run experience

Covers: WorkspaceSelector (first screen), GuidedOnboarding (9-step flow), FirstRunWizard (legacy 7-step path still referenced in some call sites), AiSetupStep, ApiKeyWizard, FeatureTour (post-onboarding), AiSetupReminder, SetupChecklist, and the vault-locked workspace interstitial — all gating and first-launch surfaces.

---

## 1. Workspace Selector

| ID | Persona | Story | Steps | Surface | Precondition | Expected | Layer | Risk | Covered? |
|---|---|---|---|---|---|---|---|---|---|
| WS-01 | any | As a new user I want to see the branded start screen so that I know the product before committing to a folder | Launch app | `WorkspaceSelector` · `data-testid="workspace-selector-dialog"` · `data-testid="welcome-dialog-pitch"` | No recent workspaces; no testMode | Full-viewport white screen with Keepance logo and tagline visible | L1 | L | `tests/e2e/welcome-dialog.spec.ts` — "branded welcome screen is visible" |
| WS-02 | any | As a new user I want to open an existing folder so that I can resume work from a previous installation | Click "Open Existing" → OS folder picker → choose folder | `WorkspaceSelector` · `data-testid="open-existing-workspace"` | App running in Tauri (or supported browser) | Workspace opens and main shell renders | L2 | H | NONE (E2E stubs FS; unit tests don't drive the Tauri dialog) |
| WS-03 | any | As a new user I want to create a new workspace so that a default folder structure is scaffolded for me | Click "New Workspace" → OS folder picker → choose folder | `WorkspaceSelector` · `data-testid="new-workspace"` | App running in Tauri (or supported browser) | Workspace created with default folders; main shell renders | L2 | H | NONE |
| WS-04 | any | As a returning user I want to expand the "Recent" section so that I can reopen a past workspace without browsing | Click the "Recent (N)" toggle | `WorkspaceSelector` · `data-testid="recent-workspaces-toggle"` | At least one entry in `recentWorkspaces` store | Section expands and lists workspace names and dates | L1 | L | `tests/e2e/welcome-dialog.spec.ts` — "shows recent workspaces" (partial) |
| WS-05 | any | As a returning user I want to click a recent workspace to reopen it so that I don't browse every time | Expand recent → click workspace row | `WorkspaceSelector` · recent row buttons | Tauri; recent workspace path still valid on disk | Workspace opens without a second picker dialog | L2 | H | NONE (Tauri FS required) |
| WS-06 | any | As a new user I want Escape to do nothing so that I cannot accidentally dismiss the first-run gate | Focus screen → press Escape | `WorkspaceSelector` (no `onDismiss` prop) | App in first-run blocking mode | Dialog remains visible | L1 | M | `tests/e2e/welcome-dialog.spec.ts` — "Escape does not dismiss" |
| WS-07 | any | As a returning user I want to dismiss the workspace selector via Escape or X so that I can cancel re-opening | Open workspace selector in switch-workspace mode → press Escape or click X | `WorkspaceSelector` (`onDismiss` prop present) · X button | User already has workspace open | Dialog closes, previous workspace still active | L1 | M | NONE |
| WS-08 | any | As a browser user on an unsupported browser I want a clear error message so that I know to use a different browser | Click "Open Existing" in a browser lacking File System Access API | `WorkspaceSelector` · error banner | Browser dev server; `WebFSBackend.isSupported()` returns false | Error banner explains to use Chrome/Edge/Opera or the desktop app | L1 | M | NONE |
| WS-09 | any | As a new user I want to see a preview of the folder structure in the New Workspace card so that I understand what will be created | View the workspace selector screen | `data-testid="new-workspace-structure-preview"` | App on first run | Preview text lists the default folder names | L1 | L | `tests/e2e/welcome-dialog.spec.ts` — "shows folder structure preview" |
| WS-10 | any | As a new user I want to open the Getting Started docs so that I can read setup instructions | Click "Learn more" footer link | `data-testid="welcome-dialog-learn-more"` | Any | External URL opens; app stays on the workspace selector | L2 | L | `tests/e2e/welcome-dialog.spec.ts` — "footer has Learn more link" |
| WS-11 | firm-member | As a user with an encrypted workspace I want to see a vault-unlock prompt instead of proceeding so that I am not silently locked out of my data | Choose a vault-enabled, locked workspace from recent list | `WorkspaceSelector` → `VaultLockedPrompt` · `data-testid="vault-locked-prompt"` (per vault tests) | Tauri; vault enabled and locked (no key on this machine) | VaultLockedPrompt renders; workspace does not open | L2 | H | `tests/unit/vault/VaultLockedPrompt.test.tsx` — renders unlock UI |
| WS-12 | firm-member | As a user with a locked vault I want to unlock it and have the workspace open normally so that I can continue work | Enter passphrase/key → click Unlock | `VaultLockedPrompt` → `handleVaultUnlocked` | Tauri; vault can accept the key | Vault unlocks; workspace opens normally | L2 | H | `tests/unit/vault/VaultLockedPrompt.test.tsx` — unlock success path |
| WS-13 | firm-member | As a user unable to unlock the vault I want to access the escape hatch so that I can recover access | Click "Can't unlock?" → `VaultEscapeHatchDialog` opens | `VaultEscapeHatchDialog` · `data-testid="vault-escape-hatch"` | Tauri; locked vault | Escape hatch dialog renders with recovery options | L2 | H | `tests/unit/vault/VaultLockedPrompt.test.tsx` — escape hatch path |

---

## 2. First-Launch Gating (App.tsx)

| ID | Persona | Story | Steps | Surface | Precondition | Expected | Layer | Risk | Covered? |
|---|---|---|---|---|---|---|---|---|---|
| FL-01 | any | As a new user I want GuidedOnboarding to appear automatically so that I am guided through setup without hunting for it | Launch app with no flag and no recent workspaces | `App.tsx` → `showFirstRun` state → `GuidedOnboarding` · `data-testid="onboarding-step-welcome"` | No `keepance_onboarding_complete` in localStorage; no recent workspaces | GuidedOnboarding overlay appears within ~1.5s | L1 | M | `tests/unit/first-run-mount.test.tsx` — "renders GuidedOnboarding on first run" |
| FL-02 | any | As a returning user I want onboarding NOT to re-show so that I am not interrupted every launch | Launch app after completing onboarding | `App.tsx` gating logic | `keepance_onboarding_complete = "true"` in localStorage | GuidedOnboarding does not appear | L1 | M | `tests/unit/first-run-mount.test.tsx` — "does not re-show on next mount" |
| FL-03 | any | As a QA tester I want testMode=true to bypass the workspace gate so that automated tests can reach the app directly | Navigate to `/?testMode=true` | `App.tsx` `IS_TEST_MODE` constant | URL contains `testMode=true` | WorkspaceSelector and GuidedOnboarding both suppressed; shell renders immediately | L1 | M | Multiple test specs use `?testMode=true` as standard harness |
| FL-04 | any | As a QA tester I want `?forceOnboarding=true` to re-show onboarding so that I can test the flow without clearing localStorage | Navigate to `/?forceOnboarding=true` | `App.tsx` `forceOnboarding` flag | Any localStorage state | GuidedOnboarding appears regardless of completion flag | L1 | M | NONE (documented in code but no dedicated test pins this) |
| FL-05 | any | As a user who has recent workspaces but has never finished onboarding I want the onboarding overlay to NOT appear so that I am not double-blocked on top of choosing a workspace | Launch app with recent workspaces but no completion flag | `App.tsx` `noRecentWorkspaces` gate | Recent workspaces exist; no completion flag | Workspace selector appears; GuidedOnboarding does NOT overlay | L1 | M | NONE |

---

## 3. GuidedOnboarding (9-step flow)

| ID | Persona | Story | Steps | Surface | Precondition | Expected | Layer | Risk | Covered? |
|---|---|---|---|---|---|---|---|---|---|
| GO-01 | any | As a new user I want to see the welcome screen with the value proposition so that I understand what Keepance does before committing | GuidedOnboarding mounts | `GuidedOnboarding` · `data-testid="onboarding-step-welcome"` · `data-testid="onboarding-next-welcome"` | FL-01 condition | Welcome step with 3 privacy bullet points and "Get started" button | L1 | L | `tests/unit/first-run-mount.test.tsx` — "onboarding-step-welcome" visible |
| GO-02 | any | As a new user I want to advance through all 9 steps in order so that I complete setup | Click through all step CTAs | `OnboardingStepFrame` · step testids 0–8 | GO-01 condition | Steps progress 0→1→2→3→4→5→6→7→8 and app opens | L1 | M | `tests/unit/first-run-mount.test.tsx` — full flow end-to-end |
| GO-03 | any | As a new user I want to pick my profession (legal / tax / consulting / advisor / other) so that my templates and AI defaults are tailored | Step 1 → click profession card | `data-testid="onboarding-step-profession"` · `data-testid="profession-card-{id}"` · `data-testid="onboarding-next-profession"` | On step 1 | Selected card highlighted; profession persisted to localStorage; model default set | L1 | M | `tests/unit/first-run-wizard-flow.test.tsx` — profession sets default model |
| GO-04 | any | As a new user I want to skip profession selection so that I can proceed without being forced to categorize myself | Step 1 → click "Skip for now" | `data-testid="onboarding-next-profession"` (without selecting) | On step 1; no profession selected | Advances to step 2; profession defaults to "other" | L1 | L | `tests/unit/first-run-samples.test.tsx` — uses skip path |
| GO-05 | any | As a new user I want to enter my name and upload a photo so that my identity shows in the sidebar | Step 2 → type in name field → click "Upload photo" → choose image | `data-testid="onboarding-step-identity"` · `data-testid="onboarding-identity-name"` · `data-testid="onboarding-identity-upload"` · `data-testid="onboarding-identity-file"` | On step 2 | Name persists to profileStore; avatar thumbnail shown; "Remove" button appears | L1 | M | NONE |
| GO-06 | any | As a new user I want to skip identity setup so that I can set a name/photo later | Step 2 → click Continue | `data-testid="onboarding-identity-next"` | On step 2 | Advances to step 3 with no name required | L1 | L | `tests/unit/first-run-mount.test.tsx` — test helper calls `onboarding-identity-next` without filling name |
| GO-07 | any | As a new user I want to choose my workspace location (Documents / Dropbox / iCloud) so that I understand where my files will live | Step 3 → click a workspace choice card | `data-testid="onboarding-step-workspace"` · `data-testid="workspace-choice-{id}"` · `data-testid="onboarding-workspace-next"` | On step 3 | Selected card highlighted | L1 | L | NONE (step 3 not individually asserted in existing tests) |
| GO-08 | any | As a new user who selects Dropbox or iCloud I want to see a cloud-sync caveat so that I understand the privacy implications | Step 3 → click "Dropbox" or "iCloud" card | `data-testid="workspace-cloud-sync-note"` | On step 3 | Warning note visible under the cards | L1 | M | NONE |
| GO-09 | any | As a new user I want to read the trust/data summary and optionally open the full data map so that I understand where my data goes before connecting AI | Step 4 → click "Read the full data map" | `data-testid="onboarding-step-trust"` · `data-testid="onboarding-trust-open-data-map"` · `data-testid="onboarding-data-continue"` | On step 4 | Data map bullets visible; clicking link opens DataMapDialog | L1 | M | `tests/unit/first-run-wizard-flow.test.tsx` — data-map step renders (FirstRunWizard variant) |
| GO-10 | any | As a new user I want to connect a cloud AI key (Claude / OpenAI / Gemini) so that I can use AI features immediately | Step 5 → click "Connect your AI provider account" → pick provider → paste key → Save | `data-testid="onboarding-step-ai-key"` → `AiSetupStep` → `ai-path-own-account` → `ai-setup-key-input` → `ai-setup-save-key` | On step 5 | Key persisted via KeychainService; wizard advances to step 6 | L1 | H | `tests/unit/first-run-mount.test.tsx` — "saving a key during onboarding persists via KeychainService" |
| GO-11 | any | As a new user I want to choose local Ollama so that nothing is sent over the internet | Step 5 → click "Keep everything on your computer" → Ollama detected → click "Use local AI" | `ai-path-local` · `data-testid="ai-setup-local-view"` · `data-testid="ai-setup-use-local"` · `data-testid="ollama-status"` | On step 5; Ollama running locally | Local-only mode set; wizard advances to step 6 | L2 | H | `tests/unit/onboarding-ai-setup.test.tsx` — local path + local-only mode set |
| GO-12 | any | As a new user I want to skip AI setup so that I am never blocked on having a key | Step 5 → click "Skip for now" | `data-testid="ai-path-later"` | On step 5 | `hasDeferredAiSetup()` returns true; wizard advances to step 6 without a key | L1 | M | `tests/unit/first-run-mount.test.tsx` — "Set this up later leaves deferred flag active" |
| GO-13 | any | As a new user I want to connect M365 email so that my email is indexed locally | Step 6 → M365 tab active → complete OAuth connect | `data-testid="onboarding-step-email"` · `data-testid="email-tab-m365"` · `MailConnect` component | On step 6 | Email connected; step marked done in progress | L3 | H | NONE (OAuth real provider flow) |
| GO-14 | any | As a new user I want to connect Gmail so that my email is indexed locally | Step 6 → click "Gmail" tab → complete OAuth connect | `data-testid="email-tab-gmail"` · `MailGmailConnect` | On step 6 | Gmail connected | L3 | H | NONE |
| GO-15 | any | As a new user I want to connect IMAP email so that a non-M365/Gmail account is indexed | Step 6 → click "Other (IMAP)" tab → fill credentials | `data-testid="email-tab-imap"` · `MailImapConnect` | On step 6 | IMAP connected | L2 | M | NONE |
| GO-16 | any | As a new user I want to skip email connection so that I am never gated on it | Step 6 → click "Connect later" | `data-testid="email-connect-later"` | On step 6 | Advances to step 7; email step NOT marked done | L1 | L | `tests/unit/first-run-mount.test.tsx` — helper calls `onboarding-email-continue` (bypasses connect; similar intent) |
| GO-17 | firm-admin | As a new firm admin I want to claim my org and set up my firm during onboarding so that I can invite members immediately | Step 7 → click "Create a firm" → FirmSignIn (claim panel) | `data-testid="onboarding-step-firm"` · `data-testid="firm-option-create"` · `FirmSignIn` | On step 7; not yet signed in; Firm-plan license | Org claimed; user signed in as admin | L2 | H | NONE |
| GO-18 | firm-member | As a firm member I want to join my existing firm during onboarding so that I am added to the shared workspace | Step 7 → click "Join your firm" → FirmSignIn (signin panel) | `data-testid="firm-option-join"` · `FirmSignIn initialPanel="signin"` | On step 7; not yet signed in; invite received | User signed in as member | L2 | H | NONE |
| GO-19 | solo | As a solo user I want to continue without a firm so that I can use Keepance independently | Step 7 → click "Continue solo" | `data-testid="firm-solo-skip"` | On step 7; not signed into any firm | Advances to step 8 | L1 | L | `tests/unit/first-run-mount.test.tsx` — `onboarding-firm-continue` used to bypass |
| GO-20 | firm-admin | As a signed-in firm admin I want to see the firm admin console and branding controls during onboarding so that I can configure the firm name and logo | Step 7 (already firm-admin signed in) | `data-testid="firm-admin-content"` · `data-testid="firm-branding"` · `FirmAdminConsole` | On step 7; `firm.isSignedIn && firm.role === "admin"` | Admin console + branding widget (name input, logo upload) rendered | L2 | H | NONE |
| GO-21 | firm-member | As a signed-in non-admin firm member I want to see a "your admin manages members" note during onboarding so that I am not confused | Step 7 (already signed in, non-admin) | `data-testid="firm-member-note"` | On step 7; `firm.isSignedIn && firm.role !== "admin"` | Member note visible | L1 | L | NONE |
| GO-22 | any | As a new user I want to enable the sample files toggle on the Done step so that I see realistic example content | Step 8 → toggle ON (default) → click "Explore the sample…" | `data-testid="onboarding-step-done"` · `data-testid="onboarding-samples-toggle"` · `data-testid="onboarding-done-confirm"` | On step 8; workspace available | Sample files written; onboarding marked complete; shell opens | L1 | M | `tests/unit/first-run-samples.test.tsx` — samples written when toggle ON |
| GO-23 | any | As a new user I want to disable samples and open Keepance clean so that I start with an empty workspace | Step 8 → toggle OFF → click CTA | `data-testid="onboarding-samples-toggle"` | On step 8 | No sample files written; onboarding complete; shell opens | L1 | L | `tests/unit/first-run-samples.test.tsx` — no writeFile called when OFF |
| GO-24 | any | As a new user who skipped AI I want to see a "connect AI later" note on the Done step so that I remember it is still needed | Step 8 (ai not connected during flow) | `data-testid="onboarding-done-no-ai-note"` | On step 8; `aiConnected === false` | Note visible pointing to Settings | L1 | M | NONE |
| GO-25 | firm-admin | As a firm admin I want to upload a firm logo and set the firm name so that the sidebar shows branded identity | Step 7 → firm-admin view → fill firm name → click "Upload logo" → choose image | `data-testid="firm-branding-name"` · `data-testid="firm-branding-upload"` · `data-testid="firm-branding-file"` | firm-admin signed in | firmName and firmLogo persisted in profileStore; displayed in sidebar | L2 | M | NONE |
| GO-26 | any | As a new user I want to skip the entire onboarding with one click so that I can get into the app immediately | Click "Skip" in the OnboardingStepFrame header | `OnboardingStepFrame` skip button | Any step | `markOnboardingComplete("other")` called; shell opens immediately | L1 | M | NONE (unit tests mock GuidedOnboarding; skip path in first-run-mount uses specific step CTAs) |
| GO-27 | any | As a returning user I want the onboarding to never show again after I complete it so that I am not interrupted | Complete onboarding → close app → relaunch | `keepance_onboarding_complete` localStorage key | Onboarding completed in a prior session | GuidedOnboarding overlay absent | L1 | H | `tests/unit/first-run-mount.test.tsx` — "does not re-show on next mount" |

---

## 4. FirstRunWizard (legacy 7-step surface, still referenced by App for browser-mode fallback)

| ID | Persona | Story | Steps | Surface | Precondition | Expected | Layer | Risk | Covered? |
|---|---|---|---|---|---|---|---|---|---|
| FRW-01 | any | As a new user I want to walk through the 7-step wizard (welcome→profession→workspace→data→ai-setup→demo→done) so that I am guided through basic setup | Click each step CTA | `FirstRunWizard` · `data-testid="onboarding-next-welcome"` etc. | `showFirstRun=true` (pre-GuidedOnboarding path) | Wizard completes; `keepance_onboarding_complete` set | L1 | M | `tests/unit/first-run-wizard-flow.test.tsx` — full flow |
| FRW-02 | any | As a new user I want to skip the wizard at any point so that I am never gated | Click "Skip for now" in header | Header skip button | Any wizard step | `onSkip` called; wizard closes without setting completion flag | L1 | L | `tests/unit/first-run-wizard-flow.test.tsx` — skip path |
| FRW-03 | any | As a new user I want to navigate Back between wizard steps so that I can correct choices | Click Back from any step | Back buttons in each step pane | On any step after welcome | Step decrements; previous step content visible | L1 | L | `tests/unit/first-run-wizard-flow.test.tsx` — back navigation |
| FRW-04 | any | As a new user I want the progress indicator to advance correctly so that I know where I am | Advance through steps | Progress bar dots at top of wizard | Any wizard step | Dots fill left-to-right for each completed step | L1 | L | NONE (visual only; not asserted) |

---

## 5. AiSetupStep (Onboarding + standalone in Settings)

| ID | Persona | Story | Steps | Surface | Precondition | Expected | Layer | Risk | Covered? |
|---|---|---|---|---|---|---|---|---|---|
| AS-01 | any | As a new user I want to see three clear paths (own account / local / later) so that I am not confused by API jargon | AI setup step mounts | `data-testid="ai-setup-step"` · `data-testid="ai-path-own-account"` · `data-testid="ai-path-local"` · `data-testid="ai-path-later"` | Step 5 of GuidedOnboarding or AiSetupStep standalone | Three path cards visible; plain English leads | L1 | M | `tests/unit/onboarding-ai-setup.test.tsx` — "leads with plain English" |
| AS-02 | any | As a new user I want to pick "Use your own account" and select a provider (Claude/OpenAI/Gemini) so that I get step-by-step guidance for that provider | Click "Connect your AI provider account" → select provider tab | `data-testid="ai-provider-tab-{anthropic|openai|google}"` | Own-account view visible | Provider tabs shown; tutorial steps update for selected provider | L1 | M | `tests/unit/onboarding-ai-setup.test.tsx` — provider picker visible |
| AS-03 | any | As a new user I want to switch provider tabs and see provider-specific guidance so that I get the right console URL | Click different provider tab | `data-testid="ai-provider-tab-{id}"` · `data-testid="ai-open-console-{id}"` | Own-account view; provider tabs visible | Tutorial steps and placeholder change; console URL button updates | L1 | L | `tests/unit/onboarding-ai-setup.test.tsx` — switching providers |
| AS-04 | any | As a new user I want to paste a key and see an inline "Test this key" result so that I know whether it works before saving | Type key into input → wait | `data-testid="ai-setup-key-input"` → `ApiKeyTester` | Own-account view; key text in input | Tester appears and shows success or failure badge | L1 | H | `tests/unit/onboarding-ai-setup.test.tsx` — test-key success + failure states |
| AS-05 | any | As a new user I want to click "Save and continue" with a valid key so that the key is persisted and I advance | Type valid key → click save | `data-testid="ai-setup-save-key"` | Valid key in input | `onSaveKey` called with provider and key; wizard advances | L1 | H | `tests/unit/first-run-mount.test.tsx` — "saving a key persists via KeychainService" |
| AS-06 | any | As a new user I want to see a clear error when my key is rejected so that I know to paste a different key | Type invalid key → click save | `data-testid="ai-setup-save-error"` | Own-account view; rejected key | Error message visible; wizard does NOT advance | L1 | H | NONE (rejection path not tested for AiSetupStep.handleSave; ApiKeyWizard covers similar in api-key-wizard.test) |
| AS-07 | any | As a new user I want to use local Ollama so that no data leaves my machine | Click "Keep everything on your computer" | `data-testid="ai-setup-local-view"` · `data-testid="ollama-status"` · `data-testid="ai-setup-use-local"` | Own Ollama running locally | Ollama status shows ready; "Use local AI" enabled; local-only mode set | L2 | H | `tests/unit/onboarding-ai-setup.test.tsx` — local path and local-only mode set |
| AS-08 | any | As a new user with Ollama not installed I want to see a download link so that I know how to install it | Click "Keep everything on your computer" → Ollama not found | `data-testid="ollama-status-missing"` · `data-testid="ollama-download-link"` | `detectOllama` returns reachable=false | Missing state shows with "Get Ollama (free)" button | L1 | M | `tests/unit/onboarding-ai-setup.test.tsx` — Ollama missing state |
| AS-09 | any | As a new user with Ollama installed but no models I want clear instructions to pull a model so that I can complete local setup | Click "Keep everything on your computer" → Ollama reachable, no models | `data-testid="ollama-status-no-models"` | `detectOllama` returns reachable=true, models=[] | No-models state shows pull command; "Check again" available | L1 | M | `tests/unit/onboarding-ai-setup.test.tsx` — Ollama no-models state |
| AS-10 | any | As a new user I want to click "Set this up later" so that I am never blocked on having an AI key | Click "Skip for now" card | `data-testid="ai-path-later"` | Choose view | `hasDeferredAiSetup()` returns true; wizard advances without a key | L1 | M | `tests/unit/onboarding-ai-setup.test.tsx` — skip sets deferred flag |
| AS-11 | any | As a user I want to open the "Where does my data go?" link from any AI setup view so that I can verify privacy claims | Click data-map link | `data-testid="ai-setup-data-map-link-choose"` · `data-testid="ai-setup-data-map-link-own"` · `data-testid="ai-setup-data-map-link-local"` | Any AI setup view | DataMapDialog opens | L1 | L | `tests/unit/onboarding-ai-setup.test.tsx` — data map reachable |

---

## 6. ApiKeyWizard (3-step modal)

| ID | Persona | Story | Steps | Surface | Precondition | Expected | Layer | Risk | Covered? |
|---|---|---|---|---|---|---|---|---|---|
| AKW-01 | any | As a user I want to open the API key wizard from Settings so that I have a guided 3-step flow for adding a key | Open Settings → AI → "Add API key" button → wizard dialog opens | `data-testid="api-key-wizard"` | Settings open | Wizard dialog renders on step 1 | L1 | L | `tests/unit/api-key-wizard.test.tsx` — "opens on step 1" |
| AKW-02 | any | As a user I want to advance steps 1→2→3 via Next so that I follow the setup flow | Click Next twice | `data-testid="api-key-wizard-step-1"` → `api-key-wizard-step-2"` → `api-key-wizard-step-3"` | Wizard open | Step content changes correctly | L1 | L | `tests/unit/api-key-wizard.test.tsx` — "advances 1→2→3" |
| AKW-03 | any | As a user I want to click Back so that I can correct previous steps | Click Next to step 3 → click Back twice | Back buttons | Wizard on step 3 | Steps 3→2→1 in order | L1 | L | `tests/unit/api-key-wizard.test.tsx` — "Back moves 3→2→1" |
| AKW-04 | any | As a user I want to switch provider tabs and have state reset so that I start fresh for each provider | Switch from Anthropic to OpenAI tab | `data-testid="api-key-wizard-provider-{id}"` | Wizard open | Provider changes; step resets to 1; key text cleared | L1 | L | `tests/unit/api-key-wizard.test.tsx` — provider switch resets state |
| AKW-05 | any | As a user I want to click "Open {Provider}" in step 1 so that the console opens in my browser | Click "Open Anthropic" / "Open OpenAI" / etc. | `data-testid` on console-launch button (step 1) | Wizard on step 1 | `openExternal` called with correct console URL | L1 | L | `tests/unit/api-key-wizard.test.tsx` — console URL opened |
| AKW-06 | any | As a user I want to see provider-specific tutorial steps in step 2 so that I know where to find the Create Key button | Advance to step 2 | `data-testid="api-key-wizard-step-2"` · `ProviderTutorialList` | Wizard on step 2 | Ordered steps for the selected provider visible | L1 | L | `tests/unit/api-key-tutorial-content.test.tsx` — tutorial content |
| AKW-07 | any | As a user I want to paste my key in step 3 and toggle show/hide so that I can verify it before saving | Advance to step 3 → type key → toggle eye icon | `data-testid="api-key-wizard-input"` · show/hide button | Wizard on step 3; cloud provider selected | Input toggles between password and text type | L1 | L | NONE (show/hide not explicitly asserted) |
| AKW-08 | any | As a user I want format validation to reject a clearly malformed Anthropic key so that I don't accidentally save garbage | Advance to step 3 → type `badkey` → click Save | `data-testid="api-key-wizard-submit"` · error message | Wizard on step 3; anthropic selected | Error "usually start with sk-ant-" shown; save blocked | L1 | H | `tests/unit/api-key-wizard.test.tsx` — format validation blocks malformed keys |
| AKW-09 | any | As a user I want live key verification when saving so that I know immediately if the key is rejected | Step 3 → type valid-format key → click Save | `data-testid="api-key-wizard-verifying"` · `api-key-wizard-result-ok"` / `api-key-wizard-result-rejected"` | Network available; cloud provider reachable | Spinner shown while verifying; ok badge or rejection message | L1 / L3 | H | `tests/unit/api-key-wizard.test.tsx` — submit calls onSaveKey (verification path via apiKeyValidation mock) |
| AKW-10 | any | As a user I want the Ollama flow in the wizard so that local model users have a guided path too | Select Ollama tab → advance to step 3 → check connection | `data-testid="api-key-wizard-step-3"` (Ollama variant) · `data-testid="api-key-wizard-ollama-check"` | Wizard open; Ollama tab selected | Step 3 shows Ollama connection check; Finish enabled when running | L2 | M | NONE (Ollama wizard flow not covered in api-key-wizard.test) |
| AKW-11 | any | As a user I want the wizard to close after successful key save so that I return to the settings surface cleanly | Complete step 3 with valid key | `api-key-wizard-submit` | Valid key; `onSaveKey` succeeds | Dialog closes; `onOpenChange(false)` called | L1 | M | `tests/unit/api-key-wizard.test.tsx` — onSaveKey called and wizard closes |
| AKW-12 | any | As a user I want to open the wizard in tutorial-only mode so that I can re-read the key-creation guide without the save path | Open tutorial-only variant (e.g. "View API Key Tutorial" button) | `data-testid="api-key-wizard"` with `tutorialOnly=true` · `data-testid="api-key-wizard-tutorial-close"` | Wizard opened with `tutorialOnly=true` | Only step 2 tutorial content shown; no step stepper; no key input; Ollama tab hidden | L1 | L | `tests/unit/api-key-tutorial-content.test.tsx` — tutorial-only mode |

---

## 7. AiSetupReminder (AI pane post-onboarding nudge)

| ID | Persona | Story | Steps | Surface | Precondition | Expected | Layer | Risk | Covered? |
|---|---|---|---|---|---|---|---|---|---|
| AR-01 | any | As a user who deferred AI setup I want to see a reminder banner in the AI pane so that I am nudged without being blocked | Open AI pane after skipping AI during onboarding | `data-testid="ai-setup-reminder"` | `hasDeferredAiSetup()=true`; `hasModelConnected=false`; not dismissed this session | Amber reminder banner visible | L1 | M | `tests/unit/onboarding-ai-setup.test.tsx` — reminder visible when deferred |
| AR-02 | any | As a user I want to click "Connect an AI" in the reminder so that I am taken directly to setup | Click "Connect an AI" link | `data-testid="ai-setup-reminder-connect"` | Reminder visible | `onConnect` callback fires | L1 | L | `tests/unit/onboarding-ai-setup.test.tsx` — reminder connect link |
| AR-03 | any | As a user I want to dismiss the reminder for the session so that it stops distracting me while I work | Click X dismiss button | `data-testid="ai-setup-reminder-dismiss"` | Reminder visible | Reminder hidden; sessionStorage flag set; does not reappear this session | L1 | M | `tests/unit/onboarding-ai-setup.test.tsx` — dismiss hides banner |
| AR-04 | any | As a user I want the reminder to disappear permanently once I connect a model so that it stops nagging me | Connect a model key in Settings → return to AI pane | `data-testid="ai-setup-reminder"` absent | `hasModelConnected=true` | Reminder not rendered | L1 | M | `tests/unit/onboarding-ai-setup.test.tsx` — hidden when model connected |

---

## 8. Setup Checklist (Settings > Onboarding tab)

| ID | Persona | Story | Steps | Surface | Precondition | Expected | Layer | Risk | Covered? |
|---|---|---|---|---|---|---|---|---|---|
| SC-01 | any | As a user I want to see live setup status (AI / Email / Firm) so that I know what is still needed | Open Settings → Onboarding | `data-testid="setup-checklist"` | Settings open | Three rows with circle/check icons; status reflects actual state | L1 | M | `tests/unit/setup-checklist.test.tsx` — renders all three rows |
| SC-02 | any | As a user I want "Set up" next to AI to navigate to the AI settings panel so that I can add a key without hunting | Click "Set up" on AI row | AI row action button | AI not connected | Navigates to AI settings category | L1 | L | `tests/unit/setup-checklist.test.tsx` — AI "Set up" calls onNavigate('ai') |
| SC-03 | any | As a user I want "Set up" next to Email to open Account connections so that I can connect my inbox | Click "Set up" on Email row | Email row action button | Email not connected | `keepance:open-account` event dispatched with `{tab: 'connections'}` | L1 | L | `tests/unit/setup-checklist.test.tsx` — Email "Set up" dispatches event |
| SC-04 | any | As a user I want "Set up" next to Firm to navigate to firm settings so that I can set up or join a firm | Click "Set up" on Firm row | Firm row action button | Firm not connected | Navigates to firm settings category | L1 | L | `tests/unit/setup-checklist.test.tsx` — Firm "Set up" calls onNavigate('firm') |
| SC-05 | any | As a user I want to restart guided setup without losing my data or keys so that I can reconfigure settings | Click "Restart guided setup" | `SetupChecklist` restart button | Any | `onRestartOnboarding` called; GuidedOnboarding re-shown without clearing localStorage | L1 | M | `tests/unit/setup-checklist.test.tsx` — restart button calls handler |
| SC-06 | any | As a user who has completed all steps I want to see "Manage" buttons instead of "Set up" so that I know everything is connected | View checklist with all connected | All three rows with `CheckCircle2` icon | All: AI key valid, email connected, firm signed in | All rows show green check and "Manage" label | L1 | L | `tests/unit/setup-checklist.test.tsx` — connected state shows Manage |

---

## 9. Feature Tour (post-onboarding)

| ID | Persona | Story | Steps | Surface | Precondition | Expected | Layer | Risk | Covered? |
|---|---|---|---|---|---|---|---|---|---|
| FT-01 | any | As a new user I want the feature tour to auto-show after completing onboarding so that I learn the interface | Complete onboarding → wait ~800ms | `FeatureTour` · `data-testid="feature-tour-center"` | `featureTour.shouldAutoShow=true`; not in testMode; onboarding overlay closed | Feature tour center step ("intro") appears | L1 | M | NONE (the auto-show hook is tested implicitly in first-run-mount but not the tour itself appearing post-onboarding) |
| FT-02 | any | As a user I want to step through all 11 tour steps so that I learn every main surface | Click "Next" 10 times → click "Finish" | `data-testid="feature-tour-next"` · `data-testid="feature-tour-finish"` | Tour open on step 1 | Each step title visible in sequence; onComplete fires at last step | L1 | L | `tests/unit/feature-tour.test.tsx` — all 11 steps, finish fires onComplete |
| FT-03 | any | As a user I want to skip the tour at any step so that I can start working immediately | Click "Skip tour" | `data-testid="feature-tour-skip"` | Tour open | Tour closes; `onSkip` and `onClose` called; persistent skip flag set | L1 | L | `tests/unit/feature-tour.test.tsx` — skip button |
| FT-04 | any | As a user I want Back navigation within the tour so that I can revisit a previous step | Advance to step 3 → click Back | `data-testid="feature-tour-back"` | Tour on step 2 or later | Step decrements; Back button absent on step 1 | L1 | L | `tests/unit/feature-tour.test.tsx` — Back navigation |
| FT-05 | any | As a user I want Escape to close and skip the tour so that I can exit with the keyboard | Press Escape | Keyboard handler | Tour open | Tour closes; onSkip fired | L1 | L | `tests/unit/feature-tour.test.tsx` — Escape closes tour |
| FT-06 | any | As a user I want arrow keys to navigate the tour so that I can step through without clicking | Press right arrow → left arrow | Keyboard handler | Tour open | Right: advances; Left: goes back | L1 | L | `tests/unit/feature-tour.test.tsx` — arrow key navigation |
| FT-07 | any | As a user I want tour steps that target a missing DOM element to auto-advance so that the tour doesn't get stuck | Run tour without the Spine nav testids in the DOM | `AutoAdvance` component | Tour open; target selectors resolve to null | Steps auto-skip to next; tour still completes | L1 | M | `tests/unit/feature-tour.test.tsx` — auto-advance when target missing |
| FT-08 | any | As a user on a small screen I want tour bubbles to flip placement so they stay on screen | Resize window to force edge collision | `AnchoredBubble` | Tour open; anchored step; small viewport | Bubble flips to opposite side | L1 | L | NONE (visual positioning; not asserted in unit tests) |
| FT-09 | any | As a user I want `?forceTour=true` to re-trigger the tour so that QA can test it without clearing flags | Navigate to `/?forceTour=true` | App.tsx `FORCE_TOUR` flag | Any localStorage state; tour previously completed | Tour opens regardless of persistent flags | L1 | L | NONE (no dedicated test) |
