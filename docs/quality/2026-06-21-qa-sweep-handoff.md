# QA Sweep Handoff — 2026-06-21

**Branch:** `keepance-3.0` · **HEAD:** `717aa15` (== origin, fully pushed) · **Nothing deployed** (a real build/release still needs Jameson's explicit go).

This session ran a deep, adversarial QA + security sweep of Keepance using **6 independent Codex audits** (binary blast-radius, matter-isolation, data-loss, licensing, prompt-injection, workspace-boundary — each found 6–12 real issues) plus targeted fixes. **31 fix batches** were committed and pushed; **~33 bugs fixed** (each with a red-capable regression test); **~30 deeper findings logged** for a focused effort or Jameson's decision.

**The single source of truth for every finding + status is [`docs/quality/2026-06-20-test-bug-backlog.md`](./2026-06-20-test-bug-backlog.md) (BUG-001 … BUG-061).** Read that first.

## What's reassuring (audits confirmed these HOLD)
- **Matter isolation** for AI *search* is enforced at the database level (matter filter before the query; IDs validated + SQL-escaped).
- **Your own files are NEVER locked behind a license** (`entitlements.ts` `dataAccessAlwaysTrue: true`); pre-3.0 grandfathering is handled first.
- Answers escape HTML before render; citations are verified against real retrieved chunks; email HTML is converted to text (tracking-pixel safe); local-only mode blocks cloud egress (round-3 BUG-021).

## Fixed + pushed this session (commits f51a3fc → d7e8d0e)
- **Binary file integrity** (BUG-033/034/035): byte-safe download/move/copy + a broadened `isBinaryFile` classifier (+ Codex-review refinements: dropped ambiguous `.ai`/`.fig`, gated the content-indexer so big binaries don't spike memory).
- **Confidentiality report** names the model instead of "unknown" (BUG-028); reconciled 6 stale-but-already-fixed backlog entries.
- **Matter-isolation hardening** (BUG-036/037/040): chat file tools + open-file context now respect the active matter (a chat scoped to Matter B can no longer reach Matter A's files); deleted-matter index purge. Shared guard `src/platform/matter/matterScopeGuard.ts`.
- **Data-loss hardening** (BUG-043/044): atomic `.docx` save (crash-safe temp+fsync+rename); workflow terminal record is awaited.
- **Licensing** (BUG-052/053): closed the `?fakeLicense=` production backdoor; gated inline AI editing by the same entitlement as chat/redline.
- **Prompt-injection hardening** (BUG-059): untrusted text (open files, PDF attachment text, email "Draft with AI") is now sanitized + framed as "DATA, not instructions."
- Workflow model fallback (BUG-025), trust-bar honesty + settings import (BUG-023/026), and the round-3 privacy/data-loss fixes (BUG-021/022/027/029/030/031/032) all landed too (see backlog).

## Gates (green as of the last push)
- `npm run typecheck` clean; full Vitest **3545 passed / 3 skipped**; pre-push hook (typecheck + vitest) passed on every push.
- Rust: `cargo check` clean; `cargo test` for the touched areas green (atomic-write 4, rag_delete_matter 3, docx).
- **ESLint CI gate:** my work adds **ZERO net new findings**, BUT there are **~15 PRE-EXISTING low-severity drift findings** (App.tsx, useFileOperations react-compiler memoization, MailConnect i18n, fileDrop, mail-commands, localOnlyGuard) not in `.eslint-baseline.json` that make the CI `quality` job red independent of this work. Separate CI-health cleanup — verify your own delta with `git stash -u` → run `node scripts/eslint-gate.mjs` → compare; bump only YOUR tuples, don't blanket `--update-baseline` (it would bury the drift).

## Biggest OPEN clusters (need Jameson's decision OR a focused, careful effort)
1. **SAVE-PATH data-integrity (BUG-045–049) — ✅ DONE + Codex-reviewed 2026-06-21 (commits `59c47f1` → `2731de2`).** Built the central per-path **`WriteCoordinator`** + revision-checked `markSaved` + the `flushDirtyTabs` primitive, all TDD'd. Fixed: BUG-045 (stale-autosave overwrite — Critical), BUG-046 (flush on workspace-switch + app-close/reload + every tab-close), BUG-047 (AI write/move/delete BLOCKED on a file open with unsaved edits), BUG-048 (`.source` saved-before-disk), BUG-049 (version-index race + `.docx` read size-cap). A consolidated **Codex review then caught + fixed** a regression (discard-close was saving) + the Critical `.docx`-bypasses-coordinator gap + a manual-save stale-content bug + switch-clears-on-failed-flush. **All review follow-ups then done too (commits → `717aa15`):** AI write/move/delete now refuses ANY open file (not just dirty — BUG-047 #7); a FAILED close-flush re-opens the tab + warns instead of dropping it (#4); version history rebuilds from snapshot files on a corrupt index (BUG-049a); the coordinator `isLatest` is documented advisory-only (#8). **ONLY residual — bench-gated:** a fully-awaitable Tauri `onCloseRequested` for a guaranteed save-on-exit (#3) — pagehide can't await an async write; desktop-window behavior verifiable only on a real Windows/Mac build. The pagehide flush + autosave + per-tab/switch flushes cover the common cases meanwhile. **This cluster is otherwise complete + independently reviewed.**
2. **Firm tier (BUG-041/050/051)** — ethical-wall local key/chunk purge on revocation, and the co-edit **CRDT lost-update** + remote-`.docx`-not-saved-locally. Firm-crypto + CRDT = xhigh-effort; needs Jameson awareness.
3. **MCP (BUG-038/039)** — external MCP reads/search are whole-workspace, not matter-scoped; "network lockdown" only blocks MCP *writes*. Architectural (the sidecar has no matter session).
4. ~~**Matter-delete semantics (BUG-042)**~~ — ✅ **DECIDED + DONE + Codex-reviewed twice (2026-06-21).** Jameson's call: **Delete = remove the matter + wipe the AI's memory of it, but KEEP the user's files** (Archive already = hide-and-keep; deleting documents would violate a law practice's retention duties). On delete, the matter's emails are re-filed to an explicit **"unassigned" tombstone** (Rust `clear_message_matter_for_matter` does an `UPDATE`-to-unassigned, not a delete) + a shared `resolve_effective_matter` helper routes all three RAG-index sites. **A Codex review caught a real cross-matter leak** in the first cut (a plain delete let an email filed to deleted matter A, but in a folder mapped to live matter B, silently move into B) — the tombstone fixes it: a deleted matter's emails become unassigned, **never** another matter's. A second Codex pass confirmed the leak is closed on every path with no regression. TDD'd both sides; full mail cargo suite green (190); the delete-confirm message is honest. **One tracked residual** (Codex Finding #2, Med, durability not leak): the cleanups are fire-and-forget with no retry — see the BUG-042 backlog entry; the robust hardening (durable retry or startup reconciliation) is a scoped follow-up shared with BUG-040.
5. **Revenue / pricing / payment backend (BUG-054–057)** — firm min-3-seats not server-enforced (PRICING decision), refund/cancel/downgrade webhooks unhandled, buyer may never see their license key, client JWT not signature-verified. Deploy-gated (firm backend is live at api.keepance.com).
6. **Per-action AI-write approval (BUG-060)** — 🟢 **DONE — all 3 layers built + Codex-reviewed (2026-06-21).** Jameson's call: a user setting `aiFileApprovalMode` with three modes, default **`risky`** (pause + before/after only for overwrite/delete/move-onto-existing; new files run free), plus `always` and `batch`. Per-action (`risky`/`always`) shipped earlier (HEAD `97177f0`). **`batch` mode (layer 3) is now finished too:** each change applies immediately but its before-state (text + bytes, 25 MB cap) is snapshotted and collected, and a single end-of-turn review panel lists every change with a diff + Keep-all / Undo-selected; undo is byte-restore based and wired per op kind. New files `aiBatchReview.ts` / `aiBatchReviewStore.ts` / `AiBatchReviewPanel.tsx` (+20 tests); full suite green; zero net-new lint. Only residual is bench-visual polish of the panel. See the BUG-060 backlog entry. Memory-fact poisoning (BUG-061) is still separate/open. _(BUG-059 workflow-excerpt framing is DONE, commit `3f079b4`.)_

   ⚠️ **STASHED MARKETING WORK — DO NOT restore wholesale (corrected 2026-06-21).** The two `git stash` entries (`stash@{0}`, `stash@{1}`, both "autostash") are **NOT** identical and are **~7 weeks STALE**: they were stashed on **2026-05-03**, i.e. BEFORE the Projelli→Keepance rebrand. Their `index.html` has **62 "Projelli" / 0 "Keepance"** mentions while HEAD has 0 Projelli / 58 Keepance and the whole current `website/` is Projelli-free. **Applying them would drag the old "Projelli" name back across the site and effectively revert the rebrand** (it also conflicts heavily — 16 files, including a delete/modify on `SHOW_HN_LAUNCH.md`), so they were deliberately left parked, NOT restored. The two stashes are byte-identical for 29/30 files; they differ only in `website/index.html`, where `stash@{0}` is the em-dash-cleaned version (the better one). **Awaiting Jameson's decision: discard the stale stash, salvage only any genuinely-new ideas re-branded to Keepance, or leave parked.** Nothing is lost (stashes intact + reflog-recoverable).
7. **Workspace-boundary hardening (BUG-062)** — the symlink escape (genuinely reachable) + Rust native commands trusting caller-supplied absolute paths (reachable via a compromised renderer). Robust fix = ONE canonical workspace guard in trusted Rust state, canonicalizing both root + candidate (handles macOS `/var` symlink trap), routing every native fs/docx/vault/rag/convert command through it. **Needs real Win/Mac verification** — do NOT rush piecemeal. The frontend `PathValidator` itself is solid.

## Gotchas for the next session
- **Backgrounded `codex-task` flakes on the stdin-hang even with `< /dev/null`** (prints the prompt, exits 0, no findings). Run Codex audits in the FOREGROUND (the harness auto-backgrounds long ones, but they actually execute). Use `codex-task --read-only "<prompt>"` (NOT `codex-review`, which recurses on AGENTS.md→CLAUDE.md in this repo).
- Use `node scripts/eslint-gate.mjs` (fingerprint/count baseline) to check lint, not raw `npx eslint` (which shows all pre-existing drift).
- Untracked `docs/superpowers/plans/2026-06-20-onboarding-journey.md` is from the separate onboarding-journey work stream (PR #33, branch `feat/onboarding-journey`) — not part of this sweep; left as-is.
