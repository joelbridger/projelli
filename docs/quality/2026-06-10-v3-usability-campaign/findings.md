# Advisor Prep Hero 3.0 Quality Campaign — Findings Log

Severity: P0 ship-blocker / P1 fix before release / P2 fix soon / P3 polish.
Type: bug / inconsistency / copy / ux-improvement.
Status: open / fixed / wontfix / needs-verify.

| ID | Sev | Type | Status | Finding | Evidence | Notes |
|---|---|---|---|---|---|---|
| F-001 | P2 | inconsistency | fixed | Website + public/ favicons still OLD Projelli brand (`website/android-chrome-512x512.png`, `public/favicon-256x256.png`) while the app now ships the Advisor Prep Hero K-shield. Brand mismatch between site and app. | Worker E visual audit 2026-06-10 | Website icons (android-chrome 192/512, apple-touch) regenerated from the brand SVG 2026-06-10; `public/favicon-256x256.png` deferred to the fix wave (swapping a public/ asset mid-campaign can force-reload the dev app under the live test agents). |
| F-011 | P2 | copy | open | website/og-image.png carries the pre-3.0 positioning ("The AI workspace for work that can't leave your machine", "attorneys, CPAs & consultants") while the site sells the 3.0 law-practice positioning. Social shares show the old story. | og-image visual check 2026-06-10 | Fix wave: regenerate the og card with the 3.0 line (private intelligence layer for a law practice); brand mark on it is already current. |
| F-002 | P3 | bug | needs-verify | Possible es/de locale loss on relative navigation in the e2e harness (`?lang=es` reported dropped, app falls back to English). Full Playwright locale projects pass, so this may be invocation-specific; if real in-app, locale switching may not survive navigation. | Fix-agent report 2026-06-10; contradicted by green locale-matrix runs | Verify during mechanical sweep (Phase 5) with an in-app language-switch journey. |
| F-003 | P2 | bug | fixed | macOS `.icns` was stale Projelli since June 3 icon pass; now regenerated via `tauri icon` (fixed alongside Bug 1), but NO automated guard exists for `.icns` brand drift (hash test covers `.ico` only). | `src-tauri/icons/icon.icns` regenerated 2026-06-10 | Extend `tests/unit/branding-icons.test.ts` with an icns hash guard during fix wave. |
| F-004 | P3 | ux-improvement | open | Workflow overflow e2e detector cannot reproduce the Windows clip in headless Chromium (body clips overflow differently than the Tauri WebView), so the regression spec guards document-level overflow only. The founder's screenshot scenario (workflow tab + adjacent split pane squeezed off-screen) should be re-verified in the native pass. | Worker D report 2026-06-10 | Add split-pane + workflow-tab scenario at 1366px to native pass checklist (Phase 6). |
| F-005 | P3 | ux-improvement | open | Accordion primitive lacks aria-controls/region linkage and arrow-key navigation; AccordionItem outside an Accordion silently no-ops. | Quality review 2026-06-10 (#14) | Fold into fix wave; also covered by the Phase 5 axe scan. |
| F-006 | P3 | bug | open | PaneScrollable duplicates Pane header/footer markup (drift risk) and uses an unexplained inline maxHeight calc(85vh - 120px). | Quality review 2026-06-10 (#10) | Consolidate via a scrollable prop on Pane during fix wave. |
| F-007 | P3 | ux-improvement | open | Two onboarding steps share data-testid="onboarding-next" while workspace uses its own id; cross-step re-query can mask a failed click in specs. | Quality review 2026-06-10 (#11) | Per-step testids during fix wave. |
| F-008 | P3 | inconsistency | open | tests/unit/open-in-explorer-path.test.ts header overclaims ("guards the actual logic"): the resolver is production-bound but the handler wiring/selection flow is untested. | Quality review 2026-06-10 (#8) | Soften comment or add a handler-level test during fix wave. |
| F-009 | P2 | ux-improvement | open | Matter management (incl. the new firm share/invite flows) is ONLY reachable from the matter scope selector inside an open AI chat header (MatterManagerDialog mounts solely in AIChatViewer). A user who has not opened a chat cannot find matters at all; the status bar shows the active matter but offers no management entry. | firm-collaboration e2e failure analysis 2026-06-10 | Persona study should confirm; likely fix wave item: mount matter management from the status-bar matter widget and/or settings. |
| F-010 | P2 | ux-improvement | open | Firm key handshake needs manual coordination: an invited member's first open registers their device but gets no key until an ADMIN clicks "Re-publish keys to all member devices", then the member opens again. Three-step human dance; conflicts with the persona's "it needs to set itself up" bar. | firm-collaboration e2e fix 2026-06-10 | Fix-wave candidate: pending-key-request queue visible to admins, or auto-publish on member device registration (owner-side background re-wrap). |
| F-201 | P3 | layout | open | Sidebar collapse button: post-expand `getBoundingClientRect().right` briefly reports ~89px beyond 1366px viewport after animation. Document scrollWidth == clientWidth (1366); no actual horizontal scroll. Detection artifact inside the `overflow-hidden` sidebar container. | sweep/sidebar.spec.ts L-010 console.warn + scrollWidth verification | |
| F-202 | P3 | layout | open | Split pane overflow false positive: second MainPanel's tab-bar toolbar reports right=1414 via getBoundingClientRect() when split is active at 1366px. Document scrollWidth == clientWidth (1366) — no user-visible overflow; element is inside SplitPane's overflow-hidden pane wrapper. Same root cause as F-004's Tauri/headless discrepancy. | sweep/shortcuts.spec.ts L-140 + misc-surfaces.spec.ts L-216 console.warn; scrollWidth confirmed 1366 | Prior reports described this as a real overflow. Confirmed false positive 2026-06-10. |
| F-210 | P2 | spec | open | `horizontalOverflow()` helper in tests/e2e/helpers/overflow.ts does not skip elements whose overflow-hidden ancestors contain them. Reports false positives for split-pane pane content (right=1414 but no scroll). Improvement: check if any ancestor has `overflow: hidden` or `overflow-x: hidden` before reporting. | Phase 5 sweep investigation 2026-06-10 | Affects L-010, L-140, L-216. Fix in the helper; not a product code change. |
| F-203 | P3 | spec | fixed | Template spec had wrong IDs for 18 templates: legal pack uses `legal-` prefix (10 templates), tax pack uses `tax-` prefix (7 templates), biz `weekly-review-workflow` → `weekly-review`. Caused L-081 (engagement-letter-builder) to time out and L-096 to fail. Fixed in templates.spec.ts 2026-06-10. | sweep/templates.spec.ts ID audit vs src/modules/workflow/templates/*/  | Not a product bug. |
| F-208 | P3 | note | open | No language picker (select/combobox) found in General settings at testMode URL. i18n locale-switching is not exposed in this build's settings UI. F-002 locale-loss finding remains needs-verify — cannot exercise the switch-locale journey in browser sweep. | sweep/misc-surfaces.spec.ts F-002 snap: F-002-no-language-picker | |
| F-301 | P0 | bug | fixed | RELEASE BLOCKER: the compiled desktop app leaks unbounded memory under GUI use: kernel OOM log shows the keepance process at anon-rss ~24 GB (total-vm 113 GB) before OOM-kill at 2026-06-10 08:05 UTC, and a second runaway froze the host ~12:09-12:15 (hard power cycle). Both windows correlate with the native-pass automation driving the app (early checklist: first-run wizard / onboarding / folder picker territory). A real customer machine would suffer the same. | ~/jameworld/docs/incidents/2026-06-09_oom-hang.md (Recurrence section); kernel OOM log PID 1942633 | Investigation running under a hard 3GB cgroup cap; native pass paused until the growth driver is identified and fixed. Host-side containment is in place (user slice MemoryMax=20G) but is mitigation, not the fix. |
