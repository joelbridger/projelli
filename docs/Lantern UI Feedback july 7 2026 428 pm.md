This document outlines detailed and thorough user interface feedback for the Lantern app dated 4:25 pm 07/07/26

Please make sure that this feedback is incorporated as follows: First it should be ingested by a Fable 5 model, and that same Fable 5 model should interview Jameson with any questions at all to clarify exactly what he wants and clarify anything confusing about the feedback. Then that same Fable 5 model should craft a master plan to incorporate all the feedback. That master plan should be implementable by sonnet agents (and opus 4.8 if necessary for critical work). Then the Fable 5 Coordinator should Act as a Coordinator and follow the Coordinator playbook to spawn agents to implement everything.


New feedback: First thing is the Advisor Prep Hero logo. Both the text and the icon are just pure white and I can barely see them on the light blue background of the left panel. So these need to be full color, dark versions. Next, I don't think it makes much sense to say the client's name with a suitcase on the very top row of the app, the top bar next to the back button. Let's just remove that completely. And I also don't like the back button all the way up there to the top left. It doesn't really make sense What really should be up there is the logo So we should move the logo out of the left pane into the top left of the very top bar And then we should move the client map and ask and workflows tabs up To fill that space And this also means making the very top bar taller to fit the logo. Next I'm finding a weird situation in the client map. If it's using local AI, there's a big white bar above the client map but below the tabs. And if it's using local AI, it says "running on device only generation uses your local model" but that text is very small. We don't need this text to indicate local AI. That is already indicated in the green pill at the top right. So we can remove running on device only and generation uses your local model text. And then we need to bump the entire container of the client map that shows the vertical tabs and the source pane. And then we need to bump that up so that it aligns with the bottom of the row that shows the client name and the download button and sync button and clock button, etc. Similarly there is a smaller white gap if I switch to using cloud AI between the client map and the row that has the name and the tabs and the sync button and clock etc. So we also need to move that area up. Also, I'm changing my mind about the three icons: the export, the sync, and the clock icon. I think we should have just three dots next to the client name that expands into a menu that lets you do each of these things. And the export needs to be clear. It needs to say export client map parentheses docx and export client map parentheses PDF. And then in that same three dot menu, it should say sync all and have a link that says history. And this way we can just have a clean top bar that has the client name, then three dots next to it, and then the status of when it was last updated. Also for the history pane that pulls out, it's very awkwardly laid out because each timed row item is cut off and the containers that show certain words are cut off on the right. So we need to figure out how to fix this layout for the history pane. Also the star icon that's used for the guided interview isn't the star I was thinking. I was thinking we would have AI stars there. Basically the same icon as in the ask tab, the three little stars together. Also in the sources pane, the little icon that shows the document type, all of them are red, which in the documents tab, red I believe means PDF and blue is Word. So we need to change the sources pane to accurately show what type of document it is. So it would be a blue document icon if it was Word and red for PDF. The vertical tabs in the Documents tab work pretty well. The one thing I wish was better is that when you're dragging a vertical tab in between other tabs, that it would more easily slide in between other tabs. Right now you really have to hover against the edge of another tab. Ideally it would slide in betwee another tab easier. Also there's a bug where if you drag a file into a group or out of a group the whole tab area stays dark gray and that doesn't change until you drag something in between something else. It seems like it gets stuck on the selected dark gray color for that whole pane. And then moving to the main content area of the Documents tab. Unfortunately, we have too much going on still over here. We need to combine the mostly empty row that says Saved and then has three dots. We need to combine that with the row that says the document name. And then we need to combine that also with the whole row that repeats the document name. And has the draft follow-up, Send to Wealthbox, Export, Revise with AI. All of that, all of those buttons should be folded into the three dots. And not be shown as separate buttons in this screen. So what we should end up with is a clean top row in the document view that has the document icon showing what type of document it is, then the document name, then the ability to edit that name with a small pencil icon, and then three dots that has everything in the current three-dot menu, such as download history, split horizontally, toggle outline, and also added the buttons that say Draft Follow-up, Send to Wealthbox, Export, and Revise with AI. And then on this clean row, after the three dots, we can show the saved indicator, and then the Reviewing toggle, and then the Collapse button. Basically, we need to create one clean row underneath of the New Document/New Folder buttons, and above the document editor area itself. IN ADDITION - We need to basically also combine all of the buttons in the row with the buttons that include new document, new folder, add files, tree and grid view. This all needs to go under a plus icon next to the title files in the left hand pane of the vertical tabs. These don't need to be separated out in the UI so they can be consolidated with a plus icon in the left pane by files text. And the tree versus grid toggle should only really appear when you are inside of the files tab itself. So that should appear at the top of looking at all the files. And then the search should also be at the top of just all of the file view. And then you'll notice once we do all of that work, that entire top row can basically go away so that we have a cleaner documents tab. And then for the email tab, I really haven't seen an example yet of where we have emails that have been imported. But I already know that I'm going to want the same visuals set up as the client map and the documents tabs. Basically, we need a vertical tabs list on the left to show different emails. And then a content area on the right to display those emails. And the same thing with the top bar on the emails tab that currently says new email and has a keyword AI search toggle and the filters. All of those need to collapse into the top left of the vertical email tab pane under a plus button and a three dot menu. And now that I'm looking at the meetings tab, I want it set up the same way as well. Basically vertical tabs in a left pane that show meetings, and then you select one and you see the details in the content area on the right. With record a meeting button being added in the left hand vertical meeting tab pane at the top with just a microphone icon. I also already know that I'm going to want to reorganize the entire workflows tab instead of being a bunch of cards to be a bunch of left hand side vertical workflow tabs that you're able then to run and all the details and the progress of that particular workflow populates in the content area on the right. As you can see there's a trend here for each tab in this app I'd like the same setup with a vertical list on the left and then a content area on the right. I just noticed that we also need the ability to collapse the sources tab in the client map area.
