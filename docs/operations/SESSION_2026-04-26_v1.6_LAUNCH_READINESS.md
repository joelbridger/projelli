# Session handoff — v1.6 launch readiness audit (CEO mode)

**Date:** 2026-04-26 → 2026-04-27 (active)
**End state:** rc.10 building (run id 24972315448), Mac fixes for API key 404 + modal overflow committed cleanly. Full launch readiness audit complete. One Jameson task pending (rc.10 dogfood + LS test license).

---

## What changed this session

### Code (release/v1.6 branch)

| Tag | Commit | Change |
|---|---|---|
| `v1.6.0-rc.7` | (superseded) | Wrong fix — removed standalone `**` from fs:scope, but the real bug was `C:\**` with backslashes that the glob crate can't parse. |
| `v1.6.0-rc.8` | (superseded) | Real Mac scope fix — collapsed allow list to a single valid `**/*` pattern. Mac workspace creation finally worked. |
| `v1.6.0-rc.9` | 103057e | `requireLiteralLeadingDot: false` so `.trash` workspace folder isn't rejected as a "forbidden path"; CSP `connect-src` += `ipc: http://ipc.localhost` to silence devtools warning. |
| `v1.6.0-rc.10` | 8da5619 | API key validator switched to `GET /v1/models` (was POSTing /v1/messages with the now-retired `claude-3-haiku-20240307`). PromptDialog body + flex container get `min-w-0` to stop long workspace paths from expanding the modal grid track. |
| (branch HEAD) | 4f59f83 | README + getting-started.html + faq.html updated to remove "Phase 0", "Week 3", "currently unsigned" stale copy. |

The first rc.10 commit (`285733b`) accidentally swept up `~/projelli/screenshots/*` and an unfinished blog draft via `git add -A`. Force-pushed a clean rewrite (`8da5619`) and re-tagged. Memory updated with a new rule: never `git add -A` in Jameson's repos. See `~/.claude/projects/-home-jameson/memory/feedback_reproduce_and_bundle.md` rule 5.

### Public docs

- `README.md` — v1.5/v1.6 status + signed Windows + Mac Gatekeeper bypass + Linux as v1.7
- `website/docs/getting-started.html` — same install rewrite for buyers
- `website/docs/faq.html` — replaced "Coming in Week 3" macOS line with current state

These will go live on projelli.com when the v1.6 ship procedure runs `infra/deploy.sh`. Not deployed yet.

---

## Launch readiness scorecard (verified this session)

| Area | State | Evidence |
|---|---|---|
| **Windows signing** | ✅ live | Azure Artifact Signing, all 6 secrets in GitHub, builds rc.4 onward sign cleanly |
| **Mac signing** | ✅ live, ⚠️ unnotarized | Developer ID cert wired since 2026-04-09; Apple notary intentionally OFF in `release.yml:203-205` due to documented service degradation since March 2026 |
| **Auto-updater path** | ✅ valid | Same minisign pubkey across v1.0.8 and v1.6 → existing v1.0.8 installs will accept v1.6 update on launch |
| **License validator service** | ✅ running 24/7 | `license-validator.service` active since 2026-04-09; `licenses.projelli.com/healthz` returns ok |
| **LemonSqueezy env** | ✅ all set | `LEMONSQUEEZY_API_KEY`, `STORE_ID`, `WEBHOOK_SECRET` all present in `/etc/license-validator.env` |
| **Buy URL** | ✅ live cart | `projelli.lemonsqueezy.com/checkout/buy/33cd497b-bffd-404c-910e-f8dd1f4453bd` returns 302 → real cart |
| **Validator endpoints** | ✅ probed | `/activate` cleanly rejects fake keys ("license_key not found"); `/validate` rejects malformed JWTs |
| **Real activation history** | ⚠️ zero | `data/activations-*.jsonl` and `data/revocations.jsonl` empty — buy→activate loop has never been walked end-to-end with a real LS license key |
| **App-side license UI** | ✅ wired | `useLicense` hook + `LicenseSettings.tsx` component, JWT in localStorage, weekly re-validation |
| **Legal docs** | ✅ live | privacy / terms / EULA at projelli.com/legal/ |
| **User docs** | ✅ live | getting-started / api-keys / faq at projelli.com/docs/ |
| **Marketing playbook** | ✅ drafted | PH, HN, IH launch packages + 10 email sequence + press kit |

---

## What's actually gating launch

In rough priority order:

1. **rc.10 Mac dogfood** (in flight) — Allison installs rc.10 aarch64 DMG, confirms (a) Claude API key adds without 404, (b) "New file" modal input no longer hangs off the right edge, (c) workspace creation still works.
2. **Real LS test purchase rehearsal** — Jameson generates a test license key in the LemonSqueezy dashboard, pastes it into the app's License panel, confirms it activates with the right tier. This is the one loop nobody has walked. Only takes ~5 minutes.
3. **Notarization probe on rc.11** — uncomment 3 lines in `release.yml`, push a fresh tag, see if Apple's notary service has recovered. If yes, ship rc.11 notarized. If no, ship as-is with the documented Gatekeeper workaround.
4. **Clean-Mac install test** — a Mac that's never seen Projelli, fresh user, walk: download → bypass Gatekeeper → workspace → API key → first AI chat → save artifact. Until this works without intervention, Mac launch isn't proven.
5. **Jameson Action Pack items** — see `docs/marketing/action-packs/JAMESON_ACTION_PACK.md` (note: marketing folder was reorganized 2026-04-22 — old `docs/features/*.md` paths no longer apply; the new entry point is `docs/marketing/README.md`). Companion doc `docs/marketing/action-packs/BETA_TESTER_CANDIDATES.md` has 36 pre-researched targets with personalized DMs ready. None of these are technical blockers; they're launch-day reach blockers.
6. **Jameson voice review** of any v1.6 launch announcement (blog post + email + social). Per `feedback_jameson_voice_profile.md` and `feedback_marketing_copy_voice.md` memory, never auto-publish marketing copy. Existing playbook drafts need a final voice pass.

---

## Operating delegation in effect

Jameson explicitly delegated CEO authority on 2026-04-27: *"You're my CEO, I trust you, only come to me for critical questions and decisions."* Per business-plan ratification, this excludes:

- New spend
- Identity / employer / NDA exposure (Wheel Health)
- Strategy ratification

Everything else is decided in-session.

---

## Immediate next step (for whoever picks up)

1. Wait for rc.10 watcher to fire (background task, polling `Projelli_1.6.0_aarch64.dmg`).
2. When it lands, ping Jameson with the download URL + a tight 3-step test (workspace, API key, new-file modal) AND ask for an LS test license key in the same message. Bundle the asks.
3. If rc.10 passes, tag rc.11 with the notarization probe (uncomment 3 lines in release.yml).
4. If notary works in rc.11, that's a launch candidate. If notary stalls > 1 hour, abandon and ship rc.10 as the launch candidate.

---

## Known cruft to ignore

- `~/projelli/screenshots/` is Jameson's local install-screenshot folder (still untracked after the force-push cleanup). Don't touch.
- `~/projelli/website/blog/the-notion-ai-math-for-solo-founders.html` is an unfinished blog draft from a previous session, also untracked. Leave for Jameson.
- `~/projelli/website/blog/index.html` has uncommitted modifications (probably the link to the unfinished post). Leave for Jameson.
