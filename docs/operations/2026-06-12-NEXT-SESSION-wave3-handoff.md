# Next-session handoff — Keepance, Wave 3 (written 2026-06-11)

> **⛔ SUPERSEDED 2026-06-13.** Waves 1–4 are all DONE and **deployed as v3.2.0**. For the current, authoritative state of everything (release, services, vendor threads, open items), read **`docs/operations/2026-06-13-CURRENT-STATE.md`** instead. This file is kept for history.

**Read this first.** Branch `keepance-3.0`, everything committed + pushed (`HEAD == origin/keepance-3.0 == 69703a4`, working tree clean). Plan of record: `docs/strategy/2026-06-10-vision-gap-closure-plan.md` (the 5 waves to 100% of the vision). Follow the **Token-Budget Operating Mode (Keepance only)** in `CLAUDE.md`: drive on Opus 4.8 at `high`; delegate well-specified implementation to Sonnet 4.6 subagents and mechanical work to `model: "haiku"`; **raise to `xhigh` only for the encrypted vault (VG-6d-v2) and the co-editing CRDT (VG-8)**; Fable 5 is break-glass only.

## Status: Waves 1–4 COMPLETE + DEPLOYED as v3.2.0 (2026-06-12) — only Wave 5 (VG-9 connectors, vendor-gated) remains

> **DEPLOYED 2026-06-12.** Desktop **v3.2.0** published (signed Win/Mac/Linux, auto-updater serving 3.2.0 all platforms); firm backend redeployed to `api.keepance.com` (SSO + relay `doc_id`); keepance.com deployed (download links v3.2.0, SSO claim live; smoke-test green). **Release gotcha fixed:** the Windows tauri build failed on a Unix-only `mkdir -p` in the OCR asset-copy npm script — now a cross-platform Node script (`scripts/copy-build-assets.mjs`); the Linux dev rig can't catch this, only a real Windows CI build does, so keep all `prebuild`/`predev` asset steps cross-platform. Jameson-owned follow-ups: send the drafted iManage partner-form reply (in Outlook, not sent); one real vault enable/recover + open a co-edited `.docx` in real Microsoft Word.

> **UPDATE 2026-06-12: Wave 4 (VG-8 live multi-user .docx co-editing) is DONE — pushed (HEAD==origin==697ab41).** The OOXML paragraphs→runs tree is now a **yjs** document-CRDT (the spike's proof transferred from yrs); edits sync as E2EE blobs over the existing matter relay (new `doc_id` stream partitioning, notes default `_notes`), reusing the per-matter key+epoch; the contenteditable editor binds per-run via text-diff; saves serialize back to `.docx` via keepance-docx with comments + unique revision ids preserved. Design `docs/superpowers/specs/2026-06-12-live-coediting-design.md`; plan `docs/superpowers/plans/2026-06-12-wave4-live-coediting.md` (13 tasks, Opus reviewed every diff). The 5 spike convergence cases re-proved on the production model with ZERO converter fixes; chaos + offline + fidelity gates green. **Reviews caught + fixed 5 real defects** (comments-drop on save; corrupt empty w:id; accept/reject bypassing the CRDT; tracked-changes losing attribution in co-edit; splitParagraph crashing on tracked runs). Presence is real (relay subscriber count). Suites green: client 3058, backend 199, keepance-docx 98. Details in the gap-closure plan's VG-8 STATUS line. **Pillar 6 is complete.**
>
> **The ONLY remaining wave is Wave 5 = VG-9 connectors (Clio → Office add-ins → NetDocuments → iManage).** It is **externally gated on vendor access** (`docs/operations/2026-06-10-vendor-access-track.md`): Clio/NetDocuments/iManage developer programs + Microsoft Partner Center all need Jameson's identity/payment/signature and the vendors' humans to grant sandbox access. **It cannot be fully built autonomously until that access lands** — so this is the honest end of the autonomously-buildable vision. When a vendor's access arrives, the connector build is a normal wave (its own plan, OAuth2 sandbox round-trip, data-map honesty, claim-only-when-shipped). Replies go to developers@keepance.com (read via the `outlook` CLI).
>
> **Jameson-owned items that gate 100% / real-world proof (none block code):** the real-Microsoft-Word open-without-repair fidelity spot check + a live 2-attorney co-editing session (VG-8); the VG-7 proof moat (entity → DPA → SOC 2 → named attorneys); the Windows spot check; one real-card purchase; the vendor-access signatures above. **Nothing else in the vision is autonomously buildable and unbuilt.**

> **UPDATE 2026-06-12: Wave 3b (encrypted vault, VG-6d-v2) is DONE — all 17 tasks, pushed (HEAD==origin==e27b02d).** New `keepance-vault` Rust crate (KPV1 AES-256-GCM format, atomic crash-safe write, BIP39 recovery, metadata, resumable walk) + Tauri command layer (keychain VMK, recovery-unlock, escrow, migration, escape-hatch) + TS `VaultFSBackend` decorator + RAG indexer decrypt seam + enable/ceremony/locked/escape-hatch UI (light theme, i18n). Destructive-failure suite written first; Opus reviewed every diff; a real data-loss bug (encrypt-all descending into `.keepance/` internal stores) was caught in review and fixed. Suites green (crate 27+4, Rust 434, client 2977, tsc/eslint clean). Details in the gap-closure plan's VG-6d-v2 STATUS line + `docs/superpowers/specs/2026-06-11-encrypted-workspace-vault-design.md`. **VG-6 is fully closed.**
>
> **NEXT = Wave 4: VG-8 live multi-user .docx co-editing** — the largest single build in the plan, **xhigh**. The firm-sync spike already settled the design (GO, 5/5 convergence on the document-tree-as-CRDT / Yjs model; the ship gate is overridden — see `spikes/firm-sync/DECISION.md` + the gap-closure plan VG-8). The E2EE relay, per-matter keys, walls, and live shared matter NOTES are already in production; VG-8 extends that stack from notes to the Word document tree. Like the vault, treat it as a careful build: its own implementation plan under `docs/superpowers/plans/`, subagent-driven, Opus reviewing diffs, with the spike's 5 convergence cases + a real-Word fidelity gate + a chaos/offline-reconnect matrix as the verification. After Wave 4, Wave 5 = VG-9 connectors as vendor access lands (externally gated). Do NOT redo Waves 1–3b.

> **UPDATE 2026-06-11: Wave 3a (SSO / VG-6c) is DONE, live-verified, and pushed** (`HEAD == origin/keepance-3.0 == 634e5f8`). 13 tasks, subagent-driven with two-stage review each, plan at `docs/superpowers/plans/2026-06-11-wave3a-sso-oidc.md`, live-verified against a real Dex OIDC provider (`docs/quality/2026-06-11-wave3a-sso/RESULTS.md`), firm-tier "SSO" claim honestly re-instated (committed, NOT deployed). Details in the gap-closure plan's VG-6c STATUS line. **Suites green: backend 183, client 2907, rust 7, tsc clean.**
>
> **Wave 3b (encrypted vault, VG-6d-v2) is IN PROGRESS — the cryptographic foundation is DONE; integration + UI remain.**
>
> Design approved + spec written: `docs/superpowers/specs/2026-06-11-encrypted-workspace-vault-design.md`. Implementation plan (17 tasks): `docs/superpowers/plans/2026-06-11-wave3b-encrypted-vault.md`.
>
> **DONE + reviewed + green (V-T1..V-T8 of 17), all pushed (HEAD==origin==0890232):** the pure `src-tauri/crates/keepance-vault` crate — KPV1 file format (AES-256-GCM, magic-header), crash-safe atomic write (temp+fsync+rename, kill-mid-write proven), BIP39 24-word recovery + HKDF KEK + VMK wrap, verifier + `.keepance-vault.json` metadata, resumable encrypt-all/decrypt-all walk (escape-hatch + migration-resume proven) — **26 crate tests, all spec §13 destructive items 1–9 green**; PLUS the Tauri command layer `commands/vault/mod.rs` (vault_create/status/read_file/write_file + keychain VMK storage + path-traversal/symlink guard) — 13 tests, security-reviewed (OsRng, zeroize incl. heap intermediates, traversal + symlink-escape tests). Opus reviewed every diff.
>
> **REMAINING (V-T9..V-T17):** V-T9 recovery-unlock + escrow export/set commands · V-T10 encrypt_all/decrypt_all/disable commands + progress events · V-T11 TS vault client + escrow provisioning via `keyWrap.ts` (escrow round-trip test) · V-T12 `VaultFSBackend` decorator + `BackendFactory` wiring · V-T13 RAG-indexer decrypt seam (CRITICAL — search breaks without it) · V-T14 enable flow + mandatory recovery-phrase ceremony · V-T15 locked prompt + escape-hatch dialog · V-T16 Data Map row + vault i18n (en/de/es, NO em dashes) · V-T17 full verification + changelog. Each spelled out task-by-task in the plan; continue subagent-driven, Opus reviewing every diff.
>
> **⚠️ CARRY-FORWARD WATCH-ITEM for V-T10 + V-T13:** `keepance_vault::vault::encrypt_all` walks the WHOLE workspace root and currently skips only `.keepance-vault.json` + `.kpv-tmp-*`. Before wiring the migration command (V-T10) and the indexer (V-T13), **verify what Keepance-internal files (if any) live INSIDE the workspace folder** (e.g. a vector-store sqlite, version-history sidecars). Anything read OUTSIDE the `VaultFSBackend` (i.e. directly, like the RAG store opening its sqlite) MUST be added to the walk's skip list, or migration will encrypt a file that another subsystem reads raw and break it. Files read THROUGH `WorkspaceService` (editor, version history) are fine — they go through the vault layer. Most Keepance internal state is in the app-data dir (outside the workspace), but confirm before shipping the migration.
>
> Per `CLAUDE.md`'s model policy the vault stays **xhigh**. Reuse `src/modules/firm/keyWrap.ts` for escrow (V-T11) and the keychain helper pattern in `commands/vault/mod.rs` for V-T9/V-T13.

- **Wave 1** (wedge proof + F-501..F-509 fix wave): done, verified. Do NOT redo.
- **Wave 2** (ingest everything): **done, verified, this session.** All 14 tasks. The native leg-3 re-run is banked under `docs/quality/2026-06-11-wedge-proof/wave2-rerun/`; verdicts in RESULTS §F; strategy STATUS ticks on VG-2/2b/3c/3d/4c/6b/6e. The Task-12 letterhead review found and fixed a real fidelity bug (commit 560d82e) — the merge now reconciles generated content's hyperlinks/media/numbering into the template package.
- **Pitch deck + token-budget setup**: done earlier. Do NOT redo.

## The working rhythm that has held (keep it)
Subagent per task → independent spec review → independent quality review → fix round → re-review; tripwire/unit tests as done-signals; the orchestrator applies small review-found fixes directly and commits/pushes. Reviews have caught real bugs at nearly every step (this session: the letterhead relationship-reconciliation bug). Keep that bar.

## NEXT: Wave 3 — the firm-sale wave (per the gap-closure plan §3 VG-6c + VG-6d-v2)

Wave 3 has two committed builds. Each needs its OWN implementation plan written at build time (same as Wave 1/2), under `docs/superpowers/plans/`.

### Wave 3a — SSO (VG-6c). Size: L. Model: Opus 4.8 · high; Sonnet 4.6 subagents.
OIDC authorization-code flow against the firm backend. **Entra ID first** (law firms live on M365), Google Workspace second, generic OIDC third; **SAML explicitly out of v1**. Per-org IdP config in the firm admin console; member sign-in through the system browser, exchanged into the existing **Ed25519 seat-token** session; walls/seat semantics unchanged. Verify against a self-hosted test IdP (**Authentik on this server**) plus a real Entra tenant when available. **The site/pricing re-claim "SSO" ONLY in the release that ships it** (the honesty rule — the false SSO claim is why we're here; it was removed in commit c0454da). Reference the firm backend auth: `backend/src/` (seat tokens, `/.well-known/seat-pubkey`), client `src/modules/firm/contract.ts` `FIRM_ENDPOINTS`.

### Wave 3b — encrypted workspace vault (VG-6d-v2). Size: L, data-loss-sensitive. Model: Opus 4.8 · **xhigh**; Sonnet subagents, Opus reviews every diff.
**The plan REQUIRES a brainstormed design doc + implementation plan + a destructive-failure test suite BEFORE any code ships.** Do not skip this. Use `superpowers:brainstorming` first. Scope (from the plan): optional per-workspace encrypted vault; document files encrypted at rest (AES-256-GCM, the same primitives the mail store + `chunk_text` already use); master key in the OS keychain; an explicit recovery phrase generated at vault creation with a "Keepance cannot recover this for you" ceremony; firm-tier admin escrow reusing the existing ECDH escrow machinery; transparent open/edit through the app; a decrypt-everything escape hatch so "your files are always yours" stays true. Destructive-failure tests: kill the app mid-write, wrong key, lost keychain. (v1 — unmissable disk-encryption guidance — already shipped in Wave 1, VG-6d-v1.)

> Note: VG-6d-v2 + VG-6c are "functional to sell" / demand-gated in the plan, but the board mandate is 100%/zero-exceptions, so they are committed builds.

## Then: Wave 4 (VG-8 live multi-user .docx co-editing — the largest single build; xhigh) and Wave 5 (VG-9 connectors: Clio → Office add-ins → NetDocuments → iManage, as vendor access lands). Wave 5 is externally gated on the vendor-access track.

## Open / needs Jameson (not blocking the build)
- **WAVE2-FU-02** (in `BACKLOG.md`): add an in-app guard to `handleCreateOrg` so the firm backend doesn't rely on the Caddy `/admin/*`→403 edge rule alone (the admin route was internet-reachable + unauthenticated; closed at the edge this prior session, RUNBOOK §K). Keep `/webhooks/lemonsqueezy` (HMAC) + `/org/claim` (key-as-secret) public; the Assured exercise uses the loopback admin route so it must keep working.
- **A valid Anthropic API key** in `~/.local/share/jameworld/keepance-assured-test.env` to finish the ONE remaining Assured sub-step (a clean 200 + completion). Every Anthropic key on the server is revoked/stale (the local-model gateway made them cold) — worth a broader look; may affect other server tools that call Anthropic directly.
- **The proof moat (VG-7, Jameson-only):** formed legal entity → executed DPA → SOC 2 → named-attorney references; the ~5-min Windows spot check; one real-card test purchase; one attorney reviewing a contradiction-finder `.docx`. None block the build.
- **Vendor access (VG-9, for Wave 5):** `docs/operations/2026-06-10-vendor-access-track.md` — Clio / NetDocuments / iManage all filed, awaiting their humans; replies go to `developers@keepance.com` (read via the `outlook` CLI).
- **Wave 2 carried, non-blocking:** native OCR + native letterhead-deliverable exercises (deterministically test-covered; carry to a future attended pass / the Windows spot check); matter-scoped finder native run; RESULTS §F residuals + §E (a)-(e).

## Native harness reminder (for any future leg-3 pass)
`scripts/wedge-proof-native.sh` (preflight/up/launch/seed-localstorage/shot/click/type/assert/down). Check `free -h` FIRST (the box is memory-tight). The seeded workspace indexes **14 files** now. **The in-app editor autosaves every ~2 s** — if you mis-target the chat input and type into an open document, undo + restore the workspace copy from `tests/fixtures/matter-corpus/` immediately (committed fixtures are never at risk, but a contaminated `/tmp/wedge-ws` copy skews a re-index; happened once this session, caught + reverted).
