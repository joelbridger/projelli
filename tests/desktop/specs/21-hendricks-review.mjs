// This is intentionally a syntax-only server-lane placeholder. The later
// desktop lane drives the real app and must not be inferred from this file.
export const hendricksReviewDesktopJourney = Object.freeze([
  'open-built-in-hendricks', 'review-task-and-crm-proposals', 'approve', 'verify-local-results',
]);
