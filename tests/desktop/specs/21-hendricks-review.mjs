// Syntax-only script for the later Windows drive. This lane makes no desktop claim.
export const hendricksReviewDrive = Object.freeze({
  client: 'The Hendricks Household', proposals: 2,
  destinations: ['local-task', 'local-encrypted-crm'],
});
