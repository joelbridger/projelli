{
  "name": "keepance-firm-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Keepance 3.0 firm platform backend — identity/auth + per-org licensing + E2EE sync relay + assured zero-retention inference proxy (3 of 3).",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "keygen": "bun run src/lib/keygen.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "dependencies": {}
}
