# WB-138 data export/backup strict-fix result

**Base SHA:** `8118b12cca5f05892e1418c254818268795694e8`

**Final verified source SHA:** `6aa2e10ad`

**Shared receipt:** `src/features/data-portability/evidence/receipt.md`

## Result

PASS. All three CHANGES-3 blockers are cured:

- the Data export panel is hidden by the new default-off
  `data-export-backup` flag and appears only when that flag is enabled;
- every sentence now matches the provider-neutral native selection rule; and
- success now requires reading the written file and proving its checksum,
  manifest counts, record shape, and fidelity coverage. Any archived source
  type missing from the fidelity matrix fails closed to **needs review**.

The native writer and all other fenced seams were untouched.

## Test truth boundary

The success path parses a committed fixture generated by the exact native
writer. The fixture includes records originating from two providers, while the
archive itself contains only the writer's provider-neutral source fields.

The unit test mocks Tauri IPC and the desktop filesystem read. It proves the
real writer-shaped parser and claims boundary, not a fresh native disk write or
the IPC bridge. This is the explicitly allowed minimum proof for the UI-only
fix. A separate regression assertion freezes all three content claims and the
complete success result so the UI cannot quietly add contents the manifest did
not prove.

## Checks at `6aa2e10ad`

```text
$ npx vitest run src/features/data-portability/DataExportBackupSettings.test.tsx src/features/settings/registry/settingsModuleRegistry.test.ts tests/unit/architecture-boundaries.test.ts tests/unit/i18n/en-json-snapshot.test.ts
Test Files  4 passed (4)
Tests       28 passed (28)
```

```text
$ npm run typecheck
> tsc --noEmit
PASS (exit 0)

$ npm run typecheck:tests
> tsc -p tsconfig.test.json --noEmit
PASS (exit 0)

$ npm run boundaries:check
✅ No feature-boundary regression (599 current baseline finding(s)).
```

## Scope attestation

The fix changed only the feature-owned archive UI/parser/copy/tests/fixture,
its one Settings descriptor flag field, its focused registry assertions, and
exactly one appended flag-registry line. No native source file changed.

WORKER-DONE: v1/data-export-backup ready for strict re-review
