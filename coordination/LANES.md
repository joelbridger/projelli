# LANTERN-PLUS LANES — live ownership ledger (coordinator-maintained)

**Purpose:** the O(1) answer to "what is every Lantern-Plus lane doing right now?" Updated on every spawn/merge/close (mirror of main line's `~/keepance-coordination/LANES.md` convention; the fleet dashboard reads both).

Last updated: 2026-07-04 ~12:15 (coordinator-7 — 6 merges landed; meetpersist+webguard+qa19fix+qa4 active; Legion sidecar staged; divergence report landed)

## Active lanes
| Worker session | What | State |
|---|---|---|
| cc-lantern-noticecard | 🎯 THE NOTICE CARD v1+v2 (lp/notice-card; ~/lp-noticecard; OPUS 4.8): local notice participant — calendar join-URL, Teams+Zoom adapters in isolated companion webview, lifecycle supervisor, canvas-camera visual card, policy hook + 2 quick wins (brief w-noticecard-brief.md; design docs/strategy/2026-07-04-notice-participant-design.md) | building |
| cc-lantern-qafix6 | QA fix batch 6 (lp/qa-fix-batch6; ~/lp-qafix6): QA-33 keychain-service outage → fast honest failure + degraded open; QA-32 dialog-never-opens investigation + bounded watchdog + manual-path fallback (brief w-qafix6-brief.md) | building |
| cc-lantern-meetverify5 | 🏁 THE final fresh no-fixtures Meetings run (brief w-meetverify5-brief.md; ~/lp-bench) at tip @e4b0f30d with ALL FOUR blockers fixed. Notes land ⇒ Meetings DONE + Jameson report. Owns the Legion | verifying |

## Merged this session
✅ lp/qa-fix-batch5 @9e87a8c2 — 🚨 QA-34 P0 KILLED (Opus, 2h14m + 6 self-review rounds): proven mechanism = DocxEditor saves bypassed ALL save-integrity guards (dirty-dot/Saved/close-checks blind to .docx reality); now truthful state + exponential-backoff retry + persistent warning + "Save a copy elsewhere" rescue + airtight close on every path + Rust atomic temp cleanup. + QA-36 reserved names rejected at create (client + Rust). My codex caught 2 more P2s (StrictMode retry-disable; rescue-copy missing newest keystrokes) — fixed w/ tests. 2 residual ms-window races gate-adjudicated as follow-ups (task #24). Gate tsc+tct+i18n + 6098 vitest + 1544 cargo bare-0
✅ lp/recording-notice-kit @0c6a8488 — 🎯 THE RECORDING NOTICE KIT (Jameson-approved, Opus lane, 2h11m): verified verbal notice (localized fuzzy transcript matcher w/ hard table-driven suite; ledger entry w/ audio timestamp + snippet; needs-review one-click resolutions), invite disclosure (clipboard fallback — calendar write needs an OAuth-scope go-ahead, follow-up filed), one-click chat notice, Standard/Strict policy dial (Strict=quarantine, never destroys; state-law recommendation line). My codex caught a P2 (cross-meeting notice staleness — wrong-meeting compliance display; fixed w/ test). Gate tsc+tct+i18n-0 + 6045 vitest bare-0 (TS-only)
✅ lp/notes-read-fix @e4b0f30d — QA-41 FIXED: Windows verbatim path (\\?\) reached JS and broke the transcript read; fixed at 3 layers (Rust display_path at every IPC path serialization + PathValidator strips prefixes + exists()-first in tryGenerateNotes so silent-swallow is impossible — every failure now classifies into the QA-31 honest-error/retry machinery). My codex caught a P2 (orphan-filter form mismatch could list a LIVE recording as recoverable — fixed w/ test). Gate tsc+tct+i18n + 5950 vitest + 1540 cargo bare-0
🏁 meetverify4 lane DONE (evidence @674846ae, 19 shots) — fresh-session verdict: TRANSCRIPTION now PASSES end-to-end (word-for-word accurate real transcript, survives restart). QA-41 CONFIRMED real (clean session rules out noise): notes stuck forever, zero provider requests, code-traced to a silently-swallowed transcript.json read in tryGenerateNotes that the QA-31 watchdog never guards → notesfix2 lane opened. Legion quiet
✅ lp/transcript-hang @b9dc02ca — QA-40 FIXED, two real bugs: (1) transcription failures were swallowed silently (a 0.1s failure looked identical to an infinite hang) → honest classified error + retry; root cause was a model staged in the wrong folder + the swallow; the existing 30s wedge-watchdog was proven fine; (2) Windows-only: the app held the temp WAV open while the engine read it → silent EMPTY transcripts (found ONLY by real-machine testing) → handle released before engine read. Live-proven: 3 real recordings → correct transcripts. Gate tsc + tct-0 + i18n-0 + 5941 vitest + 1534 cargo bare-0; codex clean. transfix ALSO flagged notes-step non-completion in its session (unconfirmed) → meetverify4 fresh run decides
✅ lp/qa-persona-d2 @8be80e76 — qa5 persona-D desktop round (bench-2, VB-CABLE): QA-32..36 filed w/ 52 screenshots. 🚨 QA-34 P0: a single failed autosave write (file lock, e.g. antivirus) permanently stops persistence for that doc while the UI shows "Saved" — verified total silent data loss across restart → Opus fix lane OPEN. Also QA-32 folder picker never opens (fresh VM), QA-33 stopped VaultSvc = silent 30s workspace-open fail, QA-35 recording timer runs at zero disk, QA-36 CON.docx creatable-on-NTFS trap. Held well: no crashes/corruption from deep paths, clock skew, dual launch, full disk. VM deallocated
🏁 meetverify3 lane DONE (evidence @bb8cf454) — QA-31 notes fix VERIFIED live on the Legion: honest classified failure on a real cut connection, real retry, NOTES LANDED (05-notes-landed-MONEY-SHOT.jpeg); restart persistence re-confirmed. FOUND QA-40 P1: transcript generation hangs on real recordings post-rebuild (sys channel never starts, 2/2) — suspected staged-shim vs new-voicefix-contract mismatch → transfix lane opened. Worker honestly disclosed its seeded-transcript workaround. Meetings DONE call still gated on QA-40
✅ lp/web-locks-guard @0aed9c4e — TabWriteGuard now on native Web Locks (browser-serialized; heartbeat kept as fallback + rollout bridge both directions so old/new bundles can't co-own; dirty-frozen tab refuses lock release). NINE total review rounds across the two lanes closed real races each time — vindicates the migration. Gate tsc + typecheck:tests-0 + i18n-0 + 5932 vitest bare-0; live 2-tab Playwright 6/6
✅ lp/cleanup-batch3 @968670ba — audit-action-type gaps closed via call-site tracing (integrity_reseal + redtail connect/sync/disconnect + salesforce connect_cancelled added to the TS union + label/icon maps; unreachable combos correctly excluded) + deterministic docx zip timestamps (fixed DOS-epoch mtime in the writer — kills the time-flaky roundtrip at the root; Word reads part XML, never zip mtimes). Gate tsc + typecheck:tests-0 + 5897 vitest + 1533 cargo bare-0; codex clean
✅ lp/meeting-notes-hang @b3e2efc8 — QA-31 P1 FIXED: root cause = un-bounded single provider.sendMessage await (a stall also blocked processingCount cleanup → eternal spinner, all errors swallowed). Now: 120s progress watchdog (reject-before-abort ordering proven by test), classified honest errors (gate-blocked/timeout/error) persisted to meeting.json, working retry serialized per-meeting, binary-safe docx detection (my codex P2). Worker 2 codex rounds + my independent pass all real findings fixed. Gate tsc + typecheck:tests-0 + i18n-0 + 5889 vitest bare-0
✅ lp/cleanup-batch2 @6f619ff9 — typecheck:tests 9→0 (incl merge-introduced errors from 3 other lanes) + useEntityLabel through i18n (50 entity-label keys ×5 professions; primary surfaces properly localized w/ German live-verify; 11 still-English files get an explicit useEntityLabelEnglish accessor — NO mixed-language sentences; my codex caught the mixed-string P2 + a plural-grammar P3, both fixed). Gate tsc + typecheck:tests-0 + i18n-0 + 5869 vitest bare-0
✅ lp/voice-sidecar-contract @aa0ab3eb — voice sidecar FIXED to speak real whisper.cpp CLI (verified against a real engine on real audio, twice) + CI now stages a portable static engine w/ pinned models + HARD portability guards (my codex caught a release-class P1: runner-native -march=native build + libgomp dep — fixed + guarded); voice no longer ships disabled in installers. Gate tsc + 5861 vitest + cargo workspace (1 time-flake root-caused to ZIP DOS-timestamp boundary, passes isolated — flake-fix task filed)
🏁 meetverify2 lane DONE (evidence @53319877 on lp/windows-smoke-evidence) — QA-30 VERIFIED FIXED live (survives restart + hard kill); REAL Teams (3:48) + REAL Zoom (1:29) two-party captures PASS end-to-end: consent, zero egress, ACCURATE transcripts, audio-seek, human titles. Zoom capture mechanism CONFIRMED (Teams already was). FOUND QA-31 P1: AI notes never finish on either platform (working key ruled out setup) → notesfix lane opened. Speaker labels inconclusive (one-laptop test-rig bleed, honestly flagged). QA-10 time-boxed out. Legion quiet
✅ lp/qa-fix-batch4 @9dd18f06 — QA-24 P1 duplicate-client isolation breach FIXED at both layers (idempotent submit + store-level uniqueness re-check) + QA-25 honest Ask-cancel on navigation (persisted record, never silent; my codex caught a P1 timing hole — cancel during provider resolution could still send+persist — fixed w/ red-first test) + QA-26 already-consistent, locked by regression test. Gate tsc + i18n-0 + 5861 vitest bare-0 (TS-only)
✅ lp/browser-tab-guard @4f26f151 — QA-15 P1 browser two-tab silent data loss KILLED (single-writer gate: blocked tab never mounts AppShell; flush-and-ack handshake w/ per-request nonce; forced rehydrate on auto-reclaim; demoted stale owner never late-flushes) + QA-13 honest desktop-only message. SEVEN review rounds (worker 5 + my 2 independent passes) each found+closed a REAL race — Web Locks migration ticket raised to next-cleanup priority. Gate tsc + 5849 vitest + i18n-0 bare-0
✅ lp/meetings-persist @cb1181c9 — QA-30 P1 FIXED: root cause was a bare catch{return []} making ANY transient scan failure identical to "no meetings"; now exists() fast-path + 3x retry + honest "Couldn't load meetings"+retry state + needs-review surfacing for orphaned entries; my codex caught a P2 (folder-less clients got a false error — guarded). Windows-shaped restart-simulation test. Gate tsc + 5818 vitest bare-0 (TS-only)
✅ lp/live-index @70bc4cc6 — QA-19 P1 FIXED: live indexing machinery existed but a silent bare-catch killed watcher installation for the whole session; essential wiring now isolated + retried BEFORE optional connectors, watcher-triggered reindex retries w/ cancellable scheduler, cancellation no longer falls through to stale-workspace connector setup (my codex caught that P2 + worker found a 2nd instance). Gate tsc + 5807 vitest bare-0 (TS-only)
✅ lp/qa-persona-c @82aea1a3 — persona-C klutz sweep (bench-1, current tip): QA-24 P1 duplicate-client isolation breach (triple-click → shared folder), QA-25 P2 silent Ask discard on client switch, QA-26 P3 inconsistent new-doc buttons; QA-20 re-verified FIXED on real Windows; multi-click safe everywhere else; force-kill mid-typing survives. Fix lane qafix4 OPENED
✅ lp/qa-fix-batch3 @d272c836 — QA-20 CLOSED (orphan already fixed @e5f0207c + silent-half by UX polish; added the missing mic-specific Rust+frontend regression tests) + QA-22 tooltip location fix w/ de/es backfill. Gate tsc + 5791 vitest + 1530 cargo (workspace, warm lp-gate target) all bare-0; both codex passes clean
🏁 sidecar lane DONE (evidence @cb95391f on lp/windows-smoke-evidence) — REAL whisper.cpp built+staged on the Legion; "Voice ready"; end-to-end transcription verified through the app's own transcribe_audio (real speech correctly transcribed). FOUND: the app's sidecar arg contract was never validated vs a real engine + CI fetch is a no-op → voice ships disabled in installers (product task filed). bench-1 still unstaged
🏁 meetverify lane DONE (evidence on lp/windows-smoke-evidence @docs/evidence/meetings-verify-20260704/): real 3.5-min two-party Teams call recorded on the Legion — consent dialog PASS, zero-egress PASS (live connection monitoring), files-safe PASS. FOUND QA-30 P1: ALL meetings vanish from the tab after app restart (disk intact) → fix lane meetpersist opened. Transcript/notes BLOCKED: sidecar binary never staged on Legion (same gap on bench-1) → task. Meetings NOT re-declared done; Legion free + quiet
✅ lp/i18n-fixes @31da41f6 — i18n:check 18→0 (dynamic-key t() calls made literal across 13 files) + QA-14 P1 fixed (Spine nav / MatterHub tabs / MattersHome actions now translate; +~36 placeholder de/es values corrected; verified live in German w/ screenshots). Worker codex clean + my independent codex clean. Gate tsc + i18n-0 + 5790 vitest bare-0. Flagged follow-ups: useEntityLabel English nouns (task), 198 suppressed hardcoded-string markers (known debt)
✅ lp/qa-persona-b @c3a00557+62691c6f — persona-B daily-driver sweep (bench-1, current-tip rebuild): 5 findings renumbered QA-19..23 (ID collision with qa3 resolved by hand — and a committed-conflict-marker slip caught+fixed next commit). Top: QA-19 P1 Ask can't see new content until restart (fix lane OPEN); QA-20 silent no-mic recording failure (likely part-fixed by w3ux polish, re-verify); QA-5/6 fixes CONFIRMED holding under real use. VM deallocated clean
✅ lp/wave3-meetings-ux @582a32fe — 🎨 Meetings-tab SENIOR-UX gate (Fable): 7 blockers + 11 should-fixes implemented (recording-pill truth + local-privacy reassurance, human meeting titles, [t:ms]→"(at m:ss)" on BOTH note paths via one shared helper, slim scrubber, inline start-failure errors, confirmed deletion, processingCount vs boolean race). Findings doc docs/design/2026-07-04-meetings-tab-ux-review.md + 15 before/after shots. Worker codex P2 + my independent codex 2 P2s all confirmed+fixed pre-merge. Gate: tsc + 5769 vitest bare-0 (TS-only, cargo skipped)
✅ lp/qa-fix-batch2 @aca9bb81 — QA-7 Ask stall watchdog (12s warning + 45s honest timeout, re-arms per chunk, covers mid-answer stalls per my codex P2 routing) + QA-8/9 onboarding overlaps + hardened spec. Verified by ME twice with fresh-server bare-exit-0 (worker's early false-fails were the shared-:5173 stale-server trap — see port rule below). Gate: tsc + 5790 vitest bare-0 + spec 4/4 on merged state
✅ lp/qa-persona-d @18824822 — QA persona-D edge-case sweep (browser build): QA-13..18 filed incl 2 P1s (QA-14 language switch skips core nav — root-caused to literal labels; QA-15 two-browser-tabs silent last-write-wins data loss). XSS held everywhere; QA-5 fix confirmed still live; 500 clients perform fine. Lane closed clean
✅ zoomacct lane DONE — Zoom free account created + login/host verified (Sarah Morgan; creds chmod-600 in demo-creds/zoom-account.md). Google Meet account BLOCKED at Google's QR/phone bot-check — correctly stopped per guardrail; one Jameson tap needed (Q#9). Session closed
✅ lp/wave3-meetings-ui — 🎬 Wave-3 MEETINGS SURFACE: record pill, per-client Meetings tab (between Email & Activity), MeetingEntry (notes+transcript+audio-seek), needs-review queue, consent dialog+ledger, dictation filing. Own codex found+fixed 3 bugs; rebase caught a 4th (stopRecording overwriting Rust's authoritative meeting metadata). Gate 5755 vitest + tsc + playwright meetings 2/2
✅ lp/qa-fix-batch1 @b0e07298 — QA-5 new clients get a scoped folder (restores isolation on first action) + QA-6 Ask responsive layout (composer never collapses); independent codex clean; 25 targeted + 5706 vitest
✅ lp/wave3-transcription @9661f9be — Wave-3 local transcription (Tasks 7-9): windowed queue over the sidecar, transcribe_meeting + shared types, battery-saver + import; independent review caught a COMPLIANCE P1 (fabricated one-party consent when meeting.json absent) + OOM-on-long-meetings P2 — both fixed robustly (persist-consent-before-finalize-delete + abort-on-write-failure; streaming windows ~1.6MB vs 1.8GB); gate 1243 cargo + 5696 vitest
✅ lp/crm-card-visibility — QA-1..4: persist CRM proposals across restart (P1), hub-wide pending banner + Review-now (P2), honest copy (P3), honest wave2 bench check (+ coordinator-review P2: navigate to Overview before asserting card). 86 touched tests green
✅ lp/cleanup-batch1 @020b5d5f — dead-dir + cosmetic keepance→lantern sweep; caught+fixed a CUSTOMER-FACING brand leak (docx author "Lantern AI"→"Advisor Prep Hero AI"); auto-smoke task-name; docs currency (independent review found the brand leak)
✅ lp/diarize-release-staging — sidecar+models staged in release.yml; FOUND+FIXED an onnxruntime lib collision that would corrupt piper in releases; local .deb verified; mac/win notarization still needs real CI
✅ qa1 evidence @1b45d8a0 — persona-A first-run: 12 findings incl 4 real P1s (new-client folders, Ask-input-collapse, AI-hang-no-feedback, onboarding overlaps)
✅ realcall evidence @13931b64 — REAL Teams call recorded; far-side (system) audio strong+continuous across 2 recordings; near-side quiet (test-rig mic isolation, not an app bug) — Teams recording CONFIRMED
✅ lp/rename-ref-migration @e88aa715 (fork) + keepance-3.0 (main repo) — /home/jameson/keepance→/lantern path hygiene in 7 scripts/docs; frozen names left; report filed
🏁 SCORECARD after cold-boot confirm: 12 PASS · 0 FAIL · 2 SETUP-BLOCKED · 5 stubs. ⚠️ CORRECTION (Jameson caught it): the 5 stubs = the UNBUILT Wave-3 UI (Meetings tab/record pill/transcription) — feature-complete was over-claimed; engine done, surface in build now (w3b+w3c lanes)
✅ lp/bench-harness-clients-tab-fix @02d153b5 — Clients-tab normalization + note-collision + wave1 modal-close (126+ harness tests)
✅ lp/bench-harness-followup-fixes @64df925e — overlay-dismiss prefers Close (native-dialog trap killed) + wave1 nav
✅ lp/pathguard-windows-verbatim @<post-pathfix> — CONFIRMED Windows verbatim-path bug in the absolute walk (red-on-Windows proof, green after) + 2 pre-existing Windows caller bugs (.. defense layer, live-recording orphan mismatch); pathguard 10/10 + capture 26/26 ON REAL WINDOWS; gate 1233 cargo + vitest green
✅ lp/audit-chain-failclosed @5af15e5e — fail-closed tamper evidence (seal-missing vs altered vs full-wipe via high-water mark) + acknowledged repair UI (inherited #14; Opus xhigh; 4 codex rounds + coordinator independent review; gate 1227 cargo + 5681 vitest)
✅ lp/wave4-gap-sync @85c4a633 — unresolved gap wins the Client Map initial tab (bench FAIL #2 fixed; red-first tests + first mirror-caught regression spec; 5673 vitest + 16/16 mirror)
✅ vaudio spike DONE @841f4ab0 — virtual audio VIABLE on cloud benches (VB-CABLE, tone proven at engine level); flagged a possible Windows-only capture_start pathguard bug → pathfix lane
✅ lp/bench-harness-round2-fixes @00558e1d — 3 live-root-caused harness bugs (wrapper-click, Grid-view state, snapshot false-negative); 124 harness tests
✅ azclone lane DONE @0bab65b7 — merged-tip verified on cloud VM (CDP fix live); bench-ready golden snapshot; clone bench-2 zero-rebuild; FIRST live 2-way sharded smoke PASS (~$0.20)
✅ lp/e2e-smoke-mirror @fc82c2a2 — Playwright mirror: 11/19 bench checks in ~15s on Linux (44 specs green post-merge)
✅ lp/azure-cdp-fix @395923fd — wry CDP env-var fix (root-caused real app bug; cloud bench is now a working 2nd smoke target)
✅ lp/meeting-capture — 🏁 WAVE 3, the LAST feature lane (device-verified on real hardware pre-merge; 17 worker rounds + coordinator independent review w/ 1 confirmed-fixed P2; gate 1218 cargo + bins + 5670 vitest + tsc)
✅ lp/symlink-hardening — pathguard module + 5 containment sites hardened (6 worker codex rounds all-real-findings + coordinator manual xhigh + independent codex CLEAN; gate 1187+51 cargo, 5670 vitest, tsc)
✅ harness-v3 @07197316 — sharded multi-target smoke + failure forensics + auto-smoke dry-run (Codex-built, coordinator-reviewed; 122 harness tests)
✅ harness-typing-fix @689f1556 — type-stdin over SSH + readback verification (kills silent typing truncation; Codex-built, coordinator-reviewed)
✅ lp/retention @3d557f20 — Wave-4 Track D (16+1 review rounds; full-branch codex caught a P1 symlink isolation breach, fixed; gate 5670 vitest + 1173 cargo)
✅ lp/harness-round2 @05feb125 — nav helpers + Wave-4 B/C checks (live shakedown pending Legion)
✅ lp/diarization @b302312c — Wave-4 Track A: diarize sidecar + encrypted voiceprints + naming UI (6 self-review rounds + coordinator xhigh + codex; gate 5634 vitest + 1133 cargo)
✅ lp/clientmap-errors @2cfe2224 — Client Map error classification (index vs provider vs unknown; 24 tests; gate 5632 vitest)
✅ lp/bench-smoke-harness — scripted bench smoke (17 checks, live-validated 6/8 on the Legion, safe-by-default; gate 5608 vitest, codex finding fixed w/ red-green proof)
✅ lp/azure-bench-fix @44b2faf8 — cloud VM now compiles+launches the full app (MSVC root-caused: cmd.exe %ERRORLEVEL% parse bug + corrupt VS Installer); fresh snapshot -clean-2; VM deallocated; CDP follow-up queued
✅ fix/vs-jump-page @5207beac (MAIN repo) — false HIPAA claim + stale facts fixed on live comparison + press-kit pages; Jameson-approved; DEPLOYED + live-verified
✅ lp/crm-wire-fixes @b3bca9a0 — Wealthbox due-date validation + write-direction fix + readback verification (gate 5608 vitest + 1108 cargo)
✅ lp/jump-strategy @e0655318 — Jump battle plan (8 sections + sources; published to claudereports)
✅ bench-prep lane DONE @617d60ea — Legion pre-warmed to current tip, health-verified, quiet (BENCH-READY.md)

## Legion sequence (one driver at a time)
✅ bench-prep → ✅ harness live-validation → 🔒 w3 Task 6 device verification (OWNS Legion now) → harness2 live validation → full Wave-3/4 scripted bench pass

## Serialization invariants
One merge in flight · one cargo per lane cache (per-lane CARGO_TARGET_DIRs on /mnt/devcache) · one Legion driver · coordinator merges only.

## Dev-server port rule (added 2026-07-04 after two false E2E verdicts)
Playwright's `reuseExistingServer:!CI` grabs ANY vite on the expected port — a lane's Playwright run against another lane's stale server on :5173 produces false passes AND false failures (bit qafix2 twice tonight). Rule: **every lane's dev server uses its own unique port (`--port NNNNN --strictPort`), never bare :5173**, and E2E verification pins `E2E_BASE_URL` to a server you started yourself from the worktree under test. Put this in every brief that runs vite/Playwright.
