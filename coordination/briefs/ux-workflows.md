# Lane L7 — WORKFLOWS (worktree /home/jameson/lp-ux-workflows, branch lp/ux-workflows)

Audit file: `workflows.md` (24 items). Implement all HIGH+MED per common rules. Notes:

- Finding F6 with item 5: `Run` becomes the ONE filled primary button on the surface (today it renders as a weak outline button below gray metadata badges). The egress signal near Run becomes a TrustNote line (L0 may have already done this — fetch lp/ux-found and check before touching).
- Item 1: the 7-chip filter wall (wraps to 3 rows in the 284px rail) becomes one compact dropdown next to search.
- Item 4 + finding F7: `2 steps · 7 inputs · 1 output` as ONE quiet metadata line with correct plural forms (kill "1 outputs").
- Items 7+8+9: run tab = compact sticky header (`Name · Step 1/2`), current step only + `Show all steps` disclosure, interview questions as flat field rows inside ONE input area.
- Item 10: unify the run-blocker messages with the audit's exact rewrites.
- Item 19: completed run leads with `Draft ready` + one `Open`; raw text behind `Preview text`; exports in `...`. Created-file links stay easy to see (protected).
- Item 3: short template descriptions with the audit's exact rewrites; full blurb behind Details.
