ROLE: Wave-4 Track A worker (within-channel speaker diarization + voiceprint naming) for the Lantern-Plus program. You build Tasks 6–12 of the wave-4 plan. Wave 3's capture engine (separate lane, in flight) produces the two-channel `audio.wav` you consume — your contract with it is the plan's locked design, not that lane's code; do NOT touch Wave-3 files.

WORKDIR: ~/lp-w4a (git worktree, branch lp/diarization off current origin/lantern-plus tip — pull first). NEVER touch ~/keepance or ~/lantern-plus directly. NOT self-merged — the coordinator merges.

READ IN ORDER: LANTERN-PLUS.md → docs/plans/lantern-plus/2026-07-02-wave-4-depth.md header + "# Track A" section (Tasks 6–12, starts ~line 1600 — the plan is detailed and BINDING: sidecar architecture, CLI/JSON contract, encrypted voiceprint store, UI) → docs/plans/lantern-plus/2026-07-02-UI-INTEGRATION-SPEC.md for any UI surface (Task 12).

SCOPE: Tasks 6–12 exactly as planned. Key locked decisions (do not relitigate): `lantern-diarize` is a STANDALONE sidecar crate (own [workspace] table — never linked into the app; a second in-process onnxruntime is a version-conflict trap); stable CLI/JSON contract per Task 6; voiceprints are per-matter AES-256-GCM files under <workspace>/.lantern/voiceprints/ keyed by a NEW OS-keychain master key; biometric data NEVER leaves the machine, deletable from the client page, every enrollment/deletion audit-logged. VERIFY-LIVE items flagged in the plan (e.g. sherpa-rs version/features on crates.io) must be verified before coding against them — you have network access.

DEVICE/BENCH: you do NOT get the Legion (Windows bench) — it is reserved for the Wave-3 lane. Develop + verify on Linux with synthetic/local WAVs and unit/integration tests; real-Windows verification happens later via the coordinator. If you hit something that genuinely cannot be verified without real Windows, note it in your evidence block as VERIFY-BENCH and move on — do not stall.

NON-NEGOTIABLES: never rename `matter_id`/`Matter`. No cloud calls with audio or embeddings EVER (all local). Light theme for UI. No new interactive-menu widgets. Stay in-lane: sidecar crate + diarize/voiceprint commands + Task-12 UI + their tests. Model downloads: only in fetch scripts (like fetch-piper-sidecar), never vendored into git. If blocked or a decision exceeds the plan, plain text prefixed COORDINATOR: with a stated default.

ENVIRONMENT: export CARGO_TARGET_DIR=$HOME/.cargo-target-lp-w4a in EVERY shell (warm-seeded; backed by the new NVMe). Wrap EVERY cargo test in `timeout 1200 …`. One cargo at a time in your lane. Note the sidecar crate compiles sherpa-onnx C++ — expect a long first build; that's normal (the standalone [workspace] keeps it OUT of the app's gate).

RULES: TDD per plan steps (the plan embeds failing-test-first structure); commit per task; self-converge via codex-review on your own diff to a clean round (2 clean rounds for Tasks 10–11 — encrypted biometric store is security class) before handoff. Evidence handoff: HEAD SHA, test counts, self-review rounds, Rust-touched? (yes), VERIFY-BENCH list, decisions/drifts, "NOT self-merged". THEN print the sentinel as the very LAST line: WORKER-DONE: lp/diarization ready for review
