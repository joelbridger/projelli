# Feedback batch 2 — 2026-07-08 3:06 PM file (7 lanes)

**Spec = `/home/jameson/lantern-plus/docs/Lantern UI Feedback july 8 2026 306 pm.md`** — line numbers below refer to it. ALL rules in `ux-common.md` still bind (worktrees now branch from `lp/ux-simplify-v1`; foundation branch to watch is `lp/fb2-railchrome`). His note on line 2 applies: if an item is already true on the branch, note-and-skip.

## Lane RAILCHROME (foundation — merges first) — worktree ~/lp-fb2-railchrome
Line 23 (the big one) + 30 + 20 + 19: ONE standard rail-header pattern for EVERY vertical rail in the app (Ask conversations, Client Map sections, Documents files, Email, Meetings, Workflows, Settings sections): title + search ICON (expand-on-click, C2, collapse-on-empty-blur) + plus icon (creates the surface's primary thing; menu if multiple) + vertical `⋮` menu + collapse control — identical order, size, spacing. Extend/replace `RailShellHeader` in src/ui/kp; adopt it in every surface rail. ALSO: all `MoreHorizontal` menu triggers app-wide become vertical `MoreVertical` (line 30); standardize rail font sizes to one token (line 20 — workflows rail font is visibly bigger); make the Ask/ClientMap/Workflows header row heights equal (line 19). Surfaces' rail CONTENT is other lanes' turf — you own the header chrome + shared component only.

## Lane BRAND (merges LAST) — worktree ~/lp-fb2-brand
Line 4 + Jameson's directive: restore Advisor Prep Hero as the visible product identity AND make it swappable. (1) Restore the original APH logo assets from the merge-base with origin/lantern-plus (`git show <base>:public/logo.svg` etc. + brand/assets) — mascot wordmark returns. (2) Visible product name: route EVERY user-facing occurrence through ONE source — i18n interpolation (e.g. `{{productName}}` default from BRAND config) or the scripts/brand-sync.mjs --rename flow — so a future rebrand = one config change + regenerate. The current copy says "Lantern" in visible text (yesterday's sweep): flip it back to "Advisor Prep Hero" VIA the new single-source mechanism, not by hand-editing 72 strings. (3) Document the swap procedure in brand/README (name, logo, colors — colors are already tokens). Do NOT touch code identifiers, `matter` facade, or non-visible strings.

## Lane CLIENTMAP — worktree ~/lp-fb2-clientmap
Lines 3, 5, 6, 7, 8, 9, 10, 15, 16: All Clients rail row highlights when the clients screen shows (and the Client Map nav tab does NOT) (3). Kill the doubled plus in "+ Add fact" (5). Source pills move to the RIGHT of each fact on the same line; rows condense (6). Section history entries get a visible Before → After block (data already in metadata) (7). Sources panel expanded: title+shield rises to the collapse-caret row (8). Fact row actions: pencil icon right of the fact opening the edit/remove menu — replaces the `⋮` for fact rows (9). The add-section plus moves below the last content tab (above Missing); tabs shift up (10). Export menu labels: `Export client map (DOCX)` / `Export client map (PDF)` (15). Client Map/Documents/Email/Meetings tabs left-align next to the `⋮` on that row (16).

## Lane MEETASK — worktree ~/lp-fb2-meetask
Lines 11, 12: notice-not-found alert becomes a small quiet notice with a menu of the resolution options (protected: strict-quarantine stays loud) (11a). Meeting rail rows: title·date·duration·status HORIZONTALLY condensed, no tall blank rows (11b). Rename-in-place: pencil icon in rail rows for meetings AND Ask conversations (12).

## Lane DOCS — worktree ~/lp-fb2-docs
Lines 21, 22: rail width + divider color match the other rails (21). Files tab row gets the search icon (searches all files incl. inside folders); New-file button moves into the rail's tab row; separator line between the "All files" tab (renamed from "Files") and the file list; the big top search bar is REMOVED in favor of the rail search icon (22). Adopt RAILCHROME's header when its branch appears.

## Lane SETTINGS — worktree ~/lp-fb2-settings
Lines 17, 24, 26, 27, 28, 29 (scout findings in coordination/briefs/fb2-scout-findings.md refine these): client rail list container must fill down to the account area (17, if confirmed). Settings batch (24): autosave delay shows units; letterhead template gets a hint + example; show-hidden-files explains what; REMOVE the tab-scroll setting and (if scout confirms unused) reopen-last-workspace; the two AI destination cards get real visual weight + obvious selected state; Local AI status/install lives INSIDE the on-this-computer card; Cloud card houses key management (which keys work, add/change/remove) + current model (27); email-updates empty state says "You haven't signed up for product emails"; voice hotkey rows either work or hide (per scout); extensions/marketplace hidden behind the existing flag if not live; Settings rail adopts the app-standard rail look with a CLEAR selected state; Help: Setup section to the top. Egress pill (26+29): clicking opens the IN-APP settings AI section (no separate modal ever); the standalone info icon goes away — data-flow info moves into the pill's hover tooltip. Privacy center (28): replace "generate firm security overview" with Export privacy center → Word or PDF (user picks).

## Lane CLIENTUX (Opus 4.8 — design-critical) — worktree ~/lp-fb2-clientux
Line 14 + 13. **Add-client overhaul (14), UX spec from the coordinator:** creating a client = ONE small modal — display name (required), nothing else mandatory; on create, land INSIDE the new client's Client Map (its Missing panel is the natural next step); the clients table below must NOT auto-expand every client's details — the table stays a clean scannable list (name · created · menu); enrichment happens inside the client, never in the create flow. Ruthlessly remove containers-in-containers and helper text; match the calm of the rest of the app. **Client groups (13), TDD:** plus/menu in the CLIENTS rail header offers "New group" → modal names it → searchable multi-select adds clients; groups render as collapsible sections at the top of the client list under All clients; persistence local-first alongside the client store (no wire-schema changes, nothing leaves the machine); a client can be in multiple groups; empty groups deletable. Full adversarial Codex review before merge (feature + data).

## Coordinator-reserved (not a lane)
Line 25: onboarding/guided-setup REVERT to the pre-preview screens (file-level restore, coordinator surgery after scout confirms the file set).
