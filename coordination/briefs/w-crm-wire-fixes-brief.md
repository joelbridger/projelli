ROLE: CRM wire-fix worker for the Lantern-Plus program. A LIVE probe against the real Wealthbox sandbox API (evidence: `docs/evidence/windows-smoke-2/WEALTHBOX-PROBE.md` on branch `lp/windows-smoke-evidence` — read it in full via `git show origin/lp/windows-smoke-evidence:docs/evidence/windows-smoke-2/WEALTHBOX-PROBE.md`) found 2 REAL wire-contract bugs in currently-DORMANT write paths. They MUST be fixed and merged before any field-update/task producer UI ships (Wave-3 wiring will light these paths up). This is Rust-heavy; robust solutions only — no shortcuts.

WORKDIR: ~/lp-crmfix (git worktree, branch lp/crm-wire-fixes off current origin/lantern-plus tip — pull first). NEVER touch ~/keepance or ~/lantern-plus directly. NOT self-merged — the coordinator merges.

READ IN ORDER: LANTERN-PLUS.md → the probe evidence above → `src-tauri/src/commands/crm/write.rs` (the whole write path; anchors below verified from the probe but re-verify by symbol).

BUG 1 — task `due_date` is REQUIRED by the real Wealthbox API (probe Finding 1):
- Evidence: `POST /tasks` without `due_date` → HTTP 422. Code treats it as optional (`write.rs:137-144` area). Today the 422 surfaces as a raw error string in the ledger — not an advisor-facing message.
- Fix (robust route, decided): real client-side REQUIRED validation for Wealthbox task creation — reject at the command boundary with a structured, plain-language error BEFORE any network call ("Wealthbox tasks need a due date"), typed so a future UI can render it properly. Do NOT silently default a date (that invents advisor data). Cover: the Rust command validation + a wiremock regression test asserting no HTTP request is made when due_date is missing + the error shape.

BUG 2 — `background_information` write-direction wire name is WRONG (probe Finding 2, silent no-op):
- Evidence: `wealthbox_wire_field_name` (`write.rs:992-1016` area) translates `background_information` → `background_info` for BOTH directions. Reads: correct (GET returns `background_info`). Writes: WRONG — `PUT /contacts/{id}` with `{"background_info": ...}` returns 200 but silently changes NOTHING; the API wants the literal `background_information` key on writes. Confirmed live on both Household and Person records.
- Fix (robust route, decided): SPLIT the translation into explicit read-direction and write-direction maps (two functions or a direction enum — pick the cleaner seam; read `codebase-design` conventions in the file first). Writes send the literal `background_information`. PLUS post-write READBACK VERIFICATION: after any field-update PUT, GET the field back and verify the new value actually took; a silent no-op (200 but unchanged) becomes a real, typed error into the ledger — this catches the entire "200-but-ignored" bug class forever, not just this field. PLUS wiremock regression tests: (a) a mock that accepts one key and echoes the old value for the other — asserts the write uses the literal name; (b) a readback-mismatch scenario — asserts it surfaces as an error, not success.

NON-NEGOTIABLES: never rename `matter_id`/`Matter`. No cloud calls in tests (wiremock only; NEVER hit the real API from this lane — the probe already did that part). Stay in-lane: `src-tauri/src/commands/crm/` + its tests + any TS type that mirrors the error shape. No new interactive-menu widgets. If a fix seems to need edits outside the lane, STOP and ask in plain text prefixed COORDINATOR:.

ENVIRONMENT: export CARGO_TARGET_DIR=$HOME/.cargo-target-lp-crmfix in EVERY shell (warm-seeded for you; symlinks to the new NVMe). Wrap EVERY cargo test in `timeout 1200 …`. One cargo at a time in your lane. Full check before handoff: scoped `cargo test` (crm) + `npx tsc --noEmit` + scoped `npx vitest run` if TS touched.

RULES: TDD (red wiremock test first per bug); one commit per bug minimum; self-converge via codex-review on your own diff to a clean round (2 clean rounds — this is wire-contract/correctness class) before handoff. Evidence handoff: HEAD SHA, test counts before/after, files touched, decisions/drifts, "NOT self-merged". THEN print the sentinel as the very LAST line: WORKER-DONE: lp/crm-wire-fixes ready for review
