# Aggressive hunt — bench-1: ISOLATION + outbound guards (non-audio)
**Lane:** cc-lantern-hunt1 · dir `~/lp-hunt1` (worktree on a fresh evidence branch `lp/hunt1-evidence` off lp/windows-smoke-evidence). **Model:** Sonnet 5 · high. **Seat:** Azure bench-1 (just started; ~1-2 min). Deallocate when done. `127.0.0.1` tunnel, unique port.
**Read:** coordination/QA-HUNT-CAMPAIGN.md + BUG-DB QA-52..59. Update VM to current origin/lantern-plus tip, rebuild, RECORD the tip SHA.
**Mission — try HARD to make client data bleed, live:** (1) LIVE-REPRODUCE the race-sweep P0s: open workspace A then quickly B (QA-52 — land on wrong one?); open email A, start "file to client"/"Draft with AI", switch to email B before it returns (QA-53 — B filed to A's client / A's draft in B's box?); verify citation A then rerender to B fast (QA-56); privilege-explainer demo on query A, change question mid-run (QA-57); memory-facts in workspace A, switch to B (QA-58). Switch FAST, mid-async, repeatedly. A confirmed live repro is gold. (2) Tier B outbound guards: try to send an UNREVIEWED/needs-review meeting note to Wealthbox or a follow-up email (should be blocked); check AI-drafted CRM notes carry provenance. (3) Free-hunt anything else that surprises. File findings to BUG-DB (IDs from QA-60), screenshots, honest real-or-setup call. Deallocate bench-1. Last line: `WORKER-DONE: hunt1`
