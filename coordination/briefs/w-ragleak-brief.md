# URGENT investigate — QA-68: does Ask/RAG leak content ACROSS workspaces? (potential worst leak)
**Lane:** cc-lantern-ragleak · dir `~/lp-ragleak` (worktree on fresh `lp/ragleak-evidence` off lp/windows-smoke-evidence). **Model:** Sonnet 5 · high. **Seat:** Azure bench-2 (RUNNING, has a working cloud AI key — USE CLOUD-AI mode so inference is FAST; hunt1 couldn't test this because the local model was too slow). Deallocate when done. Unique tunnel port.
**Read:** BUG-DB QA-68 + QA-62. Update VM to current origin/lantern-plus tip, rebuild, RECORD tip SHA. (If the tip still has the QA-60 boot-collision, apply hunt2's local-only workaround — rename meetingNoteOutboundGate.ts → meetingNoteOutboundGateCore.ts + fix its importer + clear node_modules/.vite — to get the app to boot, and NOTE you did.)
**The question (potential P0 content leak):** QA-62 proved the Client Map shows the OLD workspace's clients after switching workspaces. QA-68 asks the scarier question hunt1 couldn't test: can an ASK scoped to one of those stale cross-workspace clients actually RETRIEVE real document content from the OTHER workspace? The RAG/LanceDB store is under a global ~/.keepance (per CLAUDE.md), maybe NOT workspace-scoped — which would bypass PathValidator's filesystem protection entirely.
**Test (use CLOUD AI mode — fast inference):** (1) Workspace A: create a client, add a distinctive document (e.g. "Roth Conversion Plan: target $2.4M by 2040"), let it index, ask a question and confirm the citation returns that content. (2) Switch to a fresh empty Workspace B (via Recent Projects). (3) With the stale client still showing (QA-62), ask a question scoped to it that would only be answerable from Workspace A's document. (4) Does the answer return Workspace A's real content? If YES → P0 cross-workspace content leak (isolation broken at the RAG layer). If NO (blocked/empty) → RAG is workspace-scoped, downgrade QA-68. Screenshot everything.
**Reporting:** File the verdict to BUG-DB (QA-68 status: CONFIRMED P0 / or downgraded), evidence under docs/evidence/ragleak-20260705/, commit+push (branch-check). Deallocate bench-2. Last line: `WORKER-DONE: ragleak`
**Landmines:** No product code (the boot workaround is bench-only, uncommitted). Never touch ~/lantern. No interactive menus.
