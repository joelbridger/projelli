# FB2 scout findings + coordinator rulings (for the settings + clientmap lanes)

1. **All Clients double-highlight CONFIRMED** (clientmap lane): App.tsx:1956-1970 keeps activeTab='matters' in All Clients mode; Spine.tsx:261-289 highlights the Client Map nav while 472-505 highlights the All Clients row. Fix: when All Clients mode is active, the nav tab must NOT render active.
2. **Client list half-height CONFIRMED** (settings lane): Spine.tsx:421-427 `maxHeight: 280` + section `flex: none` (314-319). Fix: the clients section flexes to fill down to the account area; list scrolls within it.
3. **Extensions marketplace is REAL** (GitHub-backed catalog, install works). RULING: do not delete — add a feature flag, DEFAULT OFF, hiding the whole Extensions block in Advanced (SettingsContent.tsx:737-790). One-line re-enable later.
4. **"Manage AI rules" works but silently no-ops without a workspace** (useAIRulesFile.ts:25-27). RULING: disable the button with hint "Open a workspace first" when no workspace; add one line under the row: "Opens ai-rules.md — standing instructions the AI follows in every chat."
5. **Voice hotkey rows have NO renderer** (schema type `shortcut-display`, SettingsContent falls through at 286-287). RULING: render read-only shortcut chips with a short explanation line; no rebinding this batch.
6. **Jameson's removal guesses — one right, one wrong:** `tabOverflow` = vestigial in the vertical-rail UI → HIDE the setting from the schema UI, keep the code path (note for later cleanup). `Reopen last workspace` = LOAD-BEARING (drives the boot auto-resume shipped this week, App.tsx:1124-1137) → KEEP, reword the label/description so its value is obvious ("Reopen where you left off when the app starts").
7. Onboarding revert = coordinator surgery (in progress).
8. **Egress pill opens the settings MODAL over the settings page** (TrustBar → EV_OPEN_SETTINGS → SettingsModal). RULING per Jameson: the pill (and any in-app trigger while the app shell is available) navigates to the IN-APP Settings surface deep-linked to AI; audit other EV_OPEN_SETTINGS dispatchers and keep the modal ONLY where no shell exists (e.g., mid-onboarding). The standalone info icon next to the pill is removed; its data-flow content moves into the pill hover tooltip.
