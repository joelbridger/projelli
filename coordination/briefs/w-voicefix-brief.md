# Build brief — productize the voice sidecar: real engine contract + CI staging (no more never-tested protocol)

**Lane:** cc-lantern-voicefix · dir `~/lp-voicefix` (own worktree, branch `lp/voice-sidecar-contract`). **Model:** Sonnet 5 · high.
**Rules:** NO-SHORTCUTS (core feature, currently ships DISABLED in installers). TDD. Rust rules: `export CARGO_TARGET_DIR=$HOME/.cargo-target-lp-voicefix` (seed by symlinking/copying from a warm cache on /mnt/devcache if useful), ONE cargo at a time box-wide, `timeout 1200` on every cargo test. Codex self-review foreground/watched (backgrounded codex dies under load). Stay in your lane: `src-tauri/src/commands/voice.rs`, `parakeet.rs`, release.yml's voice staging, related scripts/tests. Do NOT touch the meetings UI or transcription windowing (other history there). No interactive menus.

## Context — read FIRST
`docs/evidence/meetings-verify-20260704/RUN-LOG.md` → the **SIDECAR STAGING addendum** (on branch lp/windows-smoke-evidence — read it from ~/lp-bench or git show). Summary: the app's sidecar-talking code was written before any real engine existed and speaks a wrong contract — it passes `--stdin --no-timestamps --model <tier>`, but real whisper.cpp (`whisper-cli`) wants `-f <file> -np -nt -m <model-file-path>` (no stdin mode, model = path not tier name). CI's "Fetch voice sidecar binary" step is a documented no-op (`VOICE_SIDECAR_URL` never pinned; V1_5_RELEASE.md M6 "Done (binary TODO)") — so installers ship voice DISABLED. The bench lane bridged it with a ~50-line translator shim (works, verified end-to-end on the Legion) — you are making the PRODUCT correct instead.

## What to build
1. **Fix the app's contract in Rust** (preferred over shipping a shim): `build_transcribe_args`/`resolve_sidecar_path` should speak real whisper.cpp's actual CLI — write the WAV to a temp file (or confirm a stdin mode genuinely exists in a pinned engine version — verify against the real engine's --help, don't assume), pass `-f <file> -np -nt -m <model-path>`, map UI tiers (tiny/base/small) to model file paths with an honest fallback when a tier's model isn't present. Clean up temp files. Keep the parakeet naming/back-compat facade intact (never rename `matter_id`-class identifiers; check what's locked in ARCHITECTURE.md).
2. **Tests:** unit-test the arg-building against the REAL contract (table-driven: tier→model-path, temp-file lifecycle, missing-model fallback). If a real engine binary isn't available on this Linux server, build whisper.cpp locally for the integration test if practical (cargo/cmake, ~40s per the bench lane) — a real end-to-end transcode test of a generated tone/sample is the gold standard; if genuinely impractical here, say so honestly and cover with contract-level tests.
3. **CI staging (release.yml):** stage the engine + a pinned model per-platform the same way the diarize sidecar was staged (see lp/diarize-release-staging + the DLL-collision lesson @7f46420b — engine DLLs must live in their own subfolder). Building whisper.cpp in CI or pinning released binaries — your call, state trade-offs. DRY-RUN validation only — NEVER cut a real release (deploy boundary).
4. Update the Legion note: once the product contract is fixed, the bench's translator shim becomes obsolete — note in your handoff that the next bench pass should stage the raw engine per the new contract (don't touch the Legion yourself; meetverify2 owns it right now).

## Gate + handoff
`npx tsc --noEmit` · full `npx vitest run` · eslint-gate · i18n 0 · **cargo test green** (timeout-wrapped, own target dir). PULL + reconcile before handoff. Handoff: HEAD SHA, gate counts incl cargo, engine-verification honesty (what was tested against a real binary vs contract-level), CI approach + trade-offs, self-review rounds. Push (NOT self-merged), then exactly: `WORKER-DONE: lp/voice-sidecar-contract`
