codex
Read-only check complete. I changed nothing.

**R2 Clients List**

| Requirement | Exact place | Approach | Risk |
|---|---|---|---|
| One-line client rows | [MattersHome.tsx](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:285) `MatterRow`; row body at [312](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:312); quick-action row at [415](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:415) | Merge the current second action row into the main row. Put name first, then Ask/Documents/Email/Meetings/Activity buttons, then Created, then menu. | Needs careful width handling so five buttons do not crush long client names. |
| Add Meetings + Activity row buttons | Current action type only allows `search/files/email` at [282](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:282); event router maps hub subtabs at [useGlobalEventBus.ts](/home/jameson/lantern-plus/src/app/lifecycle/useGlobalEventBus.ts:152); valid hub tabs include meetings/activity at [matterStore.ts](/home/jameson/lantern-plus/src/platform/matter/matterStore.ts:85) | Extend `MatterSurface` to include `meetings` and `audit`; map `meetings -> meetings`, `audit -> activity` in the event router. | `meetings` is not in `ALLOWED_SURFACES` today, so a button will silently fail unless router support is added. |
| Archive moves into 3-dot menu | Archive button is currently visible at [MattersHome.tsx](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:460) | Replace it with a `MoreHorizontal` icon button plus existing `DropdownMenu` components from [ui/index.ts](/home/jameson/lantern-plus/src/ui/index.ts:32). Keep Archive as first menu item. | Add a blank action column to the table/header, or the menu will fight with Created date alignment. |
| Row columns/header | Grid columns are defined at [MattersHome.tsx](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:70); header is [565](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:565) | Update the grid to include action-buttons and menu columns. If R1 removes the Documents column first, build R2 on that new shape, not today’s file-count column. | R1 and R2 overlap around the Documents column. Merge order matters. |
| New Client + Clients/Whole-Book toggle same height and adjacent | Toolbar starts at [822](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:822); New Client at [824](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:824); toggle at [759](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:759) | Remove the spacer/label, use matching sizes, and keep the button and segmented toggle as a tight group in `SurfaceToolbar`. | R1 already says remove the “View” label, so avoid doing that twice in separate branches. |
| Light lines + tab styling | List card starts at [850](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:850); row borders at [307](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:307); individual-client tabs at [MatterHub.tsx](/home/jameson/lantern-plus/src/features/matters/MatterHub.tsx:329) | Reuse the hub tab style: soft selected tint, light dividers, no heavy card feel. | Keep the list as one surface, not cards-inside-cards. |

**R3 Navigation**

| Requirement | Exact place | Approach | Risk |
|---|---|---|---|
| Where main tab clicks resolve | Rail tab button calls `onTabChange` at [Spine.tsx](/home/jameson/lantern-plus/src/app/shell/layout/Spine.tsx:145); App handles it at [App.tsx](/home/jameson/lantern-plus/src/App.tsx:1623); router renders surfaces at [AppSurfaceRouter.tsx](/home/jameson/lantern-plus/src/app/shell/AppSurfaceRouter.tsx:371) | This is the right path for Client Map tab behavior. Change it centrally, not inside every page. | Current code clears the open client map on Client Map click at [App.tsx:1638](/home/jameson/lantern-plus/src/App.tsx:1638), which is the opposite of R3. |
| Rail client click should open that client map | Rail client rows are [Spine.tsx](/home/jameson/lantern-plus/src/app/shell/layout/Spine.tsx:184); click dispatches no surface at [189](/home/jameson/lantern-plus/src/app/shell/layout/Spine.tsx:189); event bus handles launch at [useGlobalEventBus.ts](/home/jameson/lantern-plus/src/app/lifecycle/useGlobalEventBus.ts:141) | Send `{ matterId, surface: 'matters' }` from the rail. The router already maps explicit `matters` to the hub overview at [160](/home/jameson/lantern-plus/src/app/lifecycle/useGlobalEventBus.ts:160). | This fixes B3 directly. Without `surface: 'matters'`, old “restore last surface” behavior can land on the list. |
| Client Map tab: no selection = blank canvas | Current fallback is the full clients list at [MattersHome.tsx](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:729) and [802](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:802) | Split “Clients list” and “Client Map” as two modes or two surfaces. In Client Map mode: if no active client, render a simple empty state: “Click a client on the left.” | Current code has one `matters` surface doing both jobs. R3 needs that split to avoid fighting old behavior. |
| Client Map tab: with selection = selected client map | Hub opens when `clientMapHubId === activeMatterId` at [MattersHome.tsx](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:659) | On Client Map tab click, set `clientMapHubId(activeMatterId)` when there is an active client. Do not clear it. | `setActiveMatter` clears mismatched hubs at [matterStore.ts](/home/jameson/lantern-plus/src/platform/matter/matterStore.ts:1467), so order matters: set active client first, then hub id. |
| Left-rail search bar | Clients section begins at [Spine.tsx](/home/jameson/lantern-plus/src/app/shell/layout/Spine.tsx:161); list renders at [179](/home/jameson/lantern-plus/src/app/shell/layout/Spine.tsx:179) | Add `clientSearchQuery` state in `Spine`, filter `matters`, and slot the search input between the Clients header and the scroll list. | The rail is dark. The normal `SearchField` may need a rail-specific light-on-dark style. Also hide search in collapsed mode. |
| Remove “only” scope badge on map | Scope badge is [MatterHub.tsx](/home/jameson/lantern-plus/src/features/matters/MatterHub.tsx:380) | Remove that badge block. The selected rail client becomes the visible scope cue. | Keep the confidentiality/egress badges at [366](/home/jameson/lantern-plus/src/features/matters/MatterHub.tsx:366) and [389](/home/jameson/lantern-plus/src/features/matters/MatterHub.tsx:389). Those still matter. |
| App-wide Back button | Current navigation state lives in App at [App.tsx](/home/jameson/lantern-plus/src/App.tsx:310); source jumps enter through [useGlobalEventBus.ts:113](/home/jameson/lantern-plus/src/app/lifecycle/useGlobalEventBus.ts:113); Ask source cards dispatch at [SourcePanel.tsx](/home/jameson/lantern-plus/src/features/ask/SourcePanel.tsx:41) | Best hook point: a small new app-navigation store in `src/platform/state`, plus push calls before cross-surface jumps in `useGlobalEventBus`, `useTabOpening`, and App’s nav click handler. Store snapshots of surface, active client, hub id, document view, and active document tab. | Ask’s selected citation/turn is local state in `useAsk` at [useAsk.ts](/home/jameson/lantern-plus/src/features/ask/useAsk.ts:235). Back can return to the same conversation first; restoring the exact selected source panel needs extra Ask state work. |

Recommended first build order: R2 row restructure first, then R3 split “Clients list” vs “Client Map” behavior, then Back button. The main risk is that today `matters` means both “Clients list” and “Client Map,” so R3 should untangle that instead of adding special cases everywhere.
tokens used
174,531
Read-only check complete. I changed nothing.

**R2 Clients List**

| Requirement | Exact place | Approach | Risk |
|---|---|---|---|
| One-line client rows | [MattersHome.tsx](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:285) `MatterRow`; row body at [312](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:312); quick-action row at [415](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:415) | Merge the current second action row into the main row. Put name first, then Ask/Documents/Email/Meetings/Activity buttons, then Created, then menu. | Needs careful width handling so five buttons do not crush long client names. |
| Add Meetings + Activity row buttons | Current action type only allows `search/files/email` at [282](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:282); event router maps hub subtabs at [useGlobalEventBus.ts](/home/jameson/lantern-plus/src/app/lifecycle/useGlobalEventBus.ts:152); valid hub tabs include meetings/activity at [matterStore.ts](/home/jameson/lantern-plus/src/platform/matter/matterStore.ts:85) | Extend `MatterSurface` to include `meetings` and `audit`; map `meetings -> meetings`, `audit -> activity` in the event router. | `meetings` is not in `ALLOWED_SURFACES` today, so a button will silently fail unless router support is added. |
| Archive moves into 3-dot menu | Archive button is currently visible at [MattersHome.tsx](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:460) | Replace it with a `MoreHorizontal` icon button plus existing `DropdownMenu` components from [ui/index.ts](/home/jameson/lantern-plus/src/ui/index.ts:32). Keep Archive as first menu item. | Add a blank action column to the table/header, or the menu will fight with Created date alignment. |
| Row columns/header | Grid columns are defined at [MattersHome.tsx](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:70); header is [565](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:565) | Update the grid to include action-buttons and menu columns. If R1 removes the Documents column first, build R2 on that new shape, not today’s file-count column. | R1 and R2 overlap around the Documents column. Merge order matters. |
| New Client + Clients/Whole-Book toggle same height and adjacent | Toolbar starts at [822](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:822); New Client at [824](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:824); toggle at [759](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:759) | Remove the spacer/label, use matching sizes, and keep the button and segmented toggle as a tight group in `SurfaceToolbar`. | R1 already says remove the “View” label, so avoid doing that twice in separate branches. |
| Light lines + tab styling | List card starts at [850](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:850); row borders at [307](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:307); individual-client tabs at [MatterHub.tsx](/home/jameson/lantern-plus/src/features/matters/MatterHub.tsx:329) | Reuse the hub tab style: soft selected tint, light dividers, no heavy card feel. | Keep the list as one surface, not cards-inside-cards. |

**R3 Navigation**

| Requirement | Exact place | Approach | Risk |
|---|---|---|---|
| Where main tab clicks resolve | Rail tab button calls `onTabChange` at [Spine.tsx](/home/jameson/lantern-plus/src/app/shell/layout/Spine.tsx:145); App handles it at [App.tsx](/home/jameson/lantern-plus/src/App.tsx:1623); router renders surfaces at [AppSurfaceRouter.tsx](/home/jameson/lantern-plus/src/app/shell/AppSurfaceRouter.tsx:371) | This is the right path for Client Map tab behavior. Change it centrally, not inside every page. | Current code clears the open client map on Client Map click at [App.tsx:1638](/home/jameson/lantern-plus/src/App.tsx:1638), which is the opposite of R3. |
| Rail client click should open that client map | Rail client rows are [Spine.tsx](/home/jameson/lantern-plus/src/app/shell/layout/Spine.tsx:184); click dispatches no surface at [189](/home/jameson/lantern-plus/src/app/shell/layout/Spine.tsx:189); event bus handles launch at [useGlobalEventBus.ts](/home/jameson/lantern-plus/src/app/lifecycle/useGlobalEventBus.ts:141) | Send `{ matterId, surface: 'matters' }` from the rail. The router already maps explicit `matters` to the hub overview at [160](/home/jameson/lantern-plus/src/app/lifecycle/useGlobalEventBus.ts:160). | This fixes B3 directly. Without `surface: 'matters'`, old “restore last surface” behavior can land on the list. |
| Client Map tab: no selection = blank canvas | Current fallback is the full clients list at [MattersHome.tsx](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:729) and [802](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:802) | Split “Clients list” and “Client Map” as two modes or two surfaces. In Client Map mode: if no active client, render a simple empty state: “Click a client on the left.” | Current code has one `matters` surface doing both jobs. R3 needs that split to avoid fighting old behavior. |
| Client Map tab: with selection = selected client map | Hub opens when `clientMapHubId === activeMatterId` at [MattersHome.tsx](/home/jameson/lantern-plus/src/features/matters/MattersHome.tsx:659) | On Client Map tab click, set `clientMapHubId(activeMatterId)` when there is an active client. Do not clear it. | `setActiveMatter` clears mismatched hubs at [matterStore.ts](/home/jameson/lantern-plus/src/platform/matter/matterStore.ts:1467), so order matters: set active client first, then hub id. |
| Left-rail search bar | Clients section begins at [Spine.tsx](/home/jameson/lantern-plus/src/app/shell/layout/Spine.tsx:161); list renders at [179](/home/jameson/lantern-plus/src/app/shell/layout/Spine.tsx:179) | Add `clientSearchQuery` state in `Spine`, filter `matters`, and slot the search input between the Clients header and the scroll list. | The rail is dark. The normal `SearchField` may need a rail-specific light-on-dark style. Also hide search in collapsed mode. |
| Remove “only” scope badge on map | Scope badge is [MatterHub.tsx](/home/jameson/lantern-plus/src/features/matters/MatterHub.tsx:380) | Remove that badge block. The selected rail client becomes the visible scope cue. | Keep the confidentiality/egress badges at [366](/home/jameson/lantern-plus/src/features/matters/MatterHub.tsx:366) and [389](/home/jameson/lantern-plus/src/features/matters/MatterHub.tsx:389). Those still matter. |
| App-wide Back button | Current navigation state lives in App at [App.tsx](/home/jameson/lantern-plus/src/App.tsx:310); source jumps enter through [useGlobalEventBus.ts:113](/home/jameson/lantern-plus/src/app/lifecycle/useGlobalEventBus.ts:113); Ask source cards dispatch at [SourcePanel.tsx](/home/jameson/lantern-plus/src/features/ask/SourcePanel.tsx:41) | Best hook point: a small new app-navigation store in `src/platform/state`, plus push calls before cross-surface jumps in `useGlobalEventBus`, `useTabOpening`, and App’s nav click handler. Store snapshots of surface, active client, hub id, document view, and active document tab. | Ask’s selected citation/turn is local state in `useAsk` at [useAsk.ts](/home/jameson/lantern-plus/src/features/ask/useAsk.ts:235). Back can return to the same conversation first; restoring the exact selected source panel needs extra Ask state work. |

Recommended first build order: R2 row restructure first, then R3 split “Clients list” vs “Client Map” behavior, then Back button. The main risk is that today `matters` means both “Clients list” and “Client Map,” so R3 should untangle that instead of adding special cases everywhere.
